# Fidelity ledger

The reimplementation separates verified original values from behavior inferred where the 1995 executable and manual do not expose an exact formula.
Binary-derived claims and their confidence levels are recorded in [`original-findings.md`](original-findings.md); provisional values must not be presented as original facts.

## Verified balance encoded verbatim

- Attribute ranges and qualitative ranks (0-20)
- Template values documented for Ronald, Hayward, Spencer, Mordred, and Simon
- Custom starting wealth of 240 shillings and baseline equipment
- Crop cost and monthly/harvest revenue at 50% productivity
- Forest cost, labor, and revenue at 50% productivity
- Army purchase and upkeep values at 50% and 100% productivity
- Swordsmen beat halberdiers, halberdiers beat knights, knights beat swordsmen
- Steward +10%, beadle +5%, priest +5%, church +15%, monastery +15%, woodward +5%
- One house and one food tile per 100 people; 25% bean requirement; July penalties
- 200-shilling loan ceiling with 50% harvest interest; spy price of 80
- Shop prices, 75% resale rule, armor bar values, and crossbow availability
- The original store table order, item-image indices, local descriptions, Fighter's Dagger price of 84 shillings, and `BUYSELL.CSF` view/transaction labels
- Three jousts and one skirmish per tournament, lance experience cap of 20
- Joust and melee wager dialogue, five selectable melee opponents, and the documented typical 20-80 shilling wager range
- Lady-specific colors, piety/fame restrictions, win-indexed courtship rewards, and simultaneous courtships
- Dragon route requirements: Jane's lance, Wendessa's Shield of St. George, Valetta's armor, and Mighty strength
- Thirty imported youth-dilemma definitions grouped five per age from 12 through 17, with executable-confirmed random selection and inclusive breakpoint outcome rules
- Age 18 campaign start, age 30 deadline, crown and dragon endings

## Inferred and isolated

- Intermediate productivity interpolation for unit prices/upkeep is linear between the documented endpoints.
- Population growth follows the documented 1,200-population trial bands and scales by productivity.
- Field battle casualties preserve the documented counter triangle but use a seeded attrition calculation because exact executable coefficients are unavailable.
- Tactical field battles expose independent swordsman, halberdier and knight formations with live hold, advance, flank, captain-control and withdrawal orders. The exact original formation geometry remains under investigation.
- The original first-person hit threshold, damage dice, armor penetration, reach, player attack skill, player maximum-health formula, authored friendly/hostile partition, campaign retainer cap and strategic casualty conversion, four retainer commands and their local-defend/seek/follow/flee intents, death completion, and no-enemy-loot behavior are executable/resource-confirmed. Exact thinker cadence equivalence, fixed-point movement/pathfinding, direct target selection, critical-hit presentation/semantics, and several other consequence tables remain under investigation.
- Costs not given by the available manual/FAQ (castle staff and civic construction) are centralized in `Campaign.Build` pending executable observation.
- World coordinates, road-distance conversion, garrisons, and the tournament circuit are provisional pending extraction of the original map tables.
- Tournament opponent identifiers are partially recoverable from `CONQUER.EXE`; the current wager progression, difficulty tolerances, and individual eight-man compositions remain provisional pending disassembly or controlled observation.

## Media status

The verified local manifest currently contains 29,226 assets. This supersedes the older 16,092 count retained in the historical status paragraph below, which predates broader nested-resource import coverage. Runtime support now requires this owner-supplied official extraction and rejects incomplete or damaged manifests before graphics initialization.

The owned release contains a 35,361,714-byte `.RES`-signature `C1086.GOB` archive, a raw MODE1/2352 CD data track, thousands of Smacker (`.SMK`) sequences, Sierra `.RES/.LOW` resources, and five CD-audio tracks. `tools/Conqueror.Inspect` inventories that data track; `tools/Conqueror.Import` installs owned resources locally with a provenance manifest and lossless CDDA WAV conversion. Generated proprietary files remain ignored and are not redistributed. The importer can now independently verify every manifest path, length, and digest or rebuild and verify generated content from the read-only installation; the current local 16,092-asset catalog passes that end-to-end check. The bounded kind-1 archive decoder is validated across all 32,089 GOB and scene blocks, while the executable-confirmed kind-2 LZW decoder validates all five of its GOB entries and all four resulting 640x480 PCX images. All 12,982 scene `TEX` entries validate as headerless byte planes whose names encode their exact dimensions. The complete `Pal0`-`Pal127` families validate as 256-entry index remaps; generation under `SKIRMISH.PAL`, Scenario-driven dispatch, distance scaling, and north/east/south/west surface ordering are executable-confirmed, and all 90 stored active families reproduce byte-for-byte. First-person Viewer, Scenario, Map, Blocks, Backdrop, and BackImage records are now bounded and decoded: the 128x128 column-major maps provide original starts, named collision/interaction tiles, and defender/champion placements to melee and castle-skirmish sessions, while a map-aware raycast view presents that geometry with the original wrapping panorama, indexed-distance-shaded wall and door surfaces, animated combatants, and wall occlusion. The bounded conversation decoder validates all 1,311 `ALL.CIF`/`ALL.CBF` nodes, 2,062 prompt variants, 2,696 response branches, 63 zero-response continuation slots, 423 node-action references, and 2,318 response-action references; the ten populated-inn selector roots resolve to their matching named portrait nodes. Its bounded action layer validates all 689 `ALL.TMI` groups, 2,267 actions, 10,373 expressions, 13,352 values, and the 190-value `ALL.VTB` initializer. The runtime executes that grammar for persistent script variables and redirects; scope-1 selectors now mutate typed wealth, honor, fame, piety, strength, stamina, and intelligence, while item IDs 0-23 synchronize saved counters with named visible inventory. The bounded SMK implementation validates and decodes all 2,131 movie headers, 182,360 indexed-video frames, and 161,884 packed-audio packets directly; the original title, credits, `WEAPONS.DAT` item movies, dragon-lair approach `TRANDRAG.SMK`, and all three dragon outcomes are now bound to a file-backed MonoGame texture/audio adapter with timing, skip, end, and return handling. The dragon encounter uses the original 640x480 `DRJSTWIN.PCX` screen and its palette with the 25 decoded transparent `lance1.CSF` frames; target movement, timing, hit window, and frame placement remain Provisional. All 26 `.666` sound banks decode into 102 bounded rate-tagged unsigned 8-bit mono samples. At startup every installed bank is decoded once and every sample is converted once to a retained signed 16-bit session buffer; the shared interface sample is bound to mouse activation when effects are enabled, while other event bindings remain incomplete. The runtime decodes the complete conversation/action databases and every referenced portrait once at startup. It enters the inn's confirmed patron roots, executes their selector programs, selects original prompt variants, draws word-wrapped original speakers/prompts/responses, accepts recovered mouse rows and number keys, applies response actions, follows redirects and graph targets, times zero-choice continuations, and terminates on target zero. Exact army path interaction, exact spy timing/cost, `JUMP!!` behavior, further conversation entry points, remaining first-person state transitions, SMK event bindings and seeking, proportional original font metrics, broader palette associations, village/location mapping, and other Sierra payload formats remain incomplete.

## Controls represented

The older “retainer commands remain Provisional” text below is superseded: Attack, Defend, Follow, and Retreat write requested actor modes 6, 2, 16, and 10; Follow targets player actor `D4D0`; and every order uses the selected-or-all fallback before clearing selection. Their state branches further confirm that Defend reacts only inside a surrounding 3x3 neighborhood, Attack seeks the nearest hostile, Follow approaches the player, and Retreat moves away from its hostile target. Those command identities, original shell regions, dispatch rules, and intents are Confirmed and active through explicit authored actors. The runtime's cardinal grid movement and spatial hostile targeting replace the old aggregate ally-interception chance but remain Provisional approximations of the original fixed-point traversal. Formation, direct actor/cell targeting, precise obstruction/path selection, and thinker cadence remain open.

The older “direct actor/cell targeting remains open” wording is now narrowed. Executable dispatcher `0x55524` and raycaster `0x470A8` confirm exact actor/object identity, friendly selection, selected-retainer hostile targeting through mode 8, player attack fallback, alpha-mask rejection, and the `< 0x280` explicit-action limit. Those semantics are active. The runtime picker shares billboard geometry with drawing and evaluates wall depth, near-to-far occlusion, and decoded texture alpha, so clicks select what the current renderer visibly presents. Its floating-point projection is a Corroborated reimplementation mapping, not yet a claim of arithmetic identity with the original fixed-point raycaster. Empty-ground mode-12 orders and exact contact coordinates remain open alongside fixed-point movement/pathing.

The earlier status text in this section that calls defeated-enemy loot Provisional is superseded by the completed death-path trace: actor death and completion never reference wealth, ammunition, or equipment, while their complete executable reference inventories place combat mutations in authored pickup and crossbow paths. Defeated-enemy loot is **Disproved** for the hashed release, and the runtime correctly has no actor-drop path.

Imported first-person scenes now retain mask-83 exit boundaries and render every non-actor kind-4 pickup, static-debris, and contact-object billboard. The scene record field is a behavior bitmask, not an ordinal identifier. Bit `0x20` is executable-confirmed at `0x55A02`; runtime reach uses combat-row column 4 plus the original `0x40` contact allowance, and the contacted cell is replaced in one hit through its explicit record-offset-`0x40` state target. Owned trees, barrels, stools, pots, and candelabras target floor or ground directly; adjacent damaged-looking records are alternate states rather than an inferred durability sequence. Mask-19 pickups carry explicit-action bit `0x10` and use its `< 0x280` range as a two-cell limit before immediate state replacement. Dispatcher `0x51E60` reads the selected scene block's selector at `+0x48`; `+0x4A/+0x4C` supply its arguments. The only placed actionable rewards are 36 bags of coins (`+25` wealth), 70 meals (`2d6` healing capped at maximum health), 36 Chain Hauberks (equipment bit 6), and 36 piles of bolts (`+12` ammunition). The generic action consumes full-health and duplicate-equipment pickups too. Actor records reuse words `0x4A` and `0x4C` to select one of ten combatant templates and one of 25 combat rows. Imported actors retain the executable-initialized attack skill, armor, health, row-specific reach, and authored side partition. Player skill derives from strength, dexterity, and sword experience; maximum health is strength plus stamina plus honor. Player and enemy strikes now use the confirmed 200-value hit roll, defender-skill reduction, close-crossbow and rear-attack bonuses, followed by the confirmed dice-count, die-sides, armor-penetration, and zero-clamped damage calculation. Retainer commands/movement remain Provisional; defeated-enemy loot is Disproved.

The original cadence architecture, actor state templates, and completion-gate binding are now Confirmed and active. Scene loading reads Scenario offset 28 and loads `count * 0x40` bytes from `SFXDEFS`; constructor `0x4C7F4` maps each descriptor into a live record, and scheduler `0x530D0` advances independent millisecond deadlines. Attack, hit, and death copy adjacent `+1/+2/+3` state blocks, attach each state's descriptor, and resume actor logic only after a strict elapsed-greater-than-duration boundary. Renderer `0x46E3B`-`0x46F23` selects `base surface + angular sector` and mirrors behavior-bit-2 billboards instead of playing neighboring textures sequentially. All 144 placed actor bases in 42 owned combat scenes share `8/4/4/2` angular divisions for base/attack/hit/death and select descriptor 0 in every state: 80 use 384 ms gates and 64 use 400 ms gates, with ranged attack rows 23-24 doubled. Imported layouts now carry those exact gates into the processor-speed-independent simulation clock, and the renderer applies the recovered angle/mirror formula. A universal frame duration, texture-count-as-frame-count, and the former nine-frame attack/eight-frame collapse model are Disproved. Actor thinking remains a separate untimed round-robin pass. Foreground setup now follows the executable's three melee row families, their `8/16`, `40/40`, and `32/24` random spans, viewport-relative outer anchors, mirror branch, signed 8.8 velocity `(target - outer) << 9 / divisor`, contact reversal, and position-selected CSF pose. Runtime updates retain subpixels across whole elapsed milliseconds and cap one update at the original 166 ms, making the result independent of processor throughput and render count; a fixed 70 ms melee frame timer is Disproved. Door actions have no cadence: bit-`0x10` dispatch replaces the map cell immediately through offset `0x40`, disproving the former 360 ms animation. Blood advances at `0x552FB`-`0x55395` through four foreground-render invocations. The main loop calls that renderer once at `0x5841F`, performs framebuffer copies, and jumps directly back at `0x584DF`; it contains neither a clock comparison nor the explicit wait routine at `0x657C0`. Its wall-clock duration is therefore hardware-dependent, so the runtime preserves the exact four steps through a monotonic 70 ms compatibility policy rather than claiming an original duration. The runtime target-coordinate bridge remains open.

The time-based portion is safe to retain because elapsed milliseconds, not processor cycles, gate state changes. The clean-room implementation uses the game framework's monotonic elapsed time, per-record state, retained fixed-point remainders, and bounded catch-up so frame subdivision, stalls, and wall-clock changes cannot destabilize it. The original foreground weapon motion is therefore safe to retain in this adapted form. The original blood overlay is not safe to retain literally because it is render-loop counted and has no stable intended duration to corroborate. `SiegeHitEffect` future-proofs it by retaining the confirmed four-frame order while advancing explicit monotonic steps independently of processor speed and refresh rate; 70 ms is a clean-room compatibility choice, not a recovered formula.

The dragon moor is omitted from selection, drawing, travel, and captain dispatch until the decoded original discovery flag is set. Imported tournament dialogue can enter all six lady selector roots and bridges their current-colors and last-joust variables back to the playable joust. The blacksmith portrait and keyboard route enter original node `3201`; the parish route uses ordinary selector `3100` except at Cambridge, where confirmed Father Hyacinth selector `3149` exposes the armor, Book of Hours, chalice, and bribery branches. Their generated shortcuts remain available without conversation data. Anna Lisa's original secret sets the discovery flag; her second fallback courtship win supplies an equivalent clean-room route, and prototype saves already at the moor migrate safely. A prepared knight arriving at the moor now watches the owned approach sequence and proceeds directly into the dragon encounter. Anna Lisa and Sir Frederick's decoded references to raiding an absent dragon describe Frederick's backstory; no decoded action grants the player lair treasure, so the reimplementation does not fabricate a repeatable plunder system. The six parallel regional priest roots `5000`-`5500` are decoded but remain unassigned until their location mapping is recovered. Unpaid harvest debt now persists into the executable-confirmed Drogo demand: the player may pay the exact amount or enter a no-retainer, no-retreat first-person fight; victory permanently closes the moneylender and defeat is fatal. The demand uses the owned `DROGO.PCC` portrait when installed. Drogo's combat strength and generated arena remain Provisional.

Reimplementation saves are explicitly schema-versioned and written by same-directory replacement. Each of the five slots retains its previous valid generation and transparently exposes that backup if the primary JSON is damaged; unversioned prototype saves and the former single slot-1 filename are migrated on load. A separate transition-triggered autosave uses the same recovery policy, while the load screen distinguishes missing, malformed, inaccessible, invalid, and unsupported-format generations and reports actual backup recovery.

The map contains 18 named destinations with selection and calendar-costed travel, and links the original high-level destinations (Home, Village, Tournament, Overview, field battle, siege, dragon, London). War Planning persists five named divisions, edits 100-serf companies transactionally, enforces the 60-company and home-territory rules, charges upkeep across all divisions, tracks player membership, and assigns recurring spies. The joined division—whichever of the five is selected—supplies travel interception, field-battle survivors, siege retainers, and retreat losses. For each of Swordsmen, Halberdiers, and Knights, campaign assault entry contributes `min(floor(units / 3), 3)` retainers; the three contributions are added with a one-retainer fallback. Each dead retainer removes one represented unit, exhausting those contributions in that same unit-type order. These formulas and the strategic mutation path are Confirmed. The actor loader's predicate, x-major first-friendly player rule, and template 0-2 friendly versus 3/5/8/9 hostile partition are Confirmed across all 1,002 placements in the supported official combat scenes; `SiegeSession` now limits the formula result to available authored retainer positions. Formation, commands, movement AI, and raw block-color normalization remain provisional. Unjoined fielded divisions accept independent dated movement orders, render along their routes, persist in transit, and resolve remote interception with a captain's report; exact path selection and captain battle rules remain provisional. Campaign castles and London use the confirmed common `MELEE0.RES`; practice randomly selects the three executable-confirmed unsuffixed base scenes, while the distinct `MELEE00`-`MELEE24` family is reserved for tournament melee. Melee and castle-skirmish sessions require original 128x128 scene geometry, viewer starts, panorama, door/secret-passage/food/treasure roles, and defender/champion placements; conversion crops each map to the Viewer-connected play area, excluding disconnected authoring galleries outside that component. A map-aware raycast view exposes the actual geometry and wall-occluded defenders inside the exact 167x117 aperture of `SKIRMISH.PCX`. It loads the facing-wrapped `BackImage`, cardinal wall or door slot, and original actor states through the confirmed `SKIRMISH.PAL` association. `SKIRMISH.CSF` supplies the recovered weapon and hit-effect sequences. The original blood frames advance once per unrestricted render loop, so the runtime preserves their sequence with explicit monotonic 70 ms compatibility steps rather than retaining the processor-dependent mechanism. Dead actors stop blocking and attacking immediately but remain visible until their imported death-state gate expires. Actionable doors install their explicit target block immediately; placed pickups apply their exact imported reward and are consumed by the same generic replacement. Movement, facing, radar, interaction, melee and crossbow combat, retainers, equipment breakage, and retreat are playable, but dynamic status fields, exact click-selected world-object semantics, and broader AI remain provisional; defeated-enemy loot is Disproved. London and dragon victory play their executable-confirmed ending sequences, while the age-30 ending remains distinct. The desktop runtime validates a supported official extraction before graphics startup; generated or no-media presentation is not a supported execution path. Save/load are modern additions and do not alter simulation balance.
