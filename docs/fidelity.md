# Fidelity ledger

Pointer/acquisition status correction: later executable-backed work supersedes the older pointer and bounded-visibility provisional wording below. Pointer selection carries source actor/object identity through ordered candidates and uses the recovered live-center, depth, heading-sector, mirror-fold, projected-height, and alpha-column formulas. Opponent acquisition scans authored records through `0x4F98D`, derives the exact non-cardinal byte heading with `0x445C4`, casts the centered `0x470A8` ray, validates identity through `0x4CEA0`, keeps only strictly nearer depth from `0x7FFF`, and stops below `0x154`. Kind-0 Retreat mode 5 uses the separately confirmed same-side `0x4FC34` scan, strict-nearest rule, and `< 0x200` early exit before mode 7 directly approaches the stored ally; mode-7 predicate `0x4F8B0` accepts any returned living same-side actor below `0x200` and enters mode 1. Mode 1 scans the surrounding 3x3 cells in relative y/x order and chooses mode 4 when supported or mode 3 when isolated; those states return through opposite-side pursuit mode 8 or same-side regroup mode 7 respectively, with fallbacks to modes 1/2. Mode 8 applies the source row's strict raw contact distance to the actual opposite-side ray hit and chooses delayed attack mode 11 or wandering mode 6. Mode 11 uses a fixed-Manhattan `<= 0x154` shortcut and otherwise rays again. The later mode-14 hit handler compares stored-target health with the newly reduced source health: a stronger target selects mode 13, whose health-6 predicate returns a healthy friendly actor to mode 2 and sends a lower-health actor through the 1.5-scaled non-cardinal mode-10 escape. The former attribution of that comparison to mode-11 strike rejection is Disproved. The runtime uses the executable's signed 1.15 trig table at object-2 `+0xC904` with `(product + 0x3FFF) >> 15` rounding.

Interactive strategic-encounter status correction: state-zero targeting is no longer a generic provisional overlap test. The executable first excludes the source rectangle, accepts only a living opposing corner hit, and turns immediately if its target field is then non-negative. Only no-target records follow the paired destination at `+0x0C/+0x10`; once facing it, they check Y before X against the start-of-pass rectangle table, use exact near-axis motion or category-dependent 10/5-pixel probing, and preserve same-lane reversal and opposing-contact turn branches. With neither target nor destination, a living control-code-one unit uses positive lane counters to select the first strictly nearest living opponent by truncated Euclidean distance, then derives source/candidate destinations from the selected record. `AdvanceMappedStateZero` composes those verified branches without reproducing the original's invalid stale-register fallback or processor-dependent outer-loop cadence. `AdvanceMappedTacticalPass` then retains the start-of-pass rectangle snapshot through table-order death frames, zero-offset contact probes, due damage, and state-zero re-entry; its caller supplies stable timing. Broader presentation scheduling remains Provisional.

The strategic resolver's own visual assets are verified runtime dependencies: `BATTLE.PCX` is an indexed 1024x728 grass battlefield and `MEN8.CSF` supplies its palette-associated transparent unit frames. The strategic encounter screen uses them with the executable-mapped menu regions, source unit/overlay draw ordering, and special control frames, and keeps a contact active until the typed resolver settlement completes rather than delegating to `FieldBattleSession`. Both the resolver and the older `FieldBattleSession` adapter now fail explicitly if either required source asset or the resolver's three control frames is unavailable; neither may silently omit sprites or substitute a colored/constructed battlefield. The estate map likewise requires the source `ICA`/`ICS`/`ICW` seasonal atlas selected by date and its configured source frames, rather than constructing colored terrain when an atlas is unavailable. The adapter's grid-to-sprite placement and formation mechanics are explicitly host-owned replacement policy and must not be mistaken for the resolver's recovered layout. Resolver setup's post-display-mode control margin is mapped: resolved 800- and 1024-pixel horizontal spans use 80 and 160 pixels, respectively, while all other spans retain zero. The `WAR_MODE` capability-selection semantics, host backdrop scaling, source labels, raw host input-event production, and score-modifier ownership remain **Provisional**; the replacement currently identifies the four unlabelled source selections only as options.

The dragon encounter now similarly requires its imported `DRJSTWIN.PCX` background and the complete 25-frame `LANCE1.CSF` sequence, whose source identity and palette pairing are Corroborated by the decoded resource population and contact sheet. It never falls back to a constructed arena or silently hides the lance. The original eye path, timing, hit window, and frame anchors remain **Provisional**.

The interactive dispatcher now maps its executable-confirmed record mutations without claiming semantics for its event producer: code 2 selects a living player record through the viewport-adjusted rectangle selector; code 3 selects a fixed control rectangle or falls through to that selector; and codes 6/7 issue the selected-unit destination write. Its source `19C74` resolution margin is separate from the `19CA8/19CAC` scrolling pair, so the runtime does not let a same-frame edge scroll move the strip. The first control initially enables tactical passes; only its second hit presents `Do you want to retreat?`, where a true response ends the resolver and a false response falls through to selection while retaining enabled tactical passes. Before dispatch, `CaptureMappedHoverPresentation` emits the source's player-strength/foe status and the single following aggregate `WINNING`/`LOSING` status from exact live lane-count comparison. The three executable category loops append only living player swordsmen, knights, or halberdiers in authored order without clearing prior selections; the outer raw dispatcher routes `0x48/0x68`, `0x4B/0x6B`, and `0x53/0x73` to those loops respectively, and routes `0x61` to selected-list control-code one or `0x41` to all living records. The externally gated raw pairs `0x57/0x111` and `0x0C/0x4C` now retain the exact `19C98` contact-filter mutations (including the former pair's living-enemy strength reset) without assigning a meaning to their three unrecovered gate bytes. The resolver now exposes its terminal enemy-defeated, player-defeated, and player-withdrew outcomes: source checks the enemy lane first, so a simultaneous zero is enemy defeat, and its caller distinguishes raw return-one withdrawal from defeat by the remaining player count. `AdvanceMappedFrame` preserves source order after edge scrolling; an activated, nonterminal frame reaches the stable strict-deadline tactical pass unless the mapped pause state is set. The source proves a strict 200-timer-unit gate but not its wall-clock duration, so the runtime uses a caller-configurable stable host cadence; its 200-ms default is a Provisional compatibility policy rather than fabricated original timing. The event producer and the affirmative/negative input mechanics remain Provisional.

The reimplementation separates verified original values from behavior inferred where the 1995 executable and manual do not expose an exact formula.
Binary-derived claims and their confidence levels are recorded in [`original-findings.md`](original-findings.md); provisional values must not be presented as original facts.

## Verified balance encoded verbatim

- Attribute ranges and qualitative ranks (0-20)
- Template values documented for Ronald, Hayward, Spencer, Mordred, and Simon
- Custom starting wealth of 240 shillings and baseline equipment
- Crop cost and monthly/harvest revenue at 50% productivity
- Forest cost, labor, and revenue at 50% productivity
- Army purchase and upkeep values at 50% and 100% productivity
- Swordsmen beat halberdiers, halberdiers beat knights, knights beat swordsmen
- Steward +10%, beadle +5%, priest +5%, church +15%, monastery +15%, woodward +5%
- One house and one food tile per 100 people; 25% bean requirement; July penalties
- 200-shilling loan ceiling with 50% harvest interest; spy price of 80
- Shop prices, 75% resale rule, armor bar values, and crossbow availability
- The original store table order, item-image indices, local descriptions, Fighter's Dagger price of 84 shillings, and `BUYSELL.CSF` view/transaction labels
- Three jousts and one skirmish per tournament, lance experience cap of 20
- Joust and melee wager dialogue, five selectable melee opponents, and the documented typical 20-80 shilling wager range
- Lady-specific colors, piety/fame restrictions, win-indexed courtship rewards, and simultaneous courtships
- Dragon route requirements: Jane's lance, Wendessa's Shield of St. George, Valetta's armor, and Mighty strength
- Thirty imported youth-dilemma definitions grouped five per age from 12 through 17, with executable-confirmed random selection and inclusive breakpoint outcome rules
- Age 18 campaign start, age 30 deadline, crown and dragon endings

## Inferred and isolated

- Intermediate productivity interpolation for unit prices/upkeep is linear between the documented endpoints.
- Population growth follows the documented 1,200-population trial bands and scales by productivity.
- Field battle casualties preserve the documented counter triangle but use a seeded attrition calculation because exact executable coefficients are unavailable.
- Tactical field battles expose independent swordsman, halberdier and knight formations with live hold, advance, flank, captain-control and withdrawal orders. The exact original formation geometry remains under investigation.
- The original first-person hit threshold, damage dice, armor penetration, reach, player attack skill, player maximum-health formula, authored friendly/hostile partition, campaign retainer cap and strategic casualty conversion, four retainer command identities, Defend/Attack/Follow intents, friendly Defend formation loop, melee Retreat transition, death completion, and no-enemy-loot behavior are executable/resource-confirmed. Exact thinker cadence equivalence, fixed-point ray acquisition, critical-hit presentation/semantics, and several other consequence tables remain under investigation.
- Costs not given by the available manual/FAQ (castle staff and civic construction) are centralized in `Campaign.Build` pending executable observation.
- World coordinates, road-distance conversion, garrisons, and the tournament circuit are provisional pending extraction of the original map tables.
- Tournament opponent identifiers are partially recoverable from `CONQUER.EXE`; the current wager progression, difficulty tolerances, and individual eight-man compositions remain provisional pending disassembly or controlled observation.

## Media status

The verified local manifest currently contains 29,226 assets. This supersedes the older 16,092 count retained in the historical status paragraph below, which predates broader nested-resource import coverage. Runtime support now requires this owner-supplied official extraction and rejects incomplete or damaged manifests before graphics initialization. The title/options/practice/load/character/youth/campaign/estate/office/war-plan/fief/inn/conversation/blacksmith/shop/overview backgrounds are required imported roles, including `FFTITLE.PCX`, `OPTFIN.PCX`, `PRACTICE.PCX`, `LOADGAME.PCX`, `CHAR_OPS.PCX`, `PREGEN.PCX`, `MORALITY.PCX`, `FLUFF.PCX`, `ICONTEMP.PCX`, `TACTICAL.PCX`, `WARPLAN.PCX`, `FIEFMGMT.PCX`, `INNPEOPL.PCX`, `COMSCRN1.PCX`, `FORGESMI.PCX`, `SWDTEMP.PCX`, and `F_OVER.PCX`. Their presentation paths (`DrawTitle`, `DrawOptionsHub`, `DrawPractice`, `DrawLoadGame`, character creation, `DrawDilemma`, `DrawCampaignBriefing`, `DrawMap`, `DrawHome`, `DrawWarPlanning`, `DrawFarm`, `DrawInn`, `DrawInnDialogue`, `DrawBlacksmith`, `DrawBlacksmithDialogue`, `DrawShop`, and `DrawOverview`) now fail explicitly if a verified role is unavailable instead of showing an independently authored generic panel. The role mapping is Confirmed from the owner-supplied resource manifest and `ImportedArt.Definitions`; this dependency enforcement does not make a claim about original input dispatch.

Tournament screen `TOPTS.HAT` binds `JOUSTMAP.PCX` as its 640-by-480 source background, with five declared source regions. `ImportedArt.Definitions`, `ImportedLayouts.Definitions`, and `DrawTournament` now require and render that asset instead of an independently authored panel. The resource association and descriptor geometry are Confirmed from the owned resource directory and decoded HAT record; the replacement's lance meter and keyboard labels remain Provisional host policy pending the original dispatcher.

The owned release contains a 35,361,714-byte `.RES`-signature `C1086.GOB` archive, a raw MODE1/2352 CD data track, thousands of Smacker (`.SMK`) sequences, Sierra `.RES/.LOW` resources, and five CD-audio tracks. `tools/Conqueror.Inspect` inventories that data track; `tools/Conqueror.Import` installs owned resources locally with a provenance manifest and lossless CDDA WAV conversion. Generated proprietary files remain ignored and are not redistributed. The importer can now independently verify every manifest path, length, and digest or rebuild and verify generated content from the read-only installation; the current local 16,092-asset catalog passes that end-to-end check. The bounded kind-1 archive decoder is validated across all 32,089 GOB and scene blocks, while the executable-confirmed kind-2 LZW decoder validates all five of its GOB entries and all four resulting 640x480 PCX images. All 12,982 scene `TEX` entries validate as headerless byte planes whose names encode their exact dimensions. The complete `Pal0`-`Pal127` families validate as 256-entry index remaps; generation under `SKIRMISH.PAL`, Scenario-driven dispatch, distance scaling, and north/east/south/west surface ordering are executable-confirmed, and all 90 stored active families reproduce byte-for-byte. First-person Viewer, Scenario, Map, Blocks, Backdrop, and BackImage records are now bounded and decoded: the 128x128 column-major maps provide original starts, named collision/interaction tiles, and defender/champion placements to melee and castle-skirmish sessions, while a map-aware raycast view presents that geometry with the original wrapping panorama, indexed-distance-shaded wall and door surfaces, animated combatants, and wall occlusion. The bounded conversation decoder validates all 1,311 `ALL.CIF`/`ALL.CBF` nodes, 2,062 prompt variants, 2,696 response branches, 63 zero-response continuation slots, 423 node-action references, and 2,318 response-action references; the ten populated-inn selector roots resolve to their matching named portrait nodes. Its bounded action layer validates all 689 `ALL.TMI` groups, 2,267 actions, 10,373 expressions, 13,352 values, and the 190-value `ALL.VTB` initializer. The runtime executes that grammar for persistent script variables and redirects; scope-1 selectors now mutate typed wealth, honor, fame, piety, strength, stamina, and intelligence, while item IDs 0-23 synchronize saved counters with named visible inventory. The bounded SMK implementation validates and decodes all 2,131 movie headers, 182,360 indexed-video frames, and 161,884 packed-audio packets directly; the original title, credits, `WEAPONS.DAT` item movies, dragon-lair approach `TRANDRAG.SMK`, and all three dragon outcomes are now bound to a file-backed MonoGame texture/audio adapter with timing, skip, end, and return handling. The dragon encounter uses the original 640x480 `DRJSTWIN.PCX` screen and its palette with the 25 decoded transparent `lance1.CSF` frames; target movement, timing, hit window, and frame placement remain Provisional. All 26 `.666` sound banks decode into 102 bounded rate-tagged unsigned 8-bit mono samples. At startup every installed bank is decoded once and every sample is converted once to a retained signed 16-bit session buffer; the shared interface sample is bound to mouse activation when effects are enabled, while other event bindings remain incomplete. The runtime decodes the complete conversation/action databases and every referenced portrait once at startup. It enters the inn's confirmed patron roots, executes their selector programs, selects original prompt variants, draws word-wrapped original speakers/prompts/responses, accepts recovered mouse rows and number keys, applies response actions, follows redirects and graph targets, times zero-choice continuations, and terminates on target zero. Home `JUMP!!` is confirmed as an intentionally inactive label-only region. Exact army path interaction, autonomous enemy-movement binding for spy reports, further conversation entry points, remaining first-person state transitions, SMK event bindings and seeking, proportional original font metrics, broader palette associations, village/location mapping, and other Sierra payload formats remain incomplete.

The generic procedural font is no longer in the active imported-content path: executable initialization loads `CONFONT.CSF` into the global byte-text renderer, whose per-glyph advance is the selected frame's first signed width word. The runtime now uses that 256-frame proportional 2..10-by-13 face and its direct byte/ASCII indexing, not the unproven fixed `font.CSF` sibling. Palette association and screen-specific vertical/layout rules remain **Provisional**.

Shared mode-12 destinations now preserve the original single-cell actor rule.
The complete resource census shows behavior `0x87` on all 144 placed actor
bases and their 576 adjacent state blocks, so every live actor supplies blocker
bit `0x02`. Scheduler `0x53624`-`0x53694` restores the mover's saved underlying
block, captures the destination block, and installs the actor into its new map
cell before processing a later effect. One converging actor can therefore
occupy the requested cell; followers stop at the strict collision band and
repeatedly reconstruct their direct effect instead of overlapping or fanning
out. Runtime occupancy and a focused two-retainer regression implement this
**Confirmed** result. A processor-stable original winner for an exact
simultaneous tie is **Disproved**: input precedes scheduler `0x530D0` and
thinker `0x50524`, the thinker begins at persistent cursor `D5C4`, constructor
`0x4C7F4` takes the first free effect slot, and the cursor advances on every
unrestricted main-loop pass. The runtime therefore uses authored retainer-list
order (x-major for official imports) as an explicit deterministic compatibility
policy, with a regression proving update-subdivision independence. GameFAQs
contains no equivalent low-level account. This supersedes older
multi-actor-convergence clauses below.

## Strategic person state

The strategic definition layer retains the complete 176-row person population, not only its layout and selected lord records. A whole-table SHA-256 census plus independent property-lord, starting-castle, and household-count assertions protect every numeric field. Save/load evidence distinguishes the byte person link at `+0x0D` from the 16-bit property link and identifies the overlapping dynamic slices, while unknown state bytes retain neutral names. This closes schema-2 initialization data without overstating the semantics of those bytes.

The schema-2 replacement state now deep-copies those rows alongside all 14 properties, five route-capable movement slots, exact bounded speed/profile/camera/counter state, and sparse 32-bit terrain mutations. Its prepared schema-1 migration preserves recoverable troop totals through slot-ordered hostile-origin settlement and journals dispersal when an origin is no longer valid. A schema-one save created after the source-shaped new-game bootstrap may carry that state beside the obsolete dated roster; settlement validates and retains the source state rather than replacing it. Because older schema-1 saves never recorded an original starting-route selector or camera, migration uses an explicit `-1` selector sentinel and deterministic `(0,0)` camera rather than claiming recovered values. The Game boundary eagerly injects all 97 decoded paths and the exact camera-projected full-dword terrain grid through `IOriginalStrategicResources`, including route-length validation for prepared saves. The state passes structural, resource-binding, and JSON round-trip validation, and the fixed 60 Hz map host now runs its recovered scheduler; schema migration activation and retirement of the isolated dated day adapter remain **Provisional**.

## Strategic movement runtime

Strategic motion update (2026-09-17): the decoded route/grid provider now drives `AdvanceMovementPass` for physical slots 0-4. Mode 1 applies the **Confirmed** `0.9 x terrain speed x strategic speed` displacement, exact crossing behavior, grid refresh, and terrain-9 byte-garrison return. Mode 2 consumes the decoded route with one-waypoint-per-pass progression, strict `< 6` tolerance, inclusive prior-direction validation, terrain lookup, and strict horizontal `< 50.0` gate. Mode 3 tracks the live slot held at record `+0x14`, follows its current float position, and changes to direct mode toward the origin property with an immediate unscaled step on terrain 9; an inactive target produces a completion signal without destroying the pursuer. Focused tests cover each boundary. This supersedes the later compact overview's claim that runtime route consumption remained open. Generation/contact dispatch, fixed-update integration, schema migration activation, and retirement of the isolated dated adapter remain **Provisional**.

Scheduler update (2026-09-17): `AdvanceSchedulerPass` now preserves generator-first execution and physical slot order while resolving each completed slot before advancing the next. Constructors use the first free slot, exact route family, live target coordinates, and recovered force rules. A corrected trace **Disproves** the earlier ordinary-force formula: `0x38A8C` writes `household + floor(floor(unsigned lord rating / 4) / 3)` to each type, falling back to `0/0/1`. Reactive initializer `0x38B40` detaches at most `target total + 3` from the byte garrison, adds capped household support, and uses its distinct three-way/minimum-three rule. Direct grid auxiliary identity drives the five contact probes; encounter, matched-origin reinforcement, and exact retargeting are interleaved immediately. The executable's timed path can read an uninitialized property local after finder failure; the clean runtime requires a real current-pass detection instead of emulating stale stack bytes. All scheduler mechanics are **Confirmed** except that deliberate deterministic safety policy. Fixed-update inputs, presentation handoff, schema-2 activation, and dated-adapter retirement remain pending.

Reactive-finder update (2026-09-17): finder `0x3BB4C` is now active inside `AdvanceSchedulerPass`. It scans 14 properties then five player army records in authored order, uses strict per-axis near/approach thresholds `30/250`, property-7 thresholds `40/200`, London's half-open rectangle `x=[700,12640), y=[600,4560)`, and exact grid auxiliary-lord identity. Near contact sets property byte `+0x0D`; approach sets one-shot byte `+0x0E` and produces a presentation alert. Empty garrisons retain the first mutation but cannot construct a pursuit. These mechanics and focused boundary tests are **Confirmed**. Caller `0x3C290` proves the remaining activation dependency is the original six-record player update at `+0x1A4B8` (five armies plus avatar), ordered after spy reporting and before the enemy scheduler; substituting current named locations would lose original movement state.

Player strategic update (2026-09-18): the six-record dependency is implemented behind the dormant schema-2 boundary. Handler `0x11554` supplies exact tagged-target/inline-route movement, strict waypoint progression, current-terrain speed, inclusive direction guard, two-stage kind-9 probe, and grid refresh. Outer pass `0x13168` preserves movement-before-contact, player-then-enemy authored order, strict per-axis `<30` collision, selected-record global `AE64`, avatar slot 5's post-decrement 120-pass alert cooldown, and the five-cell trigger for distinguished record `AE6C`. Input `0x122AC` adds strict player/enemy/three-division hit precedence, `0x1000` enemy and `0x10000` division tags, persisted route-input global `AE58`, first-point terrain priming, stale-pair undo semantics, and the 20-point UI cap over 21-pair storage. Bootstrap `0x110E8` marks all records complete, activates only avatar 5 at the selected starting person's exact route anchor, and writes both selectors to 5 while count `AE5C` remains zero. Join/leave `0x1133C/0x113A4` synchronizes campaign membership. Constructor/removal `0x12CAC/0x12DD0` now preserves the six-entry counter boundary, exact formation anchors, former-selection command clearing, first-active replacement, and distinguished-record avatar restoration. The former statements that `AE6C` is the selected/current record or army-only are **Disproved**. Initialized automatic temporary forces now advance through their recovered looping routes before player targeting on the same fixed pass; their event producers and expiry remain open, so schema 1 stays active.

## Controls represented

The older “retainer commands remain Provisional” text below is superseded: Attack, Defend, Follow, and Retreat write requested actor modes 6, 2, 16, and 10; Follow targets player actor `D4D0`; and every order uses the selected-or-all fallback before clearing selection. Defend now follows both recovered friendly kind tables: mode 2 scans the live-centered 3x3 neighborhood in relative y/x order; kind 0 selects completion-gated attack mode 11 or formation mode 1, while kind 1 selects scaled escape mode 10 or formation mode 1. Their subsequent mode-1/3/4/7/8 edges reuse the exact neighborhood, authored-order ray, regroup, pursuit, and strict-contact predicates with one predicate per stable update. Further tracing **Disproves** the earlier claim that public Retreat directly invokes away-vector handler `0x50020`: requested mode 10 transitions friendly kind 0 to preserved-heading mode 5 when acquisition succeeds and mode 2 when it fails; kind 1 instead chooses mode 4 or 2. Kind-1 mode 4 repeats visible acquisition, then transition `0x4E6D3` selects ranged mode 11 or fallback mode 1. Mode 11 aims at the returned actor, applies a strict raw combat-row contact-distance gate, and doubles the ranged effect interval. Those command identities, shell regions, dispatch rules, formation edges, corrected transitions, and bowman continuation are Confirmed and active through explicit authored actors. Acquired-target Attack pursuit, no-visible-target Attack wandering, Follow, Defend, and melee Retreat share the recovered sub-cell scheduler. Multi-actor convergence, exact fixed-point acquisition-ray equivalence, and original thinker frequency remain open.

The former claim that handler `0x50020` is dormant is **Disproved**. Initializer `0x542F8` and the placement census establish only initial state. Mode-14 hit handler `0x503EC` compares stored target health with newly reduced source health before constructing the hit effect; mode 15 repeats the comparison at `0x504F2`. Shared transition `0x4E77E` selects mode 13 when the target is stronger and mode 11 otherwise, while `0x4E5F0` cancels the prior live effect. Predicate `0x4FD9A` tests source health against 6; transition `0x4E7A4` selects mode 2 at or above the boundary and mode 10 below it. The reachable mode-10 handler computes `current - stored target` heading, scales both descriptor deltas by `1.5`, and uses non-cardinal signed-1.15 rotation. `OriginalActorMotion` and `SiegeSession` implement this Confirmed hit-triggered route. The former mode-11 attack-rejection attribution is Disproved. GameFAQs offers no low-level corroboration.

The same hit-triggered morale route is now active for every supported hostile template rather than only commanded friendlies. Templates 3/5/8/9 retain their exact initializer profiles and kinds 2/4/2/7. Independent kind-2 and kind-4 table fixups converge on transitions `0x4E71F/0x4E745/0x4E77E/0x4E851` for modes 6/8/11/13; kind 7 shares kind 2, and both tables' mode-14/15 entries also use `0x4E77E`. Official defenders therefore acquire player-side actors, pursue through signed sub-cell movement, attack only after strict contact, apply damage on effect completion, and escape a stronger stored target after being hit below health 6 using the exact shared `1.5` motion math. `AdvanceHostileMovement` replaces the former randomized whole-cell loop for imported hostiles. Mode-4 fallback and post-escape mode-3 entry now feed the mapped formation/defend loop: modes 1/2 scan the live-centered 3x3 neighborhood for same/opposite sides, modes 3/5 acquire same-side actors by authored-order ray, and mode 7 tests a same-side contact below `0x200`. Kind-2/7 and kind-4 retain their distinct executable transition pairs. Direct mode-7/8 heading is cardinalized by `(heading + 0x20) & 0xC0`; the earlier non-cardinal pursuit is Disproved, while mode-10 escape remains non-cardinal. `0x4F49C` evaluates one predicate and then runs the selected mode's handler, so no-effect modes no longer chain inside one runtime call. The original `D5C4` thinker cursor advances per unrestricted main-loop pass and has no processor-stable wall-clock cadence; the fixed runtime update is an explicit future-proof compatibility policy. GameFAQs FAQ 66730 has no low-level evidence for this mapping.

Mode-6 Attack wandering is now **Confirmed** and active. Acquisition `0x4F98D` seeks the nearest visible opposite-side actor; on failure, kind-0 transition `0x4E71F` and kind-1 transition `0x4E745` both retain mode 6 rather than idling. Current-mode handler `0x4FDCD` preserves heading and maps movement descriptor flags through `(flags & 0xA7) | 0x40`, leaving the official `0x142` descriptor unchanged. Scheduler `0x53C13`-`0x53C6C` clears a blocked local axis and turns left with `(((heading + 0x20) & 0xC0) - 0x40) & 0xFF` while the live effect continues. Runtime acquisition uses the same bounded cell-visibility bridge as bowman Retreat and records the wandering/direct family on each movement effect, preventing a later command evaluation from changing collision semantics mid-cycle. Focused tests cover preserved heading, strict movement cadence, blocked-axis reset, and left-turn continuation. Exact `0x470A8` visibility equivalence remains **Corroborated**. The GameFAQs guide offers no state-machine or movement-flag detail and neither corroborates nor conflicts with this result.

Mode-11 attack completion is now **Confirmed** and active for both friendly kinds across public Attack and the reachable Defend/Retreat routes. Public Attack handler `0x5700E` requests mode 6 through transition helper `0x4E5F0`; kind 0 advances through acquired mode 8 while kind 1 can select mode 11 directly. Mode-8 predicate `0x4F7A2` retains the actual intervening opposite-side actor returned strictly below the raw combat-row contact distance, and exact equality fails back to mode 6. Effect constructor `0x4C7F4` stores the current clock at live-effect `+0x10`; scheduler `0x5310D` advances only under the strict `now - deadline > interval` comparison and retains deadline overrun. At `0x53365`, the effect counter must equal its configured count, actor mode `+0x18` must still be 11, and target health `+0x40` must remain positive before damage routine `0x4F070` runs. Completion path `0x53D47` then clears actor effect handle `+0x08` and immediately calls thinker `0x4F49C` for that actor. `AdvanceAttackOrder`, `ModeEightContactTarget`, and `PendingRangedTarget` preserve this state, identity, and completion sequence. Tests cover pursuit without immediate damage, strict below/equal contact boundaries, intervening identity, deferred and repeated damage, same-actor re-entry, public/pointer command routes, Retreat, and cancellation. The former synchronous kind-0 grid-range strike is **Disproved**. The monotonic elapsed-time representation preserves these semantics independently of processor and render speed; GameFAQs has no internal timing detail to corroborate or contradict them.

The older “direct actor/cell targeting remains open” wording is now narrowed. Executable dispatcher `0x55524` and raycaster `0x470A8` confirm exact actor/object identity, alpha rejection, and the `< 0x280` explicit-action limit. Primary flag-0 dispatch first sends every selected friendly to the projected contact through mode 12 and consumes the click; only otherwise does it process actor/object-specific behavior. `OriginalSiegeProjection` now reproduces the fixed horizontal vector, normalized 64-step walk, wrapped source lookup, both-axis/perpendicular probe order, behavior-bit-0 pass/stop rule, eligible state-target continuation, 31-candidate sentinel, center/diagonal shapes, face coordinate, and candidate insertion order. Kind-4 picking additionally uses the live centered 8.8 point, view-relative forward depth and lateral coordinate, exact heading-sector/mirror formula, vertical bounds, and alpha before returning the attached actor/object identity. The remaining ray gap is non-cardinal acquisition integration rather than pointer billboard geometry.

Mode 12 is now active on projected surface contacts. Executable branches confirm selected-first dispatch, target fields `+0x28/+0x2C`, `target - current` heading, target-actor clearing, and exact-cell completion; a blocked contact is accepted and later rejected by movement collision. Viewer setup supplies elevation `0x80` and horizon `height / 2`; `0x470A8` uses lateral basis `((0x400000 / width) * (x - width / 2)) >> 8`, forward basis `0x4000`, and a `0x40`-step traversal. Switch table `0x34A0C` proves kind 0 contributes no candidate. Kinds 1/4 use cell boxes, 2/3 center planes, and 5/6 diagonals; `0x44F7C` has 32 allocated slots but the comparison at `0x45061` limits the usable list to 31 contacts. Candidate vertical bounds are `top = horizon - (upper - 0x80) * width / depth` and `bottom = horizon + (0x80 - lower) * width / depth`, followed by texture alpha sampling. These formulas and decoded block fields are Confirmed. The former lower-viewport floor plane is Disproved. GameFAQs FAQ 66730 contains no internal projection formula and is not corroboration for this mapping.

The movement-cadence and sub-cell portion is now narrower and executable-backed. Actor block signed word `+0x46` is a movement-effect selector distinct from visual effect selector `+0x2E`; mode 12 reads it at `0x4FFBC` through the high word of `+0x44`. Every one of the 144 placed actor bases across the 42 owned combat scene pairs selects effect 7 or 10: all 1,002 placements begin with signed 8.8 block offsets `(0,0)` and use three ticks, 200 ms interval, descriptor flags `0x142`, local delta `(64,0)`, and surface stride 5. Loader `0x5185F`/`0x5186F` centers the owning actor at `(cell << 8) + 0x80`; scheduler `0x530D0` rotates the local delta through `0x44740`, applies it to block `+0x24/+0x28`, tests the neighbor's behavior bit 2 outside strict `-0x59..0x59`, and changes cell only beyond `+/-0x80` while wrapping the retained offset by `0x100`. Mode 12 rewrites the descriptor flags to `0x112`; on rejection, flag `0x10` preserves the actor's partial offset and heading, terminates the current effect, and returns to actor thinking so the next scheduler update retries. The previously attributed mode-12 `0x40` left turn is **Disproved**, but that family is now closed for kind-0 Retreat mode 5 and no-visible-target Attack mode 6: `0x4FDCD` preserves heading and leaves descriptor `0x142` unchanged through `(flags & 0xA7) | 0x40`; collision clears the blocked-axis remainder and applies `(((heading + 0x20) & 0xC0) - 0x40) & 0xFF`, turning left from the nearest cardinal while the effect continues. All 688,128 placed cells in the supported scenes split into 52,278 blockers and 635,850 clear cells by that bit; all 1,002 actor placements restore a clear authored underlying block. From zero the first three open ticks produce `64,128,192 -> cell + 1, offset -64`: `3 * 200 = 600 ms` is the effect cycle and first cell transition, while sustained straight travel requires four ticks/800 ms per cell. The former universal 600 ms cell deadline is **Disproved**. Selected destination travel, acquired melee Attack, no-visible-target mode-6 Attack, Follow, friendly Defend formation/pursuit, and melee Retreat now advance on the monotonic update clock and share retained sub-cell coordinates with drawing and picking. Kind-1 Defend and Retreat both reach the recovered scaled-escape/ranged continuations. Exact fixed-point acquisition-ray equivalence, multi-actor destination behavior, exact ground coordinates, and full actor-wide use remain Provisional.

The earlier status text in this section that calls defeated-enemy loot Provisional is superseded by the completed death-path trace: actor death and completion never reference wealth, ammunition, or equipment, while their complete executable reference inventories place combat mutations in authored pickup and crossbow paths. Defeated-enemy loot is **Disproved** for the hashed release, and the runtime correctly has no actor-drop path.

Imported first-person scenes now retain mask-83 exit boundaries and render every non-actor kind-4 pickup, static-debris, and contact-object billboard. The scene record field is a behavior bitmask, not an ordinal identifier. Bit `0x20` is executable-confirmed at `0x55A02`; runtime reach uses combat-row column 4 plus the original `0x40` contact allowance, and the contacted cell is replaced in one hit through its explicit record-offset-`0x40` state target. Owned trees, barrels, stools, pots, and candelabras target floor or ground directly; adjacent damaged-looking records are alternate states rather than an inferred durability sequence. Mask-19 pickups carry explicit-action bit `0x10` and use its `< 0x280` range as a two-cell limit before immediate state replacement. Dispatcher `0x51E60` reads the selected scene block's selector at `+0x48`; `+0x4A/+0x4C` supply its arguments. The only placed actionable rewards are 36 bags of coins (`+25` wealth), 70 meals (`2d6` healing capped at maximum health), 36 Chain Hauberks (equipment bit 6), and 36 piles of bolts (`+12` ammunition). The generic action consumes full-health and duplicate-equipment pickups too. Actor records reuse words `0x4A` and `0x4C` to select one of ten combatant templates and one of 25 combat rows. Imported actors retain the executable-initialized attack skill, armor, health, row-specific reach, authored actor kind, and side partition. Player skill derives from strength, dexterity, and sword experience; maximum health is strength plus stamina plus honor. Player and enemy strikes now use the confirmed 200-value hit roll, defender-skill reduction, close-crossbow and rear-attack bonuses, followed by the confirmed dice-count, die-sides, armor-penetration, and zero-clamped damage calculation. Retainer commands, direct targeting, acquired-target Attack pursuit, Follow, friendly Defend formation, kind-0 melee Retreat, and kind-1 ranged Retreat continuation are active at the fidelity described above; exact acquisition-ray equivalence and broader AI remain Provisional. Defeated-enemy loot is Disproved.

The original cadence architecture, actor state templates, and completion-gate binding are now Confirmed and active. Scene loading reads Scenario offset 28 and loads `count * 0x40` bytes from `SFXDEFS`; constructor `0x4C7F4` maps each descriptor into a live record, and scheduler `0x530D0` advances independent millisecond deadlines. Attack, hit, and death copy adjacent `+1/+2/+3` state blocks, attach each state's descriptor, and resume actor logic only after a strict elapsed-greater-than-duration boundary. Renderer `0x46E3B`-`0x46F23` selects `base surface + angular sector` and mirrors behavior-bit-2 billboards instead of playing neighboring textures sequentially. All 144 placed actor bases in 42 owned combat scenes share `8/4/4/2` angular divisions for base/attack/hit/death and select descriptor 0 in every state: 80 use 384 ms gates and 64 use 400 ms gates, with ranged attack rows 23-24 doubled. Imported layouts now carry those exact gates into the processor-speed-independent simulation clock, and the renderer applies the recovered angle/mirror formula. A universal frame duration, texture-count-as-frame-count, and the former nine-frame attack/eight-frame collapse model are Disproved. Actor thinking remains a separate untimed round-robin pass. Foreground setup now follows the executable's three melee row families, their `8/16`, `40/40`, and `32/24` random spans, viewport-relative outer anchors, mirror branch, signed 8.8 velocity `(target - outer) << 9 / divisor`, contact reversal, and position-selected CSF pose. Runtime updates retain subpixels across whole elapsed milliseconds and cap one update at the original 166 ms, making the result independent of processor throughput and render count; a fixed 70 ms melee frame timer is Disproved. Door actions have no cadence: bit-`0x10` dispatch replaces the map cell immediately through offset `0x40`, disproving the former 360 ms animation. Blood advances at `0x552FB`-`0x55395` through four foreground-render invocations. The main loop calls that renderer once at `0x5841F`, performs framebuffer copies, and jumps directly back at `0x584DF`; it contains neither a clock comparison nor the explicit wait routine at `0x657C0`. Its wall-clock duration is therefore hardware-dependent, so the runtime preserves the exact four steps through a monotonic 70 ms compatibility policy rather than claiming an original duration. The runtime target-coordinate bridge remains open.

The time-based portion is safe to retain because elapsed milliseconds, not processor cycles, gate state changes. The clean-room implementation uses the game framework's monotonic elapsed time, per-record state, retained fixed-point remainders, and bounded catch-up so frame subdivision, stalls, and wall-clock changes cannot destabilize it. The original foreground weapon motion is therefore safe to retain in this adapted form. The original blood overlay is not safe to retain literally because it is render-loop counted and has no stable intended duration to corroborate. `SiegeHitEffect` and the no-trajectory foreground fallback retain the confirmed frame order while advancing explicit monotonic 70 ms compatibility steps independently of processor speed and refresh rate; 70 ms is a clean-room compatibility choice, not a recovered formula.

The dragon moor is omitted from selection, drawing, travel, and captain dispatch until the decoded original discovery flag is set. Imported tournament dialogue can enter all six lady selector roots and bridges their current-colors and last-joust variables back to the playable joust. The blacksmith portrait and keyboard route enter original node `3201`; the parish route uses ordinary selector `3100` except at Cambridge, where confirmed Father Hyacinth selector `3149` exposes the armor, Book of Hours, chalice, and bribery branches. Their generated shortcuts remain available without conversation data. Anna Lisa's original secret sets the discovery flag; her second fallback courtship win supplies an equivalent clean-room route, and prototype saves already at the moor migrate safely. A prepared knight arriving at the moor now watches the owned approach sequence and proceeds directly into the dragon encounter. Anna Lisa and Sir Frederick's decoded references to raiding an absent dragon describe Frederick's backstory; no decoded action grants the player lair treasure, so the reimplementation does not fabricate a repeatable plunder system. The six parallel regional priest roots `5000`-`5500` are decoded but remain unassigned until their location mapping is recovered. Unpaid harvest debt now persists into the executable-confirmed Drogo demand: the player may pay the exact amount or enter a no-retainer, no-retreat first-person fight; victory permanently closes the moneylender and defeat is fatal. The demand uses the owned `DROGO.PCC` portrait when installed. Drogo's combat strength and generated arena remain Provisional.

M5 correction (2026-09-22): the desktop debt-refusal route no longer uses the generated arena mentioned above. Its imported `MONEY.RES` layout contains the x-major first friendly player proxy plus four hostile thug bases (templates 9, 8, 3, and 3) with their scene combat rows and state metadata. The no-retainer policy remains a campaign decision; no one-on-one count is inferred. Archive placement and metadata are Confirmed by the bounded scene census, while associating this scene with the original Drogo dispatch remains Corroborated pending executable control-flow recovery.

The Village exterior now requires the verified `TOWN_Y.PCX` background named by `VOPTS.HAT`, rather than a generated panel. Its map, inn, blacksmith, and church routes use the descriptor's source coordinates; source evidence supports the lender region's identity but not yet its separate transaction/dialogue flow, so that route remains explicitly provisional. Startup validates the complete `VILLAGE.DAT`/`TVILLAGE.DAT` exterior catalogs and every referenced owner-imported PCX. The recovered `0x43080` selector reads original person `+0x0F` as a zero-based VILLAGE index, so a new campaign home now lazily renders that person’s authored exterior and uses its enabled Map/Inn/Smith/Lend/Church catalog rectangles for click and hover geometry. Disabled entries and Tournament remain excluded: the latter’s first source-order callback requires a saved tournament-person identity match and a calendar-field check whose larger scheduling semantics are not yet mapped. Seven start-home person/index pairs are regression-pinned; generic locations away from home retain `TOWN_Y.PCX` because their original person/location mapping, and the tournament selector globals, remain Provisional rather than being inferred.

Reimplementation saves are explicitly schema-versioned and written by same-directory replacement. Each of the five slots retains its previous valid generation and transparently exposes that backup if the primary JSON is damaged; unversioned prototype saves and the former single slot-1 filename are migrated on load. A separate transition-triggered autosave uses the same recovery policy, while the load screen distinguishes missing, malformed, inaccessible, invalid, and unsupported-format generations and reports actual backup recovery.

The status clause in the following compact overview is superseded for kind-1 Retreat, no-target Attack, and Follow. Bowmen follow the executable-confirmed mode-4-to-mode-11 ranged continuation, while failed mode-6 acquisition follows the confirmed preserved-heading flag-`0x40` movement path. Follow no longer stops within one cell: mode 16 evaluates `0x4F8B0` and unconditionally transitions through `0x4E7CA` to mode 17's preserved-heading wandering effect; mode-17 predicate `0x4FB39` returns to direct mode 16 only when the ray resolves exact player actor `D4D0` below strict depth `0x7FFF`. Both friendly kind tables use the same edges. The alternating `0x142`/`0x112` effects, exact identity, strict boundary, and one-predicate ordering are Confirmed and active. Exact fixed-point acquisition-ray equivalence remains Provisional because the fallback runtime path uses a bounded cell trace.

Actor color normalization is now Confirmed and active. Loader `0x51A8B`-`0x51B74` assigns every friendly the player's red `(Pal0, texture 64)`, green `(Pal32, texture 128)`, or blue `(Pal64, texture 96)` tuple and changes a hostile only when its authored family conflicts, choosing blue except against a blue player, where it chooses green. State copier `0x4E39C` preserves that normalized 32-map family while replacing the attack/hit/death texture; the renderer adds the state block's recovered 0-31 distance remap. The owned-scene census covers all 144 placed actor bases and 1,002 placements. `SiegeActorColorMapping` and `SceneEnemyTexture` implement the mapping without retaining proprietary bytes.

Friendly kind-1 Retreat and hit recovery now preserve the table's one-predicate ordering. Public mode 10 selects mode 4 or 2 through `0x4E805`; only a later thinker pass can run mode 4 and select ranged mode 11 through `0x4E6D3`. Hit-selected mode 13 uses fixup `0x4E4B0` and transition `0x4E851` to choose ordinary mode 6 at health 6 or higher and scaled escape mode 10 below it. Modes 5 and 6 retain or reacquire through `0x4E70C/0x4E745`. These edges, strict pass separation, deferred damage, and health boundary are Confirmed and active. The former same-pass bowman Retreat shot is Disproved.

The original autonomous enemy movement is a live five-record map-motion system, not dated travel. Generation tests the prior value of a strict 5,000-unit accumulator boundary and consumes `random(100)` only with a free slot, accepting only values above 96. The added unit is object-2 `+0xAEEC`: reset initializes this strategic speed multiplier to 1, four UI paths bound it to 1-15, and both generator and routed movement consume it once per eligible unrestricted main-loop pass. It is not elapsed milliseconds; that former interpretation is Disproved. The reimplementation will apply the multiplier on its explicit fixed update so the original arithmetic is retained without processor-dependent throughput. The timed branch uses global origin plus route flag 0 when the property list is absent, or an authored-order eligible property plus canonical property/person-group route flag 1 when it is present; mode 2 always falls back to mode 1. Its preceding reactive branch has exact property-7 and existing-pursuer counter gates, constructs mode-3 pursuit in the first free slot, and may add a flag-0 mode-2/direct fallback on rolls 0/1 of six. Helper `0x3BDC0` scans all 14 properties in authored order, retaining only a nonzero `+0x00` row whose `+0x0D` equals 1, and uniformly selects an ordinal from that compact list or returns `-1`; the byte meanings remain unnamed. The ordinary force initializer writes three equal counts from an active-household count plus one quarter of a lord byte, with a one-knight minimum. The executable-initialized supporting state is preserved too: 14 exact named 15-byte property definitions from York through Okehampton, their linked person/lord inputs, the mapped 18-byte person layout, and initial eligible household counts by group. Mode 2 reads canonical `rt_<low>_<high>.rat` waypoint resources, reversing pairs for high-to-low travel; only self-routes and Cambridge/Dunster are absent. Each selected route is a little-endian signed 32-bit point count followed by that many signed 32-bit x/y pairs, with no trailing data. All 90 executable-selected pair routes are required from a supported import and decode through `StrategicRouteDecoder`; the owned set ranges from 13 to 197 points. Route flag 0 uses the new-game selector stored at `C9D0` to load one of seven unreversed `sc_0.rat`-`sc_6.rat` starting-home paths. Their exact person/name/group/assignment/coordinate bindings are exposed by `StartingRoutes`, and startup requires all seven. Routed movement uses exact named Summer/Autumn/Winter/Spring profiles selected from the zero-based month table `B720`; Winter alone has the distinct slower 30-entry values, terrain kind 9 deactivates, and normalized displacement is terrain speed times the 1-15 scalar. Its tile identity is classified through the exact 331-byte lookup at `AF78`. The source grid is GOB resource 292 `icon.jp`: a 16-byte `80,80,200,400` header followed by 200-by-400 dwords in column-major order. `StrategicWorldGridDecoder` restores row-first addressing and supported startup requires the resource; `temp.jap` is the original mutable scratch/save copy. Projection helpers `0x63E20/0x629B0/0x6E5A0` map route space through staggered centers and the exact inclusive diamond raster `abs(dx)+2*abs(dy)<=40`, returning the first cell in wrapped current-camera draw order. The complete 8,304-point owned route census has no projection misses; 210 occurrences are on edges and 34 occurrences at six coordinates demonstrate that camera state is semantically material. `StrategicWorldProjection` preserves this exact inversion. Only absolute x displacement is subject to the same-call strict `< 50.0` application gate. Completed live movement uses a fixed five-cell person probe, unassigned-eligible encounter precedence, and only transfers troops into the origin property when the contacted assignment matches that property's state. Retargeting chooses the first active field army strictly within 300 units, else the nearer player/origin point with a player-favoring tie. The runtime's generic destination-reinforcement model is Disproved. `OriginalStrategicMovement` preserves these **Confirmed** identities, generator gates, construction plans, terrain lookup/profiles/tables, arithmetic, probes, and decisions. Several property/person byte meanings, runtime consumption of decoded paths, persisted camera/mutation state, and migration implementation remain open, so daily generation and dated arrivals remain an isolated **Provisional** adapter; the required one-way schema migration is specified in `architecture.md`.

The map contains 18 named destinations with selection and calendar-costed travel, and links the original high-level destinations (Home, Village, Tournament, Overview, field battle, siege, dragon, London). War Planning persists five named divisions, edits 100-serf companies transactionally, enforces the 60-company and home-territory rules, charges upkeep across all divisions, tracks player membership, and assigns one live spy for 80 shillings. The spy waits for the first active record in a persisted five-slot enemy-movement roster, reports its three unit counts in slot order before that day's movement advance, and is consumed; the former monthly unknown-garrison proxy is removed. Autonomous enemy columns transfer detachments between hostile strongholds, though their daily scheduling, route selection, troop formula, and arrival outcome remain Provisional while the original lord and path records are recovered. The joined division—whichever of the five is selected—supplies travel interception, field-battle survivors, siege retainers, and retreat losses. For each of Swordsmen, Halberdiers, and Knights, campaign assault entry contributes `min(floor(units / 3), 3)` retainers; the three contributions are added with a one-retainer fallback. Each dead retainer removes one represented unit, exhausting those contributions in that same unit-type order. These formulas and the strategic mutation path are Confirmed. The actor loader's predicate, x-major first-friendly player rule, and template 0-2 friendly versus 3/5/8/9 hostile partition are Confirmed across all 1,002 placements in the supported official combat scenes; `SiegeSession` now limits the formula result to available authored retainer positions. Command dispatch and intent plus Attack, Follow, Defend formation, no-target wandering, and both Retreat kind paths are active; exact acquisition visibility remains provisional. Unjoined fielded divisions accept independent dated movement orders, render along their routes, persist in transit, and resolve remote interception with a captain's report; exact path selection and captain battle rules remain provisional. Campaign castles and London use the confirmed common `MELEE0.RES`; practice randomly selects the three executable-confirmed unsuffixed base scenes, while the distinct `MELEE00`-`MELEE24` family is reserved for tournament melee. Melee and castle-skirmish sessions require original 128x128 scene geometry, viewer starts, panorama, door/secret-passage/food/treasure roles, and defender/champion placements; conversion crops each map to the Viewer-connected play area, excluding disconnected authoring galleries outside that component. A map-aware raycast view exposes the actual geometry and wall-occluded defenders inside the exact 167x117 aperture of `SKIRMISH.PCX`. It loads the facing-wrapped `BackImage`, cardinal wall or door slot, and original actor states through the confirmed `SKIRMISH.PAL` association. `SKIRMISH.CSF` supplies the recovered weapon and hit-effect sequences. The original blood frames advance once per unrestricted render loop, so the runtime preserves their sequence with explicit monotonic 70 ms compatibility steps rather than retaining the processor-dependent mechanism. Dead actors stop blocking and attacking immediately but remain visible until their imported death-state gate expires. Actionable doors install their explicit target block immediately; placed pickups apply their exact imported reward and are consumed by the same generic replacement. Movement, facing, radar, interaction, melee and crossbow combat, retainers, equipment breakage, and retreat are playable, but dynamic status fields, exact click-selected world-object semantics, and broader AI remain provisional; defeated-enemy loot is Disproved. London and dragon victory play their executable-confirmed ending sequences, while the age-30 ending remains distinct. The desktop runtime validates a supported official extraction before graphics startup; generated or no-media presentation is not a supported execution path. Save/load are modern additions and do not alter simulation balance.
