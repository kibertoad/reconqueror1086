# Data-driven gameplay architecture

Gameplay content is described in typed definition records in `Balance.cs` and interpreted by the campaign engine. Presentation code reads the same definitions for menus, so adding an item or courtship entry does not require another UI branch.

## Definition families

| Definition | Content controlled | Interpreter |
| --- | --- | --- |
| `CropBalance` | cost, labor, normal and harvest returns | `Campaign.Plant` / monthly settlement |
| `ForestBalance` | cost, labor and monthly return | `Campaign.DevelopForest` / monthly settlement |
| `UnitBalance` | 50%-100% purchase and upkeep curves | recruiting / monthly settlement |
| `EquipmentBalance` | price, power, armor, availability and equipment slot | blacksmith / inventory |
| `BuildingDefinition` | cost, productivity bonus and repeatability | `Campaign.Build` |
| `CourtshipDefinition` | eligibility, marriage threshold and win-indexed rewards | `Campaign.RequestColors` / jousting |
| `VictoryDefinition` | required destination, fiefs, army, strength and items | `Campaign.AttemptVictory` |
| `GrowthBand` | capacity threshold and population growth rate | monthly settlement |
| `WorldLocation` | map position, destination type, garrison and villages | travel / conquest |
| `Dilemma` | prompt, choices and stat/wealth/item effects | character creation |
| `YouthDilemmaDefinition` | imported prompt, scoring attribute, breakpoints, outcome prose and typed changes | `YouthDilemmaRules` / `Campaign.AnswerDilemma` |
| `YouthDilemmaPoolDefinition` | age range, variants per age and stable resource-number mapping | campaign youth selection |
| `YouthDilemmaPresentationDefinitions` | original HAT-derived choice hitboxes | dilemma mouse input and highlighting |
| `SiegeDefinition` | map size, garrison scaling, champion health, healing and break chance | `SiegeSession` |
| `SiegeLayout` / `SiegeSpawn` | imported collision tiles, viewer start/facing, and original defender positions | `SiegeSession` |
| `FieldBattleDefinition` | battlefield size, counter bonus, morale and withdrawal pressure | `FieldBattleSession` |
| `StrategicDefinition` | spy cost, interception chance and minimum field force | campaign travel and reconnaissance |
| `StrategicEnemyMovement` / `StrategicSpyReport` | persisted five-slot hostile movement and the one-shot report captured before movement advances | `Campaign.AdvanceDays` / enemy movement partial |
| `OriginalStrategicMovement` | executable movement/person/property layouts, all 14 named initial property rows, linked lord inputs, household eligibility/counts, generation gate and ordered property filter, force initializer, 90 canonical property routes, seven starting-home routes, and exact calendar terrain profiles | strategic movement recovery boundary |
| `OriginalStrategicCampaignState` / `StrategicSchemaTwoMigration` | mutable 14-property/176-person copies, five enemy slots, six player records, exact cursors/sub-cell/route-input state, generator/speed/profile/camera state, sparse full-dword terrain mutations, and deterministic schema-1 settlement | dormant schema-2 replacement boundary pending live binding and game-loop integration |
| `IOriginalStrategicResources` / `ImportedOriginalStrategicResources` | Core-owned signed route points and projected full-dword terrain cells backed by all 97 imported paths and `icon.jp` | eagerly verified application boundary shared by initial, new, and loaded campaigns |
| `StrategicRouteDecoder` | bounded little-endian count plus signed 32-bit x/y pair decoding for imported `rt_*.rat` resources | `ImportedContentCatalog.DecodeStrategicRoute`; consumed by the exact routed handler and generator constructors |
| `StrategicWorldProjection` | exact staggered route-space anchor, viewport adjustment, inclusive diamond raster, wrapped scan order, and route-point-to-grid inversion | terrain lookup boundary for decoded strategic routes |
| `TournamentOpponentDefinition` | wager, joust tolerance and eight-man melee composition | joust and tournament skirmish settlement |

The remaining switches in the core are state adapters: they map typed equipment slots, building kinds, and dilemma attributes to serialized player fields. They contain no prices, balance coefficients, content names, reward order, or eligibility rules. Imported resource names are translated at boundaries such as `ImportedDilemmaAdapter` and `ImportedSiegeLayouts`; screen code does not interpret raw resource records.

## Strategic save migration boundary

The current schema-1 `StrategicEnemyMovement` stores incompatible reimplementation `World.Locations` indices plus departure/arrival dates. The original system instead persists five movement records (FMT-STRATEGY-001) with a property origin, mode, target identity/slot, current and destination coordinates, direction remainder, route identity and waypoint cursor, path-complete state, lord, and three troop counts. These representations are not bijective: a dated schema-1 column has no recoverable original route cursor, person target, sub-cell position, or property-table identity. Schema 2 must therefore be an explicit semantic migration, never an index relabel.

Before enabling exact movement, schema-2 load settles every active schema-1 column in slot order by returning its complete troop total to its still-hostile schema-1 origin garrison when that origin remains valid, otherwise dispersing it, and recording either outcome in the campaign journal. `StrategicSchemaTwoMigration.Prepare` implements and tests that destructive boundary, clears the dated roster, and installs `OriginalStrategicCampaignState` only when a save has none. Schema-one saves written after source-shaped new-game bootstrap can contain both the exact state and the old dated roster; migration validates and preserves that exact state while settling only the incompatible columns, rather than discarding source route/camera/person state. The replacement owns mutable deep copies of all 14 original properties and 176 original people, five indexed enemy movement slots, six indexed player movement records, selected/distinguished player indices, route-input state, generator accumulator/reactive counter, bounded speed, calendar terrain profile, list heads, strategic camera row/column, and sparse full-dword terrain mutations. Enemy slots persist route identity/direction, waypoint count/index, integer destination/grid coordinates, floating current position/direction remainder, the mode-3 player target slot, path completion, lord, origin, mode, and troop counts. Player records persist their enemy/division-tagged target, fixed inline-route storage, cursor, destination/grid, cooldown, terrain kind, and floating motion state. Validation rejects partial populations, duplicate/missing slots or terrain cells, unknown target tags, non-finite remainder, and incomplete active records. JSON round-trip tests cover every state family and prove definitions are not aliased.

Legacy schema-1 saves that lack `OriginalStrategicState` contain neither a starting-home selector nor strategic camera coordinates. Migration therefore records selector `-1` as an explicit unavailable sentinel and camera `(0,0)` as a deterministic compatibility default; it derives the terrain profile from the saved month and clamps the existing 1-15 UI speed into the exact range. Newly created campaigns now select one of the seven valid `StartingRoutes` through `Campaign.InitializeOriginalStrategicNewGame`, persist the exact bootstrap alongside the still-schema-1 compatibility state, and validate its route during application resource binding. The immutable base terrain remains required resource 292 `icon.jp`; sparse mutations replace the process-global `temp.jap` scratch copy. Camera state is material because the cell a point finds depends on the camera (RULE-STRATEGY-014).

`IOriginalStrategicResources` is the dependency-inversion boundary. Core receives immutable signed route coordinates and camera-projected cells with the complete source dword; Game's `ImportedOriginalStrategicResources` eagerly decodes every distinct executable-selected property/starting route and `icon.jp`, caches forward and reverse order, rejects unknown or malformed assets, and uses `StrategicWorldProjection` for camera-dependent edge ownership. `ConquerorGame` binds the same provider to its initial campaign, every character constructor, and every loaded save. A prepared replacement save is structurally validated during binding, and each active routed slot must match the decoded route's exact point count before the provider becomes observable.

`OriginalStrategicMapCamera.FocusOnGridCell` implements `focus_on` (RULE-STRATEGY-014) for a future host rather than substituting conventional map centering.

`OriginalStrategicMapCamera.ApplyMappedEdgeScroll` implements edge panning (RULE-STRATEGY-014) as a state-only operation. It moves both axes in one call and leaves the hostile pass running, where the rule moves one axis and holds the hostile pass back.

`OriginalStrategicMapPointer.ToMappedRoutePoint` converts the pointer to route space as RULE-STRATEGY-013 does. Its input is the same 640-by-480 logical coordinates `ConquerorGame.OriginalPoint` supplies after host-window scaling; the mapper remains separate from that application boundary.

`OriginalStrategicMapHitTesting.HitTest` keeps the hit rectangles and precedence of RULE-STRATEGY-013 outside the dated campaign model.

`OriginalStrategicMapCommands.DispatchRawPointer` composes that map conversion and hit test with `DispatchPlayerMapCommand`. Target hits expose `RequiresTargetConfirmation` for the attack prompt of RULE-STRATEGY-013. The host preserves that synchronous boundary with an explicit pending state, repeats the saved raw pointer only on its Enter acceptance, and consumes Escape as decline; those key bindings and wording are Provisional host policy. Thus an unconfirmed target hit is consumed while a player hit selects and an empty point follows the fixed 20-waypoint route constructor. The core consumes the raid requests of RULE-STRATEGY-019 after each full strategic pass to create the two raids, but their visible labels and availability remain outside the dated map adapter's authority.

`OriginalStrategicMapTerrainRendering.BuildDraws` is the resource-neutral terrain pass for that screen. It keeps the order of `draw_terrain` (RULE-STRATEGY-015), and `BuildTileDraws` binds every position to its `icon.jp` low word. `OriginalStrategicTerrainPresentation` now binds that plan in the MonoGame host when a schema-2 state is present: it selects the season's atlas from the state profile (RULE-STRATEGY-009), intersects each source 80-by-80 frame with the logical viewport before scaling, and fails rather than remapping an out-of-range source tile. This presentation binding does not activate the strategic scheduler, pointer dispatcher, or legacy map controls.

The marker frames follow RULE-STRATEGY-015. `ImportedAnimations` makes `icon_men.CSF` and `marker.CSF` required application assets, while retaining the campaign-shell palette association as Corroborated pending recovery of the load-site palette. `OriginalStrategicCharacterColor`, the persisted `Player.OriginalStrategicCharacterColor`, and `OriginalStrategicMapMarkerPresentation.FrameBaseForCharacterColor`/`FrameBasesForCharacterColor`/`FrameFor`/`BuildDraws` preserve the frame base and selection, physical-slot order, active filtering, caller-supplied source bases, and pre-projection integer coordinates. `OriginalStrategicMarkerPresentation.BuildBlits` now combines that plan with `TryProjectBlitOrigin`, crops it to the same logical viewport as terrain, and the MonoGame host draws it after the source terrain when that state and the confirmed marker frames are available. It deliberately leaves the palette association Corroborated and does not activate strategic input or scheduling.

`OriginalStrategicMapMarkerProjection.TryProjectBlitOrigin` maps the bounds and origin of `draw_marker` (RULE-STRATEGY-015), and `OriginalStrategicMarkerPresentation` applies the viewport intersection as the host counterpart to the blitter's clipping.

`OriginalStrategicMovementSlot.MarkerFrame` persists each hostile record's frame (RULE-STRATEGY-004, RULE-STRATEGY-015), and the scheduler, `BuildMovementDraws`, `BuildMovementBlits`, and the MonoGame player-then-movement draw order preserve it. Palette binding remains Corroborated.

The MonoGame map host now invokes `FocusOnGridCell` at the new-campaign map handoff, using the persisted original home grid before its first terrain/marker draw. On every fixed host update it applies `ApplyMappedEdgeScroll` to the logical 640-by-480 pointer. Primary clicks inside the recovered source viewport dispatch through `OriginalStrategicMapCommands` with the three persisted temporary-force records in source slot order: source player-marker selection and route construction are active, and enemy/division target clicks enter the shared confirmation boundary. The host repeats a saved raw click only after its explicit confirmation policy accepts it. This deliberately removes the source's processor-dependent panning cadence without fabricating the original dialog's wording or physical controls.

`OriginalStrategicHostRuntime` also invokes `Campaign.AdvanceOriginalStrategicPass` exactly once from each MonoGame map update. The host is explicitly configured for a 60 Hz fixed timestep, so recovered temporary patrol, player, and hostile work use a stable application cadence instead of the original's unrestricted processor throughput. The campaign first advances initialized slot-1/2 temporary routes in physical order, then applies the order expiry of RULE-STRATEGY-017 before projecting the resulting three persisted records into the player-command target array; this preserves the original temporary -> report -> player -> scheduler relationship without a stale host snapshot. New campaigns retain their source-established fallback target/origin globals and therefore run the hostile scheduler; schema-one migrations retain safe player-record updates but block that scheduler rather than inventing fallback identities. A typed player/enemy contact now opens the source-backed resolver screen and freezes subsequent strategic passes until its automatic or interactive settlement returns. The yearly brigand order and the orders from the king (RULE-STRATEGY-016) are not created.

The map's Plus/Minus controls now call `Campaign.AdjustStrategicSpeed`, changing the live `OriginalStrategicCampaignState.SpeedMultiplier` within 1 to 15 (RULE-STRATEGY-009) and mirroring it into schema-1 `DaySpeed` while the dated adapter remains. `Campaign.AdvanceDays` reselects `TerrainProfile` at each month boundary from the month (RULE-STRATEGY-009), so the imported seasonal atlas and source movement speed table advance together. The calendar-boundary update is a host policy; the original sets the season at map setup and in the map dispatcher. This binding does not make the dated travel, spy, and enemy-movement adapter source-equivalent; schema 2 stays gated on those remaining migrations.

`ImportedMovies`, `OriginalStrategicTerrainPresentation.TransitionMovieRoleFor`/`TransitionMovieBounds`, and `ConquerorGame.PlayPendingStrategicSeason` bind the season movies of RULE-STRATEGY-009, whose 192-by-268 dimensions the Smacker inventory gives, to a dated profile change. The host queues a movie at manual calendar advancement or travel and plays it on map reentry; that trigger timing is **Provisional** because the source's complete map reentry calendar path is not yet mapped. `ResourceAndDefinitionTests.StrategicCalendar` pins the profile-to-movie mapping and source placement.

The dragon's lair ends the player pass before later player records or the hostile pass run (RULE-STRATEGY-010). The host's optional Escape withdrawal leaves a live record in that region, so `ConquerorGame.UpdateOriginalStrategicMapRuntime` keeps a transient suppression latch until that record leaves. It forwards `SuppressDragonEntry` to the player pass only during this host-only interval, allowing the later records and scheduler to continue without opening another battle. The source-backed, unsuppressed entry still returns early and defers scheduler work while the asynchronous battle opens.

`OriginalStrategicMovement.AdvanceSchedulerPass` is the complete mutable scheduler boundary around the three-mode motion kernel. It validates replacement state, runs generation before movement, constructs into the first inactive physical slot, then interleaves one handler and its completion outcome for slots 0-4. The shell implements the two exact force initializers, direct grid-person probes, origin-state reinforcement, encounter handoff, and first-nearby-army/player/origin retargeting. `IOriginalStrategicResources.TryGridCell` exposes the cell's auxiliary person identity without reprojection. The rare timed branch of RULE-STRATEGY-003 tests a stale property (BUG-STRATEGY-001); the rebuild admits that branch only when the current pass supplied a real detection.

`AdvanceSchedulerPass` derives reactive contact directly from those five player-army targets. It scans properties then player slots in authored order, applies the gates of `find_reaction` (RULE-STRATEGY-003), sets `alerted` and `approached`, and returns one-shot `OriginalStrategicPropertyAlert` values for presentation. The optional injected detection remains only a focused test/integration seam.

`CurrentSaveSchemaVersion` intentionally remains 1 and `Campaign.AdvanceDays` still uses the isolated dated adapter. The six-record player kernel, command constructors, player/enemy contact shell, exact new-game avatar bootstrap, army/avatar membership exchange, and field-record construction/removal are persisted and executable through `OriginalStrategicPlayerCommands`, `AdvancePlayerMovementPass`, `AdvancePlayerPass`, and `Campaign.ToggleArmyMembership`. New custom and pre-generated Game campaigns now initialize that bootstrap through the campaign-owned random source; loaded legacy saves remain selector `-1` until migration. `Campaign.FieldArmy` now constructs its matching original player record when a replacement state is present. A new game also stores `fallback_person` and `fallback_origin` (RULE-STRATEGY-012); `OriginalStrategicCampaignState.SchedulerFallbackTargetPerson` and `SchedulerFallbackOriginProperty` persist those source-owned globals rather than asking a screen to recreate them. `Campaign.AdvanceOriginalStrategicPass` reads those stored globals directly, binds a configured resource provider, external division targets, the live five army records, and the report -> player -> hostile-pass order of RULE-STRATEGY-001. It allows the report/player portion of a migrated save to run when the globals are unavailable, but refuses the scheduler rather than fabricating values. Property 7's household is derived internally from the persisted person list (RULE-STRATEGY-005). `SchedulerBlockedByModal` stands for `map_busy` (RULE-STRATEGY-001). `PlayerEncounterHandoffActive` stands for the encounter's `map_session_over` guard (RULE-STRATEGY-011); it suppresses a duplicate contact handoff without suppressing player selection or the hostile pass. The scheduler receives `ridden_force`, which can be 5. Replacement reports retain the original property name rather than fabricating an incompatible `World.Locations` index. The remaining activation gate is screen input/handoff wiring and fixed-update/load-save integration. Only after that replaces the adapter may migration be invoked from `Campaign.Load` and the schema advance to 2.

Strategic player/enemy contact deliberately remains a typed application handoff, not a call to `FieldBattleSession`. Resolver gate `0x256FC` is an interactive choice dialog; `OriginalStrategicEncounterMenu` preserves its five original regions and jump-table codes in input order: upper-left `30,30,215,185` -> 2, upper-right `400,30,205,200` -> 3, lower-left `25,250,185,185` -> 1, lower-right `375,250,185,185` -> 0, and bottom-centre `220,417,148,38` -> zero-return automatic fallback. Its shared `0x6FF10` selector scans that same order with left/top-inclusive and right/bottom-exclusive containment; the four tactical names remain intentionally unrecovered. When an interactive choice proceeds, `OriginalStrategicInteractiveEncounter` materializes one 52-byte-compatible live unit per staged counter in player swordsman/halberdier/knight order followed by the enemy's same order. It preserves immutable category columns `0/120/240`, lanes `0/360`, type codes `3/7`, and values `10/20/40`; terminal write-back counts only strictly positive strength by that immutable classification. For menu code 0, `0x2904B` places the player prefix at `(60 * floor(i / rows) + 60, 60 * (i % rows) + 30)` where `rows = verticalSpan / 60`; code 1 at `0x290BA` mirrors that x coordinate around `(playerCount / rows) * 60 + 60`. Code 2 at `0x29132` uses the smallest triangular row count `r` with `r * (r + 1) / 2 >= playerCount`, emits row lengths `1..r`, starts at `(45r, verticalSpan / 2)`, and shifts each row by `(-45,-30)`. `ApplyMappedMenuFormation` implements those exact code-0/1/2 player paths, while `ApplyMappedMenuCodeThreeFormation` separately preserves code 3's raw-draw-dependent player/enemy grids. This establishes the six-counter contract for a future exact tactical simulation without replacing it with aggregate `FieldBattleSession` logic. `ResolveAutomatic` compares staged totals plus caller-provided `modifier / 3`, zeros only the staged player counters on a non-winning score, or subtracts three ordered raw-draw remainders modulo the shrinking hostile total from player categories and then restores reserves. `Campaign.AdvanceOriginalStrategicPass` exposes an immutable `OriginalStrategicPlayerEnemyEncounter` snapshot for each accepted contact, carrying the exact player/enemy slots, raw counters, and staged counters before scheduler work; it deliberately has no legacy battle dependency or resolution side effect. `Campaign.ResolveAutomaticOriginalStrategicEncounter` applies that fallback only after verifying the snapshot still matches live records, writes category totals back to the player army and hostile movement slot, and reports either the exact ordinary-field-record removal or the distinguished-player modal requirement. `BATTLE.PCX` and `MEN8.CSF` are loaded as `Encounter.Strategic.Background` and `Encounter.Strategic.Units`, respectively, so the source battlefield and its palette-associated unit frames are verified application dependencies. Their exact layout/frame state dispatch remains intentionally external until the resolver presentation path is recovered. The interactive tactical UI, choice meanings, and conversion to a first-person session remain Provisional; no aggregate battle, garrison, or dated `World.Location` value is an authorized substitute for its six-counter contract.

The preceding initial `3/7` values are headings, not immutable type codes. Constructor `0x28C38` stores them in record `+0x14`; `0x2964C` derives the desired eight-way heading from live source/target coordinates, and `0x296B4` turns that field one octant per pass before entering state `0x28` only when already aimed. State-zero first uses its own source-excluding rectangle-corner acquisition; any resulting or retained target field turns immediately and prevents both later branches that pass. With no target but an outstanding paired destination, it turns once toward that coordinate and then walks vertical before horizontal axes in the same pass. With neither target nor destination, control-code-one sources scan living opposing records in authored order, retain strictly nearer truncated Euclidean distances from `0x7FFF`, and seed the source/candidate destination fields from the chosen record. `AdvanceMappedStateZero` composes this exact precedence from `DetermineMappedHeadingOctant`, `AdvanceMappedTargetHeading`, `AdvanceMappedDestinationOrder`, and `TryAssignMappedAutomaticDestination` against one pre-pass rectangle table. Broader input/presentation scheduling remains pending.

Code 3 at `0x29209-0x29643` is now mapped separately because its exact path needs both viewport dimensions and one raw encounter draw. It splits player swordsmen at `floor(swordsmen / 2)`: its first half uses four-high rows at `x = 60 * (floor(floor(swordsmen / 2) / 4) + 1) + 60 * floor(nonSwordsmen / rows) - 60 * floor(i / 4) + 120`, `y = 60 * (i % 4) + 30`; its latter half retains x and begins at y 480. The non-sword player suffix uses `x = 60 * (floor(floor(swordsmen / 2) / 4) + 1) + 60 * floor(nonSwordsmen / rows) - 60 * floor(i / rows)`, `y = 60 * (i % rows) + 30`. One non-negative raw draw modulo two selects the enemy grid: zero mirrors each column at `horizontalSpan - (60 * floor(i / rows) + 60)`, while one starts at `horizontalSpan - (60 * floor(enemyCount / rows) + 60)` and advances x by 60 per column; both use `60 * (i % rows) + 30` for y. The original's four-entry switch has cases 2/3 which cannot follow its modulo-two remainder, so `ApplyMappedMenuCodeThreeFormation` preserves only the two reachable layouts rather than recreating dead paths. This supersedes the earlier code-3 limitation above; the existing three-layout API remains intentionally narrow because it has no source for the required width or draw.

The first exact interactive-loop record mutation is also mapped. At `0x26B88`, each positive-strength record produces an 8-byte render rectangle from `(+0x04 - 15, +0x08 - 20, 25, 30)` while non-positive records produce zero rectangles, then the loop calls `0x264F8` and returns immediately on its nonzero result. In its completed death state `+0x1C = 0x50`, when `(prior + 1) % 5 = 4`, the loop decrements the matching side total, writes `+0x20 = 0`, and changes category `+0x00` from knight `0xF0` to halberdier `0x78`. Thus category values are initial classification values, not permanently immutable live fields; strict-positive survivor write-back remains unaffected. `OriginalStrategicInteractiveEncounterSession.AdvanceMappedTacticalPass` retains one rectangle snapshot and visits the table in source order: it advances death state, applies state-`0x28`'s zero-offset probe/contact, and then enters state zero only when that result has cleared the current state. The original input and processor-dependent timing wrapper remain deliberately external.

Interactive resolver loop `0x26B88` is timed by helper `0x18420`, which returns interrupt counter `A688` shifted left two. Setup `0x183E8` registers callback `0x183C0` for 250 Hz through timer service `0x69AAC`; the service derives the hardware divisor from `0x1234DC / 0xFA` and programs timer port `0x40`. The callback increments `A688`, so the resolver sees nominal four-ms steps. The loop skips a tactical pass while `currentClock <= lastSample + 0xC8`, then samples the clock again and replaces `lastSample` before visiting units. The first eligible sampled clock is 204 ms after an aligned baseline, with no catch-up passes after a late frame. `OriginalStrategicInteractiveEncounterTiming` quantizes its default monotonic host clock to four-ms ticks and preserves the strict comparison. Explicit custom cadences remain available for tests. `19C7C` initially skips tactical work; the first control stores zero there and sets `19C8C`, enabling passes. Only later hits with `19C8C` set display the retreat dialog. `AdvanceMappedFrame` preserves that activation, later confirmation, enemy-first/player-second terminal order, and deterministic cadence without reproducing processor dependence.

The interactive neighbor path now has its exact geometry primitive. Selector `0x6FF10` scans the temporary eight-byte rectangle array in increasing table order, returns its first hit as a one-based index (zero for no hit), and delegates to `0x64164`'s `left <= x < left + width`, `top <= y < top + height` predicate. The `0x26B88` loop emits `(PositionX - 15, PositionY - 20, 25, 30)` only for positive-strength units and a zero rectangle otherwise. `OriginalStrategicInteractiveEncounterGeometry` preserves this ordered, half-open selector, `0x28408`'s source-excluding transformed-corner helper including its dead-first-hit asymmetry, and state-zero's distinct opposing-only corner acquisition. `AdvanceMappedDestinationOrder` uses that retained start-of-pass table for the verified state-zero destination step; broader state-machine scheduling remains intentionally unmapped rather than approximated.

Before dispatching encounter controls, `0x264F8` applies viewport scrolling in four source-ordered checks. A pointer at most five pixels from the left/top decrements its respective offset by 10 only when it is positive; a pointer at least five pixels from the right/bottom increments it by 10 only when `contentWidth - viewportWidth - 10 > horizontalOffset` or `contentHeight - viewportHeight - 50 > verticalOffset`. The strict inequalities and distinct 50-pixel lower reserve are material compatibility facts, not generic camera clamping. `OriginalStrategicInteractiveEncounterViewport` exposes those offsets and `ApplyMappedEdgeScroll`; the input event codes and associated UI labels remain deliberately unmapped.

The live interactive unit model now retains the constructor's remaining state fields from `0x28C38`: `+0x0C/+0x10 = -1`, `+0x18/+0x1C = 0`, `+0x28 = 0/1` for player/enemy, and `+0x30 = -1`. `+0x0C/+0x10` are later written together by the control path, `+0x18` is a modulo-five phase counter, `+0x1C` reaches the confirmed death state `0x50`, and `+0x30` is later consumed as a table-relative unit index. `+0x28` is intentionally exposed as `ControlCode`, not falsely named a permanent side flag, because the control path overwrites it. `OriginalStrategicInteractiveEncounterUnit` preserves these fields with source-offset comments and initialization tests while their remaining semantics stay Provisional.

The first player-control operation is now executable rather than inferred from a generic tactical model. For each selected unit index, `0x26A7F-0x26B64` clears `ControlCode`, writes `AuxiliaryX = localX + horizontalOffset` and `AuxiliaryY = clampedLocalY + verticalOffset`, where local Y is first raised to 45 and then lowered to `verticalSpan - 85`; X is not clamped. `ApplyMappedDestinationOrder` preserves the original selected-list order, paired destination writes, and this asymmetric boundary rule. `ApplyMappedInput` dispatches recovered codes 6/7 to it, while retaining caller-owned event production and later state evaluation.

Two neighboring control mutations are also literal-mapped but deliberately unnamed as gameplay commands. `0x26918-0x26966` writes `ControlCode = 1` to every index already in the selected list, with no fresh side or strength check; `0x26968-0x26999` writes the same value to every positive-strength record across both lanes. `SetMappedControlCodeOneForSelectedRecords` and `SetMappedControlCodeOneForLivingRecords` preserve their different filters. The outer raw dispatcher reaches these exact branches only through `0x61` and `0x41` respectively, exposed by `ApplyMappedRawControlCodeOneInput`; the source display regions, host event producer, and meaning of control codes zero and one remain Provisional.

The first control strip rectangle is no longer unnamed: its initial hit enables tactical passes by writing `19C7C=0` and `19C8C=1`; only a later hit invokes the object-2 prompt `Do you want to retreat?`, whose accepted reply ends the resolver. `ActivateMappedFirstControl` and `ResolveMappedRetreatConfirmation` retain this Confirmed activation/terminal route without inventing the host's affirmative/negative event mechanics. Before any input dispatch, `CaptureMappedHoverPresentation` uses the scroll-adjusted unit selector to expose `OUR %3d%%` for player records, `FOE` for enemy records, then exactly one blank-frame aggregate `WINNING` when live enemy count is at most player count or `LOSING` otherwise. `HoverStatusPanelBoundsFor` preserves its `(controlMargin + 160, viewportHeight - 25, 83, 20)` anchor. `AdvanceMappedFrame` carries that source-ordered presentation result for a future screen binder; it remains independent of the stable tactical cadence.

The session also exposes the three mapped category-selection loops separately from unrecovered key-event production. `AppendMappedPlayerCategorySelection` appends living player swordsmen, knights, or halberdiers to the current byte-list in authored materialization order and preserves prior selections. `RouteMappedCategorySelectionInputCode` and the session's raw-input facade additionally preserve the outer dispatcher's exact `0x48/0x68`, `0x4B/0x6B`, and `0x53/0x73` routes to swordsmen, knights, and halberdiers. They remain distinct from the small pointer/control dispatcher codes. The implementation does not invent selection clearing, an arbitrary capacity, a visual-order sort, or an operating-system key producer.

The state-`0x28` contact's resolved-record arithmetic uses the mapped zero-offset neighborhood probe. At phase two, `0x26DCB-0x270B4` subtracts raw remainder modulo 10 for knight→halberdier, swordsman→knight, and halberdier→swordsman contacts, or modulo 7 otherwise; targets in the enemy lane also receive the resolver's `playerScoreModifier / 3`. Target health strictly between zero and 20 enters state `0x50` with phase zero; a non-positive target clears the attacker's target/state and clears its auxiliary pair only when the attacker has control code one. `OriginalStrategicInteractiveEncounterCombat` and `AdvanceMappedTacticalPass` preserve these record effects, the source's `19C98` lane filter, and re-entry into state zero without assigning unverified buttons or timing.

Immediately before that arithmetic, `0x26D4B-0x26DC5` calls `0x28408` with zero X/Y offsets and treats probe results zero and one as a contact loss: it clears state and target, then clears auxiliary coordinates only for control code one. Every other result advances the phase counter modulo five, and only the resulting phase two may resolve damage. `ApplyMappedContactProbeResult` and the fixed pass preserve that raw return-code boundary and carry a contact-loss record into the immediately following state-zero branch.

Player hit selection is likewise mapped as an append-only source operation. After the ordered rectangle selector finds a unit, `0x26999-0x26A77` accepts it only when its lane is player and strength is positive, scans the existing selected-index list, and appends it only when absent. It does not toggle a prior selection. `TryAppendMappedPlayerSelectionAt` supplies the source's viewport-adjusted pointer selector, and `ApplyMappedInput` dispatches code 2 plus the code-3 control-strip miss to it. Deselect/control semantics beyond the recovered control-one routes remain deliberately unlabelled.

`OriginalStrategicInteractiveEncounterSession` now composes the evidence-backed unit table, all four formation paths, selection, recovered input routes, destination ordering, render rectangles, exact lane counters, full state-zero branch precedence, and fixed tactical pass into one application-facing state boundary. Code 3 alone receives the raw random provider it needs; codes 0-2 do not consume one. Post-animation death completion decrements the matching lane counter once, so the automatic branch retains its original global gate. Its one-record `AdvanceStateZero` helper constructs a visible rectangle snapshot, while `AdvanceMappedTacticalPass` retains one snapshot for the full unit iteration. `AdvanceMappedFrame` connects scrolling, input dispatch, armed-confirmation termination/suspension, and the stable timer in recovered source order; it takes the otherwise unrecovered dialog result as a host value. `ApplyMappedInput` remains available for caller-provided event coordinates without frame advancement.

The control-strip coordinate is intentionally independent from battlefield scroll. Resolver setup stores the resolution margin at `19C74` (`0`, `0x50`, or `0xA0` on its recovered paths) and adds it to the three fixed control X positions; dispatcher code 3 consumes raw pointer coordinates against those rectangles. Player selection and destination orders instead add scroll globals `19CA8/19CAC`. `OriginalStrategicInteractiveEncounterViewport.ControlStripMargin` retains this split, and `AdvanceMappedFrame` resolves the control strip after edge scrolling without allowing that scrolling to change its hit geometry.

`OriginalStrategicInteractiveEncounterPresentation.FrameFor` maps the renderer's source frame address through the strategic encounter screen: lane + category + `5 *` heading + phase remainder + state selects ordinary `MEN8` frame 0-719. `UnitDrawsFor` retains source comparator `0x289CC`'s dead-before-living, then Y/X ascending ordinary pass; exact ties use authored materialization order as the explicit stable compatibility policy because the source comparator returns zero. `SelectionOverlayDrawsFor` then preserves `0x28B5C`'s post-unit selected-list pass: source frame 720 is emitted once per selected-list byte, in list order, at live x/y after scroll translation and offsets `(5,7)`. `PendingFirstControlDrawPositionFor` and `ControlStripDrawPositionFor` retain the two remaining literal special-frame placements: source frame 721 sits at first control rectangle plus `(15,6)` when armed, and initial frame 722 at the same rectangle plus `(20,6)`. The host also renders `AdvanceMappedFrame`'s pre-dispatch status at `HoverStatusPanelBoundsFor`: exact source messages are `OUR %3d%%`, `FOE`, and the one blank frame of `WINNING`/`LOSING` after hover. Wrapper `0x35924` also supplies the resolver's former caller-owned player modifier exactly as `(HONOR + FAME + (record == AE6C ? 20 : 0)) / 6`; its enemy modifier is literal zero. `ConquerorGame.StrategicEncounter` captures that value for the active resolver, freezes the strategic scheduler while the captured contact is active, dispatches the five recovered menu regions to either exact automatic settlement or the typed interactive session, and settles only through `Campaign.ResolveInteractiveOriginalStrategicEncounter`; it never substitutes `FieldBattleSession`. Its host queues all simultaneous primary and secondary transitions in original bit order with captured timestamp and coordinates, then classifies one entry into source code 1-8 per stable update before dispatch. `OriginalStrategicPointerClock` follows the recovered timer-service accumulator through the chained BIOS IRQ0/INT 1Ch route, giving an aligned four-unit window of 220 ms. Initial interrupt phase, controller secondary shortcut, keyboard convenience choices, 640-to-canvas scaling, and textual option labels remain **Provisional** replacement policy. Frame identity, source ordering, status text/panel position, score formula, selection placement, menu geometry/codes, source art, strict 200 ms tactical gate, and six-counter settlement are **Confirmed**.

## Validation

The executable specifications reject duplicate equipment and courtship names, duplicate win rewards, mismatched building keys, and dragon score items that cannot be earned from a defined reward ladder. Full campaign behavior tests then exercise the same generic interpreters used by the game.

## Runtime paths and packages

`GamePathResolver` keeps distributable content and writable player state separate. Imported `UserContent` is selected explicitly by `--user-content`, then `RECONQUEROR_USER_CONTENT`, an adjacent or package-root directory, a development checkout, and finally the platform's per-user application-data directory. Saves and settings always use the per-user `ReConquerorAD1086` state root, so an installed game never assumes its program directory is writable. Windows Inno Setup, Linux Debian, and macOS package builders all consume the same verified, self-contained portable publish layout.

`Directory.Build.targets` enforces a 1,000-line ceiling for every compiled C# source file in every project, making oversized responsibilities a local-build, test, CI, and release failure. `ConquerorGame` is divided into lifecycle, campaign-input, general-presentation, and location/combat-presentation partials; the large resource regression suite is divided along the same responsibility boundaries. The limit can be lowered with `MaximumSourceFileLines` for validation; disabling it requires the explicit `DisableSourceFileLineLimit=true` MSBuild property.
