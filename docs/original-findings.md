# Findings from the owned original

This register records compact, non-copyrightable facts derived from the user's installed GOG release. It deliberately does not contain extracted binaries, resource dumps, long dialogue, or media.

## Confidence scale

| Grade | Meaning | Can be treated as factual? |
| --- | --- | --- |
| Confirmed | Directly measured in the identified files, or unambiguous executable text/structure | Yes, for the hashed GOG build below |
| Corroborated | Independent documentation agrees with binary/resource evidence, but the exact executing code path has not been recovered | Usually; not necessarily an exact formula or table |
| Provisional | Plausible implementation chosen to make the game playable while exact original behavior remains unknown | No |

Confidence applies narrowly to the stated claim. Finding a name near joust resources, for example, proves that the identifier is present; it does not prove an opponent's odds or wager.

## Provenance

| Artifact | Size | SHA-256 | Confidence |
| --- | ---: | --- | --- |
| `game.gog` | 667,316,496 bytes | `8a584cc03a0a19f74851d2c1f4653804a58bfbf2c16e4fd34759f7a7f88cf015` | Confirmed |
| Track 1 ISO payload | 244,445 sectors | derived from track 2 index `54:19:20` in `game.ins` | Confirmed |
| `CONQUER.EXE` | 919,107 bytes | `5d7231758766204ad061e6b82cf2f0e0cbe28899b35d095f13e4aad75c8b79d6` | Confirmed |
| `C1086.GOB` | 35,361,714 bytes | installed loose resource archive; hash is intentionally left to each local report | Confirmed size for this build |

These hashes identify the evidence source and are not claims that every retail or localized release is byte-identical.

## Tournament findings

| Finding | Evidence | Confidence | Implementation consequence |
| --- | --- | --- | --- |
| Jousts and castle melee both have wager-acceptance flows. | Distinct wager/refusal text adjacent to joust code around `CONQUER.EXE` offsets `0xCF1F7`-`0xCF2CE`, and melee text around `0xD1CF4`-`0xD1DF3`. | Confirmed | Both tournament event interpreters debit a stake and settle it on victory. |
| A participant with no money can be refused. | Separate zero-money refusal messages exist for joust and melee. | Confirmed | An event cannot begin when the selected wager is unaffordable. |
| The original exposes opponent choice and wagers commonly fall between 20 and 80 shillings. | [GameFAQs playthrough documentation](https://gamefaqs.gamespot.com/pc/574792-conqueror-1086-ad/faqs/66730); resource text also contains spelled-out shilling amounts. The full numeric table has not been recovered. | Corroborated | Definitions constrain current stakes to that range. Do not treat the individual stake assignments as original facts. |
| Joust resources identify Simon, Richard, Gerard, and Gilbert. | Printable identifiers `jsimonle.pcc`, `jrichard.pcc`, `jgerard.pcc`, and `jgilbert.pcc` in `CONQUER.EXE`. | Confirmed identifiers only | Those first names are preferable to invented full identities. It remains unproven that these are the complete opponent list. |
| Five selectable melee opponents accompany roughly eight soldiers per side. | [External FAQ description](https://gamefaqs.gamespot.com/pc/574792-conqueror-1086-ad/faqs/66730); executable has a distinct melee selection/acceptance flow. | Corroborated | Five data rows, each totaling eight enemy soldiers. |
| Current stakes `20/35/50/65/80`, hit tolerances, and per-opponent unit mixes reproduce the exact original table. | No direct evidence yet. | Provisional | Centralized in `TournamentOpponentDefinition`; expected to change after disassembly or controlled observation. |

## Other executable findings

| Finding | Evidence | Confidence |
| --- | --- | --- |
| The release has separate first, second, and third joust ordinal text and refuses further jousts when opponents are tired. | Adjacent printable strings in `CONQUER.EXE`; external documentation specifies three jousts. | Corroborated |
| Castle skirmish uses `SKIRMISH.RES`, `SKIRMISH.CSF`, and `SKIRMISH.PCX`. | Literal resource names in `CONQUER.EXE`. | Confirmed |
| The configured original supports a `WAR_MODE` setting and the shipped CD configuration uses `640`. | Extracted `CONQUER.INI` in the hashed disc image. | Confirmed for this disc configuration |
| Campaign first-person assaults pass a friendly-combatant cap `max(1, sum(min(floor(units[type] / 3), 3)))` over Swordsmen, Halberdiers, and Knights. Loader `0x51560` recognizes actors by behavior bit `0x80` plus selector 1, scans x-major, and makes the first friendly actor player `D4D0`; template indices 0-2 are friendly and 3/5/8/9 hostile throughout all 1,002 placements in the supported 42-archive combat population. Counter `0x4D870` excludes `D4D0`, and pruning at `0x51B87` only removes excess authored retainers. Combat exit returns the living-retainer count; each missing retainer removes one strategic soldier from the represented per-type contributions in that same order. | Complete `0x5877C` caller inventory and focused disassembly of hashed `CONQUER.EXE` at `0x39EAA`-`0x39F79`, `0x3A0F9`-`0x3A1CE`, `0x4D870`, `0x517B7`-`0x5192E`, `0x51B87`, and `0x586A8`; `0x29E58/0x29E80` read/write the three strategic pools. Full owned-resource census covers 144 actor-base blocks and all 1,002 placements. The [GameFAQs FAQ](https://gamefaqs.gamespot.com/pc/574792-conqueror-1086-ad/faqs/66730) independently reports the one-soldier fallback and its loss consequence. | Confirmed for the hashed release and supported official scene population. `ImportedSiegeLayouts`, `SiegeLayout.Retainers`, `OriginalRetainerCombat`, `SiegeSession`, and `Campaign.FinishSiege` implement the mapping and formulas; focused tests cover actor partitioning, x-major player omission, authored caps, formula boundaries, loss order, and each one-soldier army. The former `/50`, 2-10 campaign attribution is Disproved. Raw block-color normalization, formation, and exact movement/pathing remain Provisional; command intent is covered separately below. |
| The combat strip's original-coordinate region `x=4..209`, `y=175..187` dispatches Attack, Defend, Follow, and Retreat in 56-pixel columns. The handlers write requested actor modes 6, 2, 16, and 10; Follow also targets player actor `D4D0`. Orders affect selected living friendlies or every living friendly when none is selected, then clear selection. Defend scans the surrounding 3x3 cells, Attack seeks a strictly nearer hostile, and Follow approaches its player target. Friendly template-to-kind mapping is `0/1 -> 0`, `2 -> 1`. Kind-0 Attack enters acquired mode 8; kind 1 enters ranged mode 11. Public Retreat mode 10 acquires a visible opponent, then kind 0 enters preserved-heading mode 5 (or mode 2 on failure) while kind 1 enters mode 4 (or mode 2). The former claim that public Retreat directly invokes the away-vector handler is Disproved. | Input dispatch `0x55D20`-`0x55DAE`; command handlers `0x5700E`, `0x57168`, `0x572C5`, `0x5744B`; thinker/state `0x50524/0x4F49C`; transition helper `0x4E5F0`; template initializer `0x542F8`; kind tables at object-1 `0x3E43C/0x3E480`; Attack transitions `0x4E71F/0x4E745`; Retreat transitions `0x4E76B/0x4E805`; acquisition `0x4F98D`; handlers `0x4FDAB/0x4FDCD/0x4FE76`; runtime tables `0x4F414/0x4F458`; actor fields `+0x18..+0x30`; owned `SKIRMISH.PCX` labels. | Command dispatch, Defend/Attack/Follow intent, friendly kind identities, Attack/Follow movement states, and corrected Retreat transition are Confirmed for the hashed release. `OriginalCombatantTemplates.ActorKindForSceneTemplate`, imported spawns, `SiegeRetainerCommand`, `SiegeSession`, presentation, and focused tests implement the mapping. Exact acquisition visibility/contact range, no-target Attack, later mode-5 formation behavior, kind-1 mode 4, and thinker cadence remain Provisional. |
| Pointer world actions preserve the exact ray-hit identity. Primary flag-0 input first gives every selected friendly the projected contact coordinates and mode 12, clears selection, and consumes the click. Only without that selected pass does a friendly toggle selection, a hostile become an explicit mode-8/player target, or an actionable object pass the `< 0x280` limit. Transparent pixels do not count as a hit. | Centered-pointer call `0x55DB8`-`0x55DE5`; world dispatcher `0x55524`-`0x55A58`; fixed-point raycaster `0x470A8`-`0x47589`; per-pixel test `0x444E8`; block-to-actor lookup `0x4CEA0`; actor fields `+0x18/+0x1C/+0x24/+0x28/+0x2C/+0x30`; action dispatcher/replacement `0x51E60/0x4C900`. | Dispatch precedence, target identity, selection behavior, mode numbers, alpha testing, object limit, and downstream mode-12 orders Confirmed. Runtime pointer picking implements ordered structural and kind-4 candidates, block-specific vertical bounds, depth, heading/mirror selection, alpha, and live identity. |
| Requested mode 12 stores a projected surface contact in actor `+0x28/+0x2C`, applies only to selected actors, and clears selection. The contact cell need not be passable; movement collision governs entry. It compares target coordinates with the actor's current cell, derives heading from `target - current`, clears target actor `+0x24`, schedules movement, and finishes only on the exact target cell. Heading helper plus `(heading + 0x20) & 0xC0` makes exact diagonal ties choose the clockwise cardinal. | Pointer branch `0x555AB`-`0x5562E`; acquisition `0x4FD7B`-`0x4FD95`; mode handler `0x4FF53`-`0x5001F`; heading helper `0x445C4`-`0x446AA`; scheduler return `0x53CE4`-`0x53D5F`. | Mode identity, dispatch precedence, fields, selected-only behavior, direction quantization, target clearing, effect-boundary re-aim, blocked-contact acceptance, and exact-cell completion Confirmed. `CommandSelectedRetainersTo` implements it using the ordered candidate contact, including live kind-4 identity. |
| Actor block signed word `+0x46` selects movement independently of visual-state selector `+0x2E`. All 144 placed actor bases (1,002 placements) select descriptor 7 or 10, start cloned-block offsets `+0x24/+0x28` at `(0,0)`, and use three 200 ms ticks, descriptor flags `0x142`, local 8.8 delta `(64,0)`, and surface stride 5. Actor records start at `(cell << 8) + 0x80`; the scheduler rotates the delta by heading, checks a neighboring block's behavior bit 2 beyond `+/-0x59`, and crosses/wraps only beyond `+/-0x80`. Direct handlers for mode 12 and shared Attack/Follow modes 8/16 rewrite `0x142` to live flags `0x112`; collision therefore keeps the actor's moving-axis offset and heading, terminates the live effect through flag `0x10`, recalls actor thinking immediately, and retries with a fresh direct aim on the next scheduler update. The earlier mode-12 left-turn interpretation is Disproved: the flag-`0x40` branch applies to modes 5/6/17, whose handler installs `0x142`. Offsets on an open line progress `0,64,128,192 -> next cell/-64`. `3 * 200 = 600 ms` is the first cell transition and normal effect duration, not a universal cell interval; retained remainder gives four ticks/800 ms per sustained cell. Across all 688,128 placed cells in the supported scenes, 52,278 have bit 2 and 635,850 do not; every actor's underlying state target is clear. | Loader actor centers at `0x5185F`-`0x51875`; shared direct handler `0x4FE76`; mode-12 selector and flag rewrite at `0x4FFBC`-`0x4FFDA`; mode-5/6/17 flag rewrite at `0x4FE17`-`0x4FE30`; constructor `0x4C7F4`; scheduler deadline, heading rotation, collision/cell bands, blocker lookup, flag branches, early completion, and thinker return at `0x5310D`-`0x5313C`, `0x53395`-`0x53D5F`; complete `MELEE*`/`DEFEND*` owned-resource census produced by the ignored `--scene-blocks` report. | Selector identity, complete official population, effect fields, strict tick cadence, sub-cell formula, authored blocker predicate, direct-mode retained-offset stop/retry, open-path completion boundary, first-transition formula, and sustained rate Confirmed; universal 600 ms cells and mode-12 left-turn collision are Disproved. `SiegeLayout`, imported object stages, `SiegeActorMovement`, Attack/Follow/destination per-tick movement, surface-frame stepping, and shared draw/pick projection implement them. The `0x40` corner/turn family, multi-actor destination behavior, no-target mode 6, Retreat mode 10, acquisition thresholds, and full actor-wide cadence remain Provisional in runtime. |
| Kind-0 Retreat mode 5 closes the previously Provisional flag-`0x40` actor branch. Handler `0x4FDCD` preserves heading and maps descriptor `0x142` through `(flags & 0xA7) | 0x40`, still `0x142`. On a rejected step, the scheduler clears each colliding local axis and applies `(((heading + 0x20) & 0xC0) - 0x40) & 0xFF`, a left turn from the nearest cardinal, then continues the effect. Separate handler `0x50020` must not be attributed directly to the public Retreat command: it derives an exact `current - stored target` heading, multiplies descriptor x/y deltas by `1.5`, and installs `0x112`; its supported-population reachability is resolved below. | Command/transition path `0x5744B`, `0x4E5F0`, `0x4F98D`, `0x4E76B`; mode-5 flag construction `0x4FDCD`-`0x4FE68`; scheduler branch `0x53C13`-`0x53C6C`; separate handler `0x50020`-`0x5010A`; object-2 double `0x7CEA`, independently decoded as `1.5`; hashed executable `5D723175...B79D6`. | Public kind-0 Retreat transition, preserved-heading 200 ms sub-cell movement, flag `0x40`, collision reset/turn formula, and separation from `0x50020` Confirmed. The earlier direct away-vector interpretation is Disproved. `OriginalCombatantTemplates.ActorKindForSceneTemplate`, imported actor kinds, `AdvanceRetainerMovement`, and focused cadence/collision tests implement the recovered kind-0 path. Exact acquisition visibility and later mode-5 formation transitions remain Provisional; the later dormant-handler row confirms `0x50020` has no supported entry route. This row supersedes the preceding row's older claim that the whole `0x40` actor family and Retreat remained Provisional. |
| Raycaster `0x470A8` is not a floor-plane unprojection. It derives fixed-point view vectors, asks `0x45158` to walk at most 64 wrapped map steps, transforms kind-4 offsets, projects candidate elevation bounds, tests pixels through `0x444E8`, and returns contact X/Y, vertical contact, ray depth, block index, and pixel coordinates. | Complete bounded disassembly of `0x470A8`-`0x47589`, traversal `0x45158`, candidate helper `0x44F7C`, rotation helpers `0x446BC`/`0x4472C`/`0x447C4`, and pixel test `0x444E8`; hashed executable `5D723175...B79D6`. | Candidate traversal, 64-step bound, block/pixel participation, and output contract Confirmed. Treating `0x470A8` as a floor intersection is Disproved. |
| Actor acquisition `0x4F98D` scans 68-byte actor records in authored order, skips inactive/self/same-side records, subtracts live fixed positions at `+0x0C/+0x10`, derives heading with `0x445C4`, and casts `0x470A8` through the centered viewport column. `0x4CEA0` must resolve the returned block to the exact candidate actor. Nearest depth starts at `0x7FFF`; only strictly smaller depth replaces it, and accepted depth below `0x154` ends the scan. Heading folds quadrants around `floor(0x20 * minor / major)` with local `(dx,dy)` mapped to original `(-dy,dx)`. Sine/cosine/rotation use the signed 1.15 quarter-wave table at object-2 `+0xC904`, rounding each product as `(product + 0x3FFF) >> 15`. | Disassembly of `0x4F8D0`-`0x4FB80`, `0x445B4`-`0x44840`, and `0x470A8`; relocation-backed object-2 table decode; hashed executable `5D723175...B79D6`; imported scenes and official texture pixels. | Actor iteration, side/self filters, heading, centered ray, identity test, quarter-wave values, rounding, strict nearest selection, and close exit are Confirmed. `OriginalSiegeProjection`, `OriginalSiegeActorAcquisition`, and `SiegeSession` implement the imported-scene path; tests cover headings, non-cardinal depth, alpha rejection, actor identity, ties, and the early exit. The older bounded-visibility clauses below are superseded. GameFAQs FAQ 66730 does not expose these internals and is not corroboration. |
| Candidate helper `0x44F7C` uses 32-slot arrays but stops when the post-increment count reaches `0x1F`, so the exact usable cap is 31. Traversal `0x45158` probes a changed corner as `(newX, oldY)`, `(oldX, newY)`, `(newX, newY)` and a single-axis entry before its two perpendicular neighbors. Behavior bit 0 decides continuation versus stop; eligible kind-4/behavior-bit-3 blocks follow state target `+0x40` in place. Switch table `0x34A0C` excludes kind 0, maps kinds 1/4 to a cell box, kinds 2/3 to center planes, and kinds 5/6 to diagonals. `0x470A8` consumes candidates in insertion order, projects `top = horizon - (upper - cameraElevation) * viewportWidth / depth` and `bottom = horizon + (cameraElevation - lower) * viewportWidth / depth`, then alpha-samples using block fields `+0x10..+0x20`. Kind 4 replaces box depth with the rotated live center's forward coordinate and computes horizontal texture position as `((rayLateral * depth) >> 14) - centerLateral + 0x80`; it selects `(((Surface3 - viewHeading + 0x80 / Surface2) & 0xFF) * Surface2) >> 8`, folding and mirroring the far half when behavior bit `0x04` is set. | Viewer setup `0x5421F`-`0x542E4`; intersection `0x44A28` and table `0x34A0C`; candidate helper `0x44F7C` including sentinel `0x45061`; traversal branches `0x4527D`-`0x4566D`; kind-4 transform `0x47308`-`0x47419`; raycaster `0x470A8`; alpha sampler `0x444E8`; rotation `0x447C4`; hashed executable `5D723175...B79D6`; owned scene-block census. | Shape mapping, exact cap, elevation formulas, raw geometry fields, alpha, wrapping, traversal/probe order, pass/stop behavior, state continuation, and kind-4 center/depth/sector/mirror formulas Confirmed. `OriginalSiegeProjection` implements them with focused tests and pointer picking preserves live identity. The former below-horizon floor plane and 32-usable interpretation are Disproved. Non-cardinal acquisition integration remains Corroborated. GameFAQs FAQ 66730 has no internal projection formula and is not corroboration. |
| The following older viewer/ground-ray row is superseded by the candidate row above. Viewer initialization stores elevation `0x80`, integer horizon `viewportHeight / 2`, and viewport width. For pointer x, `0x470A8` computes lateral basis `((0x400000 / viewportWidth) * (x - viewportWidth / 2)) >> 8` beside forward basis `0x4000`. Traversal `0x45158` normalizes the major component to signed `0x100`, scales the minor component by signed division, wraps original map lookup with `& 0x7F`, and stops after `0x40` iterations. | Viewer initialization `0x5421F`-`0x542E4`; pointer dispatcher `0x55524`; complete `0x470A8` and `0x45158` disassembly; hashed executable `5D723175...B79D6`. The GameFAQs FAQ contains no internal ray math and supplies no corroboration for these formulas. | Elevation, horizon, horizontal bases, normalization, original wrapping, and 64-step limit Confirmed. `OriginalSiegeProjection` now carries these formulas into ordered pointer candidates. The former speculative 60-degree floating plane and kind-0 ground return are Disproved. |

| Kind-1 Retreat continues beyond mode 4 into a ranged attack state. Kind-1 table mode 4 targets transition `0x4E6D3`, which records fallback mode 1 and success mode 11 after acquisition `0x4F98D`. Mode-11 handler `0x5010B` aims at target actor `+0x24`, bypasses a new raycast only at fixed Manhattan separation `<= 0x154`, otherwise calls `0x470A8`, requires the returned actor to be opposite-side, and compares ray distance strictly below raw combat-row contact column `0xCE24 + 28 * row`. Rows 23/24 double descriptor field `+0x14` when constructing the attack effect. | Raw decode of mode 4 in the relocated kind-1 table at object-1 `0x3E480`; disassembly of `0x4E6D3`, acquisition/current tables `0x4F414/0x4F458`, acquisition `0x4F98D`, and handler `0x5010B`-`0x50357`; object-2 combat table `0xCE14`; hashed executable `5D723175...B79D6`. | State route, close threshold, strict raw range, opposite-side check, and doubled ranged interval Confirmed. `ActorContactDistanceForCombatRow`, `RetreatOrderTarget`, and `RetainerRangedAttack` implement the route with focused range, obstruction, and attack tests. The runtime cell line trace is Corroborated pending exact `0x470A8` equivalence, and exact attack-effect completion cadence remains Provisional. This supersedes earlier rows that leave kind-1 mode 4 wholly Provisional. The GameFAQs guide does not address these internals. |
| Mode-11 damage occurs on the final live-effect tick, not when handler `0x5010B` constructs the effect. Constructor `0x4C7F4` stores the clock at effect `+0x10`; scheduler `0x5310D` uses the strict `now - deadline > interval` gate and preserves overrun. At `0x53365`, effect counter equal to count plus actor mode `+0x18 == 11` and target health `+0x40 > 0` gates call `0x4F070`. Completion then clears actor effect handle `+0x08` to `-1` and immediately re-enters thinker `0x4F49C` for that actor. | Disassembly of constructor `0x4C7F4`-`0x4C8FC`, scheduler `0x530D0`-`0x53D5F`, mode-11 handler `0x5010B`-`0x50357`, and thinker `0x4F49C`; decoded scene SFX counts/intervals; hashed executable `5D723175...B79D6`. | Strict completion damage, living-target/mode checks, doubled row-23/24 cadence, effect cleanup, and same-actor thinker re-entry Confirmed. `PendingRangedTarget`, `AdvanceEnemyAnimations`, `AdvanceRetainerOrder`, and focused tests implement deferred/repeated Attack and Retreat shots plus command cancellation. Monotonic elapsed time is the processor-independent compatibility clock. This supersedes the preceding row's Provisional cadence clause. GameFAQs contains no comparable timing detail. |
| Attack mode 6 continues as preserved-heading wandering when no visible target is acquired. Acquisition `0x4F98D` seeks the nearest visible opposite-side actor; kind-0 transition `0x4E71F` and kind-1 transition `0x4E745` both retain mode 6 on failure, while success selects modes 8 and 11. Mode-6 handler `0x4FDCD` clears target fields, preserves heading, reads movement selector `+0x46`, and maps the low flags through `(flags & 0xA7) | 0x40`, leaving official descriptor `0x142` unchanged. Scheduler collision clears the blocked local axis and turns left with `(((heading + 0x20) & 0xC0) - 0x40) & 0xFF` while the effect continues. | Acquisition/current tables `0x4F414/0x4F458`; disassembly of `0x4F98D`, transitions `0x4E71F/0x4E745`, handler `0x4FDCD`-`0x4FE68`, and scheduler branch `0x53C13`-`0x53C6C`; supported-scene descriptor census; hashed executable `5D723175...B79D6`. | Failure route, handler, flag rewrite, collision reset, and turn formula Confirmed. `AttackOrderTarget`, `MovementWanders`, and `AdvanceRetainerMovement` implement it with focused unobstructed/collision tests. The bounded cell-visibility bridge is Corroborated pending exact raycaster `0x470A8` equivalence. This supersedes older rows that leave no-target Attack/mode 6 Provisional. The GameFAQs guide contains no internal state or movement-flag detail. |

| The shared mode-9/10 handler `0x50020` is dormant for the complete supported official actor population. Mode-9/mode-10 success destinations for kinds 0-6 are `9/5`, `9/4`, `6/4`, `9/10`, `6/4`, `9/10`, and `9/10`; kind 7 shares kind 2. Failure/previous destinations are `6/2`, `6/2`, `1/3`, `1/5`, `1/3`, `1/2`, and `1/1`. Every mode-9 entry is therefore a self-loop. Initial template current/requested/previous triples are `4/4/4,4/4/4,4/4/4,6/8/6,4/10/6,4/8/1,1/4/2,4/10/1,6/8/4,4/8/4`; the supported placement census uses only templates 0,1,2,3,5,8,9, none initialized to mode 9 or 10. Templates 4 and 7 alone preload requested mode 10 and have zero supported placements. | Relocated kind tables at object-1 offsets `0x3E43C`, `0x3E480`, `0x3E4C4`, `0x3E500`, `0x3E53C`, `0x3E578`, and `0x3E5B4`; decoded transitions `0x4E6C0`, `0x4E758`, `0x4E76B`, `0x4E805`, `0x4E818`, `0x4E8D8`, `0x4E926`, `0x4E95F`, and `0x4E9A7`; initializer `0x542F8`; complete ignored owned-resource census of 1,002 placements. | Unreachability for the hashed executable and supported official asset population is Confirmed. `OriginalCombatantTemplates.ModeProfileForSceneTemplate` records the exact initializer state, and focused tests assert both all ten triples and the placed-template exclusion. The handler's `current - target` heading, 1.5 delta multiplier, and non-cardinal rotation remain documented original design, but deliberately have no runtime route. The GameFAQs FAQ has no internal state-machine description and neither corroborates nor conflicts with this result. |

| Multiple actors ordered to one mode-12 destination cannot stack or automatically fan out. Every one of the 144 placed actor bases and all 576 adjacent base/attack/hit/death blocks use behavior `0x87`, including movement blocker bit `0x02`. On crossing a cell, the scheduler restores the mover's saved underlying block to its old cell, captures the destination block, installs that captured block as the actor's new underlying state, and writes the actor block into the destination before processing another effect. One actor may occupy the shared coordinate; subsequent direct movers encounter bit 2 beyond the strict `-0x59..0x59` band, stop their `0x112` effect with retained offset, and retry. | Complete owned `--scene-blocks` census across 42 `MELEE*`/`DEFEND*` pairs; actor behavior/state population totals 144 bases, 576 state blocks, and 1,002 placements; scheduler collision and map-block swap at `0x53425`-`0x53694`; direct-effect cleanup/thinker return at `0x53CC9`-`0x53D5F`; hashed executable `5D723175...B79D6`. | Single-cell occupancy, no automatic fan-out, sequential map swap, blocked-follower retained offset, and persistent direct retry Confirmed. `OriginalCombatantTemplates.PlacedActorBehavior`, `SiegeSession.RetainerCanEnter`, and `SharedGroundDestinationAllowsOneOccupantAndBlocksFollowers` preserve the mapping. Exact ordering is resolved by the following processor-stability finding. GameFAQs contains no comparable low-level behavior. |

| The original does not provide a processor-stable identity for the actor that wins an exact simultaneous shared-destination tie. Ground-click dispatch at `0x55D0F`-`0x561DB` assigns the orders before the loop calls scheduler `0x530D0` and actor thinker `0x50524`. The thinker begins at persistent cursor `D5C4`, processes a computed actor budget, advances/wraps the cursor, and stores it again; effect constructor `0x4C7F4` scans slots zero through 63 and takes the first whose active field `+0x0C` is zero. The main loop returns at `0x584DF` without a wait, so the cursor position when input arrives depends on processor/display/input timing. | Focused executable disassembly of input dispatch `0x55D0F`-`0x561DB`, main-loop order `0x583AA`-`0x584DF`, actor thinker `0x50524`-`0x50578`, and live-effect allocation `0x4C7F4`-`0x4C816`; hashed executable `5D723175...B79D6`. | Cursor behavior, first-free allocation, and loop ordering Confirmed; a stable original tie winner is Disproved. Runtime authored retainer-list order (x-major for official imports) is an explicit clean-room compatibility policy, tested to be independent of update subdivision. This resolves the preceding row's exact-ordering question. GameFAQs has no low-level scheduling account and neither corroborates nor conflicts with the result. |

## Resource archive findings

| Finding | Evidence | Confidence |
| --- | --- | --- |
| `C1086.GOB` and all 99 scene `.RES`/`.LOW` files use the same indexed Dynamix container family. | Every file begins with `.RES`, followed by a little-endian directory offset whose target contains a bounded entry count; all 26,276 scene entries decode or copy through the common codec registry. | Confirmed for the hashed build |
| Directory records are 52 bytes: a 32-byte null-padded name followed by two 32-bit fields, stored size, expanded size, and data offset. | All 486 GOB records and representative 222-entry scene archives fit exactly within their files; every tested extent remains before the directory. | Confirmed structure; semantics of the first two fields remain provisional |
| An entry with equal stored and expanded sizes is stored byte-for-byte. | Representative entries have equal lengths and readable resource payloads; synthetic parser tests verify bounded extraction. | Corroborated pending broader format sampling |
| The first field distinguishes stored (`0`), block LZ/RLE (`1`), and adaptive LZW (`2`) entries. | The hashed GOB contains 10 equal-size kind-0 entries, 471 unequal-size kind-1 entries, and five unequal-size kind-2 entries; the two decoder branches were recovered from the executable and validated against their complete populations. | Confirmed for this build |
| All 26 `.666` resources are rate-tagged unsigned 8-bit mono PCM banks: magic `0x004A5031`, then repeated `UINT32LE` byte length, `UINT32LE` sample rate, and payload. | A bounded decoder consumes all 26 GOB/scene banks exactly; waveform centering distinguishes unsigned from signed PCM. The same 2,159-byte/11,025-Hz sample occurs identically in 17 interface banks. | Framing/rates/PCM Confirmed; shared activation role Corroborated; other event bindings Provisional |
| Every kind-1 payload is a sequence of `UINT16LE length` plus exactly that many bytes. | Bounded traversal of the 471 top-level GOB kind-1 entries consumes every stored byte exactly, producing 2,963 blocks with no zero or out-of-range lengths; the same framing validates all scene entries. | Confirmed framing for this build |
| Kind-1 blocks expand independently in 16,384-byte slices; marker `0x80` stores the slice verbatim and marker `0x40` selects an MSB-first controlled LZ/RLE token stream. | Every GOB and `.RES`/`.LOW` scene kind-1 entry has `ceil(expanded size / 16384)` blocks. All 32,089 blocks decode to their exact expected slice size; strict follow-on parsing identifies 188 PCX-compatible images. | Confirmed for the hashed release |
| Kind-1 LZ copies encode a 12-bit distance and a 3-18 byte length; distance zero instead encodes a 16-4111 byte repeated-value run. | The owned executable's decoder branches directly on these fields; an independently written bounded decoder accepts the complete GOB/scene kind-1 population. | Confirmed for the hashed release |
| Kind 2 is a continuous MSB-first LZW stream with clear `256`, end `257`, first dictionary code `258`, and adaptive widths from 9 through 14 bits. | Static code at LE virtual addresses `0x45C6E`–`0x45E04` establishes the controls and width ceiling; an independent bounded implementation expands all five entries exactly, and all four PCX results validate strictly as 640x480 images. | Confirmed for the hashed release |
| Kind-1 outer blocks use the same raw LZW stream as documented Dynamix inner chunks. | A bounded raw-LZW probe rejects the real streams at their first codes. | Disproved; do not apply the inner-chunk decoder to outer blocks |
| Kind-1 outer blocks are independently reset 16 KiB classic LH1/LZHUF streams. | A bounded probe under both bit orders produced no valid header among all 187 kind-1 `.PCX`/`.PCC` entries. | Disproved; block count and compression ratio alone do not identify the codec |
| `richard.pcc` contains a standard single-plane 8-bit RLE PCX image with a 256-color trailer palette. | The byte-stored entry passes strict header, scanline, RLE, palette, and exact-consumption validation and decodes to 195×203 pixels with index hash `bee27150…fcd36`. | Confirmed for this resource |
| `engmap1.pcx` is a kind-1 compressed 640x480 PCX and depicts the labeled parchment map of England used by the strategic screen. | Exact decoding, strict PCX validation, and local visual inspection of the owned image. | Confirmed payload and dimensions; runtime role Corroborated |
| `FLUFF.PCX` is the one-time campaign briefing shown after character preparation and before estate play. | Direct visual match to the owner-supplied 640x480 briefing capture, including baked parchment text and decoration; normalized screenshot RMSE 0.07360. `MESSAGE.PCX` is a visibly distinct blank writing screen. | Confirmed resource association and new-campaign sequence |
| `VINN.HAT` screen 12 names `INNPEOPL.PCX`; regions 0-9 bind Frederick, Gerard, Barkeep, Otto, Hugh, Gilbert, Nellie, Richard, Ivo, and Albert in that order. Region 10 is Exit and overlapping region 11 is the bottom hover-label surface. The matching named PCC portraits are all 195×203 except Nellie's 194×202 image. `INN.CSF` contains two small face patches whose event semantics remain unknown. | Direct visual match to the owner-supplied inn capture; exact descriptor decode; contiguous executable labels at file offsets `0xD1FA8`-`0xD1FFB` followed by `INN.CSF`; matching named PCC resources; and palette-assisted CSF rendering. | Background, ordinal patron identities, portrait bindings, exit, and footer geometry Confirmed; dialogue records and animation timing Provisional |
| `ALL.CIF` indexes 1,311 unique conversation nodes in `ALL.CBF`. Each node has a fixed `0x348`-byte header, byte `0x08` exactly counts prompt variants, byte `0x48` counts up to five responses, and targets begin at `0x4C`. Zero-response nodes use the first target slot as an automatic continuation: 55 link onward and 8 terminate; all nonzero response and continuation targets resolve. | Complete bounded decode of every indexed record, 2,062 prompt variants, 2,696 response records, and executable interpreter at `0x17498`-`0x174FE`. | Record framing, visible-string roles, counts, targets, continuations, and complete graph integrity Confirmed; remaining conditions and mutations Provisional |
| The ten populated-inn patrons enter through selector roots `1900,1100,3200,3500,1600,1400,3000,1698,3300,3600` in screen order. Their continuations resolve respectively to named Frederick, Gerard, Bartender, Otto, Hugh, Gilbert, Nellie, Richard, Ivo, and Albert portrait nodes. | Metadata-only probe of each root/target pair against decoded `ALL.CIF`/`ALL.CBF`; portraits and speakers match the executable-ordered `VINN.HAT` hotspot catalog. | Confirmed |
| Conversation node `3201` is the blacksmith prompt with `BLACKSMI.PCC`; root `3200` is instead the populated-inn bartender selector whose normal continuation is bartender node `3298`. | Exact decoded node speakers, portraits, prompts, continuations, and the `VINN.HAT` Barkeep association. | Confirmed |
| Generic parish selector root `3100` invokes action group `3102`, whose sole operation redirects to priest node `3101`. That node offers an offering, joust blessing, fief blessing, information, or exit. Selector `3149` instead runs group `3101` and enters Father Hyacinth's dragon-proof-armor chain, whose own text places the armor and Duchess Valletta in Cambridgeshire; Cambridge therefore uses `3149`. Six structurally parallel regional priest roots `5000`-`5500` use `PRIEST2.PCC` through `PRIEST7.PCC`. | Complete decoded conversation graph and action groups `3101`/`3102`; the `3149` branch text repeatedly identifies Cambridgeshire; matching repeated prompt/response structures at each regional root. | Generic and Cambridge roots Confirmed; remaining regional location association incomplete |
| Conversation prompt variants are selected randomly when a node declares more than one; all declared responses are rendered in five possible 558x35 rows beginning at `(41,302)`, with mouse activation and number keys 1-5. Zero-response spoken nodes remain visible for 2,000 ms before continuing. | Interpreter paths `0x176BA`-`0x176E7`, `0x17E74`-`0x17FBA`, `0x17E3C`-`0x17E5B`, and the `0x7D0` delay passed at `0x174BC`. | Confirmed |
| Conversation headers carry 30 action-ID slots per node and per each of five possible responses. The interpreter exhausts the applicable node and response slots before consuming the last redirect written by function 3. The complete database contains 423 node and 2,318 response action references; every inn selector root invokes one action before its default continuation. `ALL.TMI` contains an unknown leading dword and 689 unique ID/offset records; the bounded `ALL.TMB` graph decoder validates 2,267 actions, 10,373 expressions, and 13,352 typed values. All distinct conversation action IDs resolve except original-data reference `5011`. | Header-copy routine `0x13BDC`; the two unconditional executor loops at `0x17507`-`0x17577`, followed by the redirect read at `0x17579`; executor/evaluator paths `0x67F38`-`0x687E4`; and complete owned-data graph traversal with metadata-only `action-tree-report.txt`. | Slot order, redirect timing, recursive grammar, population, and anomaly Confirmed; semantic bindings detailed below |
| Action function IDs 3-9 respectively redirect dialogue, assign a variable/attribute, add to one, read one, increment an item counter, clear one, and test possession. The relocated operator table maps codes 1-8 to `!=`, `<=`, `>=`, AND, OR, `>`, `<`, and `==`; only codes 3-8 occur in the reachable data. `ALL.VTB` initializes 190 signed 32-bit script variables to zero. | Dispatcher `0x1FB04`, handlers `0x17698` and `0x1F82C`-`0x1FAA4`, operator evaluator `0x68302` with relocation sources `0x6ABBC`-`0x6ABD8`, bounded full-data reports, and the exact 772-byte VTB shape. | Function/operator semantics and script-variable initialization Confirmed |
| Scope-1 action selectors `0`, `2`, `3`, `5`, `6`, `7`, and `8` address wealth, honor, fame, piety, strength, stamina, and intelligence. Produced/consumed item selectors `0`-`23` cover the Holy Chalice, tournament gifts, dragon quest objects, the Orchid, armor title, Book of Hours, and Gilbert's note; multiple generic dagger, sword, and hammer selectors intentionally share visible names while retaining distinct counters. A lone `161` possession test has no producer and occurs only in Victoria's final shield-offer gate. | Selector-to-character-index switch at `0x1F86F` and clamped character accessor at `0x13448`/`0x13464`; complete metadata action trace; selected owner-local dialogue correlation; contiguous executable item-name catalog at file offsets `0xD02B4`-`0xD03AF`. | Selectors and item IDs `0`-`23` Confirmed; `161` identity remains Provisional and is preserved raw |
| Anna Lisa's response that leads directly to node `24261`, where she discloses the lair in northwestern Wales, invokes action group `2447`; its sole operation increments global variable `2`. Action group `3202` separately tests variable `2 == 1` and redirects, while group `3203` provides the only other writer by assigning it to 1. Tournament selector roots are `2200` Adela, `2900` Jane, `2400` Anna Lisa, `2100` Victoria, `2000` Wendessa, and `2500` Valletta. Global variable `42` stores current lady colors as 1 Wendessa, 2 Victoria, 3 Anna Lisa, 4 Valletta, or 5 Jane; variable `3` distinguishes the latest joust outcome in each lady selector. | Exact conversation targets/text plus complete action-tree writer/reader audit in `conversation-text-report.txt` and `action-group-report.txt`; each root resolves to the matching named portrait. | Lair discovery variable, lady roots, colors selector, and joust-result bridge Confirmed for the hashed release; exact external call sites of groups `3202`/`3203` remain Provisional |
| Sir Frederick's repeated raids on the lair while the dragon is away are backstory, not a player plunder loop. The player-facing flow enters the dragon encounter on arrival at the discovered lair. | Anna Lisa and Frederick's decoded branches describe only Frederick taking treasure; no conversation action awards the player lair treasure. The owned executable contains the approach movie name `TRANDRAG.SMK` and the three encounter outcomes, while the surviving detailed playthrough says entering the lair cuts directly to the dragon joust. | Narrative-only absent-dragon raids Corroborated; direct arrival-to-encounter flow Corroborated; exact executable call graph remains incomplete |
| Drogo arrives to collect the exact outstanding shilling debt. The player is asked whether to pay; inability or refusal leads to combat. Killing Drogo permanently ends moneylender harassment, while losing is fatal. `DROGO.PCC` is his 195x203 portrait. | Six contiguous outcome/prompt strings at executable file offsets `0xC9293`-`0xC948C`; strict decoded portrait dimensions and index hash in `stored-image-report.txt`. | Trigger choices and consequences Confirmed; encounter layout, enemy strength, and annual recurrence Provisional |
| Defeating William and usurping the throne plays `CROWNL30.SMK`. Defeating the dragon first plays its combat victory and then `CHAMPL30.SMK`, whose accompanying text invests the player as King's Champion. | The William-victory text and movie name are contiguous at executable file offsets `0xCE455`-`0xCE50C`; the dragon victory, `CHAMPL30.SMK`, and investiture text are contiguous at `0xC976C`-`0xC987D`. Five evenly spaced decoded frames identify `CROWNL30.SMK` as the throne ceremony. | Crown ceremony and dragon investiture sequence Confirmed for the hashed release |
| A campaign that reaches its age limit without either victory uses `AVG_END.SMK`, distinct from fatal Drogo and dragon outcomes. | The movie name and the text “The faiths have been unkind” / “I am not the dragon slayer nor the conqueror” are contiguous at executable file offsets `0xCCE0B`-`0xCCE88`. | Non-victory age-limit sequence Confirmed for the hashed release |
| Original text controls expose an explicit `WORDWRAP` property, and conversation prompt/response controls are assigned bounded widths and heights. | `WORDWRAP` printable identifier at executable file offset `0xD2498`; conversation control construction at `0x17E74`-`0x180D3`. | Word-boundary behavior Corroborated; exact original proportional-font metrics remain incomplete |
| `fftitle.pcx` is a kind-1 compressed 640x480 PCX depicting the Conqueror A.D. 1086 title composition. | Exact decoding, strict PCX validation, and local visual inspection of the owned image. | Confirmed payload and dimensions; runtime role Corroborated |
| Byte-stored `.CSF` resources begin with magic `0x4A32`, a 32-bit frame count, and one 32-bit size per frame; concatenated frames consume the remainder exactly. | All five stored CSFs validate: counts are 723, 32, 337, 337, and 337, with each size-table sum exactly matching its remaining bytes. | Confirmed for these resources |
| CSF frames contain width/height followed by per-row literal, transparent-skip, and repeated-color segments. | Every one of 1,766 frames decodes exactly to its declared dimensions and consumes its complete chunk; only operation values 0, 1, and 2 occur. | Confirmed for these resources |
| Stored CSFs contain 119,292 literal, 254,433 transparent-skip, and 19,573 fill segments. | Reproducible `csf-report.txt` totals emitted by the bounded decoder. | Confirmed for the hashed release |
| Five stored scene `.PAL` entries are raw 256-color RGB tables with 8-bit components. | Each is exactly 768 bytes; observed maxima are 252–255 and stable hashes are emitted by `stored-palette-report.txt`. | Confirmed for these resources; screen association remains Provisional |
| Scene texture entries are headerless byte planes whose names encode `TEXnnn width height`. | Every one of 12,982 `TEX` entries across all 99 `.RES`/`.LOW` containers has a decoded length exactly equal to the encoded width times height; the reproducible report records counts and distinct dimensions without exporting pixels. | Naming, dimensions, and one byte per pixel Confirmed for the hashed release; palette, ordering, and surface mapping Provisional |
| First-person scene archives contain a fixed 128x128 column-major map, Viewer start/heading, 96-byte named block records, a wrapping BackImage, and 128 canonical `Pal0`-`Pal127` index-remap tables. Non-billboard offsets 44/48/52/56 are the north/east/south/west texture references; kind-4 billboards use only offset 44 as an image reference. | Complete bounded validation of 12,784 block records, including 4,468 kind-4 records, all map references and remaps; executable intersection masks at `0x44CC0`-`0x44F61` and the corresponding field selector at `0x46739`-`0x467F7`; coherent local renders and byte-exact active-family regeneration under `SKIRMISH.PAL`. | Structure, counts, map order, field distinction, cardinal surface order, remap framing, and palette association Confirmed |
| Door definitions appear in adjacent same-name visual families, but they are not a timed closed/intermediate/open animation. A placed door is actionable only when its behavior bitmask contains `0x10`; the action path replaces the map cell immediately with the record-offset-`0x40` target. Placed knight/champion/footman records separately use adjacent base/attack/hit/death templates whose texture populations depict walk/view, strike, impact, and collapse imagery, but the executable selects an angular surface within the active template rather than playing each population sequentially. | Population comparison and contact sheets; action dispatch `0x5568D`-`0x556E9` and replacement routine `0x4C900`; actor state copier `0x4E39C` and billboard renderer `0x46E3B`-`0x46F23`. | Immediate explicit door transition and actor state/run structure Confirmed; a timed adjacent-door sequence and sequential actor playback Disproved |
| Nested `SKIRMISH.RES/skirmish.csf` contains 53 frames: 24 weapon-item views, three shields, five foreground weapon groups at frames 27-41, a dagger frame at 42, and large and small five-frame blood-effect groups at 43-47 and 48-52. The executable builds a weapon mask at `0x588BB`-`0x58AC6`, permutes store item IDs 0-22 into combat rows 0-22 at `0x4CB20`-`0x4CCB0`, and maps item IDs 43 and 44 directly to rows 23 and 24. The fixed-width row-name table at file offsets `0xD6340` onward names those final rows Light Crossbow and Heavy Crossbow respectively. Each 28-byte combat row begins at data offset `0xCE14`: the store rows confirm foreground bases 27/33/36/39/41 for axes, hammers, maces, swords, and daggers despite their non-catalog order, while both crossbow rows use base 30. On a missed contact, `0x551BF`-`0x55222` skips breakage for combat row zero and otherwise breaks the weapon only when a random roll in `0..(200 + 50 × row field 2 - 1)` is zero; both crossbow rows have rating 6 and therefore a 1-in-500 roll. Successful contact selects the large blood group for a fatal target hit and the small group otherwise, rendering the first four frames of either group. | Bounded nested-CSF decode with exact chunk consumption and local contact-sheet inspection under the archive's raw 768-byte `SKIRMISH.PAL`; executable item-mask, row-selection, fixed-width row-name, foreground-table, miss/break, hit dispatch, and effect-rendering traces. | Frame count, dimensions, grouping, visible identities, item-to-row mapping including both crossbows, foreground bases, per-weapon break rolls, fatal/nonfatal dispatch, and rendered extent Confirmed; runtime attack cadence Provisional |
| First-person melee foregrounds start with stored offset 2, select offset 1 when horizontal target separation is below one quarter of the viewport width, and return through offset 0. Crossbow rows 23 and 24 use base frame 30, always render offset 2/frame 32, start at the viewport bottom, rise exactly one frame height to `viewport bottom - frame height`, and then remain there. Their zero horizontal velocity and completed vertical displacement never enter the melee return or out-of-bounds teardown branches; replacement foreground setup or siege teardown ends the held image. Contact reverses horizontal velocity and reverses/halves vertical velocity only for melee rows. | Setup at `0x54E9D`-`0x54F12`, velocity/reach branches at `0x54F98`-`0x550C3`, contact branches at `0x550F2`-`0x55140`, and pose/teardown branches at `0x5529B`-`0x55429`. | Crossbow rise, held lifetime, frame offset, melee pose selection, threshold, contact reversal, and mirror anchoring Confirmed and mapped to `SiegeForegroundTrajectory`; a timed one-frame crossbow flash is Disproved |
| The 25 combat rows at data-object offset `0xCE14` store fixed-point contact distance in column 4 (`0xCE24 + 28 * row`). The player-contact branches at `0x558D4` and `0x559CA` and actor-versus-actor branch at `0x4F10D`-`0x4F125` add `0x40` before their distance comparisons. Melee row values range from 350 to 450; Light and Heavy Crossbow use 7,000 and 8,192. Runtime player and imported-enemy reach now derive from these exact row values instead of item power; enemy lanes are blocked by intervening scene geometry. | Bounded numeric `--weapon-combat-table` report, the executable-confirmed item-to-row permutation, and actor/non-actor contact branches. | Table values, field use, and runtime binding Confirmed; fixed-point-to-grid conversion and sub-cell collision geometry remain Provisional |
| Combat-row columns 0-2 are dice count, die sides, and armor penetration. Routine `0x4CE4C` sums `random(sides) + 1` once per die; `0x4CE74` subtracts `max(target armor - penetration, 0)` and clamps damage at zero. Routine `0x4EA04` reads armor and health at combatant offsets `0x3C` and `0x40`, then subtracts that result. The ten initialized 68-byte combatant templates have attack-skill/armor/health triples `50/7/12`, `70/8/15`, `60/5/10`, `50/6/10`, `50/6/10`, `50/6/10`, `50/6/10`, `50/5/10`, `70/8/15`, and `85/10/20`. The player attack skill is `strength + dexterity + 2 × sword experience`; maximum health is `strength + stamina + honor`. Both are assigned to their corresponding combatant fields at `0x58851`-`0x58887`. At `0x4F2AC`-`0x4F2D6`, a strike hits when `random(200) < 100 + attacker skill - defender skill / 2`, with 30-point bonuses for a crossbow attacker within one map cell of the player and for an attacker facing the same direction as the defender. A placed actor selects its template through the signed word at scene-block offset `0x4A` and its combat row through the following word at `0x4C`; all 1,002 placed actor instances in the owned combat scenes use bounded rows `4`-`23`. Imported siege actors now retain those exact skills, defenses, and attack rows, so strikes in either direction use the recovered hit and damage calculations. | `CHARACTR.DAT` attribute order; combat-table numeric report; focused executable traces at `0x4CE4C`, `0x4CE74`, `0x4EA04`, `0x4F234`-`0x4F2D6`, `0x517F5`-`0x5184E`, the template initializer beginning at `0x542F8`, and player initialization at `0x58818`-`0x58887`; plus a population-wide scene-block report. | Player attack skill and health, hit threshold, positional bonuses, damage calculation, template combat fields, actor selectors, and runtime binding Confirmed for the hashed release; cadence and grid abstraction remain Provisional |
| `SKIRMISH.RES/SKIRMISH.PCX` is a headerless 64,000-byte 320x200 indexed screen paired with the raw 768-byte `SKIRMISH.PAL`. Its stone interface contains a black combat aperture at X 26-192/Y 24-140, message/status panels, four visibly labeled command buttons, a health strip, and a lower-right dynamic well. | Exact byte count, exhaustive zero-index run measurement, palette application, and local visual inspection of the decoded owned resource. | Dimensions, row-major indices, palette association, aperture, and visible static roles Confirmed; current dynamic field placement Corroborated; exact control dispatch Provisional |
| `Pal32` begins an identity-to-black 32-step remap family ending at `Pal63`; the other identity/near-identity starts occur at `Pal0`, `Pal64`, and `Pal96`. Runtime surface shading regenerates the first Scenario-selected family under `SKIRMISH.PAL`, then selects `clamp((depth8_8 >> (DistanceShift - 2)) - blockOffset, 0, MapCount - 1)`. All owned Scenarios enable 32 maps; melee uses shift 10 and blend target 20, selecting `Pal0`-`Pal31` by cell distance before the block offset. | Decoded Scenario offsets 40/44/48/52 and block offset 12 across the 90 Scenario-bearing archives among 99 scene containers; executable generation at `0x47914`-`0x47A84`, loading at `0x508E9`-`0x5093E`/`0x5117B`-`0x51232`, and repeated render selection paths. Every stored active family matches regenerated output byte-for-byte under `SKIRMISH.PAL` for all four owned blend targets. | Family boundaries, palette association, serialized parameters, generation, and selection Confirmed for the hashed release |
| This DOS/16M-bound executable's LE enumerated page offset is relative to the embedded MZ module, not the LE header. The module begins at raw `0x26654`, its LE header is at relative `0x2AA8`/raw `0x290FC`, and its data pages begin at raw `0x4C254`. | Correct mapping yields coherent entry-point disassembly and makes all 14 fixups targeting object 2 offset `0xADB8` correlate exactly. It places `Pal%d` at object 2 offset `0x7D28`, referenced by the two recovered loading paths. | Confirmed for the hashed release; inspector mapping corrected |
| The scene population contains three five-member families, `MELEE00`-`MELEE04`, `MELEE10`-`MELEE14`, and `MELEE20`-`MELEE24`. Each family shares its architectural texture/backdrop population while its five members vary Viewer start, map, block state, and actor placement. Unsuffixed `MELEE0`, `MELEE1`, and `MELEE2` are separate base scenes. The executable's two-digit `MELEE` name builder at `0x5CDE3`-`0x5CE1A` is reached from tournament melee. The practice callback at `0x42014` rolls inclusively from 0 through 2 at `0x4204E`, appends that digit to `MELEE`, and loads the corresponding base scene. Separately, initialization `0x21DFC` registers trigger dispatcher `0x21EEC` through `0x59C24` in engine callback slot `+0xB0`; the sole indirect invocation of that slot is at `0x59BC7`. The dispatcher acknowledges slot 128 and loads literal `MELEE0.RES` at `0x22226`; there is no other executable `MELEE` filename stem. | Exact resource cardinality and ordering; hashes and structural decoding distinguish every member while grouping their shared art; executable filename references/builders, inclusive RNG helper at `0x24C38`, adjacent tournament/practice flows, and complete direct-call plus relocation inventory of the scene loader and its wrapper. | Resource structure, practice three-way selection, and common campaign `MELEE0` callback chain Confirmed; tournament family selection Corroborated |
| In placed `MELEE*`/`DEFEND*` map records, the field previously described as an ordinal “behavior” is a bitmask: value 135 identifies combatants through its flag combination, 83 marks the observed `exit`/`gate` boundaries, kind-4 value 19 marks pickups, and door definitions use values including 1, 3, 17, 18, and 19. The action dispatcher at `0x55524` raycasts the selected record; its block-bit-`0x10` branch at `0x556A5` accepts distances below `0x280`, calls `0x51E60`, and immediately reaches state replacement at `0x55A49`. The earlier callback interpretation was wrong: `0x51F1E` reads the low word at selected-block offset `0x48` and uses it as the selector into the 20-entry table at object-1 offset `0x41E10`. The field loaded from offset `+4` of the active player's 68-byte combatant record at `0x556B9`-`0x556D0` is passed as a recipient discriminator within those cases. Since a scene cell is `0x100` fixed-point units, the reimplementation exposes bit-`0x10` pickups through explicit interaction up to two unobstructed cells away rather than collecting them on movement. | Bounded population report over the owned scene archives, including placement counts, block kinds, behavior masks, names, and words at `0x48`/`0x4A`/`0x4C`; focused executable disassembly at `0x51E60`-`0x52DDE` and `0x55524`-`0x55744`; relocated jump-table recovery at object-1 offset `0x41E10`; cross-check against the 68-byte combatant stride. | Behavior-as-bitmask, explicit pickup dispatch/range, interaction-selector source, recipient discriminator, and immediate replacement Confirmed; the former behavior-19 sound-only-pickup interpretation Disproved |
| The only placed actionable kind-4 reward families are 36 bags of coins, 70 meals, 36 chain hauberks, and 36 piles of bolts. Their block selector/argument tuples are `(5,25,0)`, `(7,2,6)`, `(9,6,0)`, and `(10,12,0)`. Case 5 adds argument 25 to wealth global `0xD4A4`; case 7 calls the shared dice routine for `2d6` healing and caps player health at `0xD4AC`; case 9 sets equipment bit 6 in `0xD4C4` unless already owned; case 10 adds 12 to ammunition global `0xD4A8`. The generic caller replaces the pickup with its offset-`0x40` target after the callback, including when health is already full or equipment already owned. | Case bodies `0x52509`-`0x525DD`, `0x5269F`-`0x52803`, `0x52929`-`0x52A70`, and `0x52BFC`-`0x52CC7`; dice routine `0x4CE4C`; crossbow ammunition reads/decrement at `0x54EED`-`0x54F64`; population-wide placed-block census. | Pickup identity, counts, arguments, resource mutations, `2d6` formula, caps/duplicate handling, and runtime mapping through `DynamixSceneBlock`, `ImportedSiegeLayouts`, and `SiegeSession` Confirmed for the hashed release |
| A non-player death subtracts damage from combatant health at `+0x40`, replaces the map cell from the actor block's state target at `+0x40`, copies adjacent actor state `base + 3`, and starts its effect gate. After that gate completes, `0x4F49C` removes the actor and sets victory when `0x4D8C0` reports zero living hostile combatants; `0x4D8C0` counts records after the ten template slots whose faction/side field `+0x30` is nonzero and whose health is positive. The alternate completion test accepts death-state effect-definition index 11, although every adjacent death state for all 144 placed actor bases in the 42 owned `MELEE*`/`DEFEND*` archives uses effect 0. Neither the damage/death path nor the completion path reads or writes wealth `0xD4A4`, ammunition `0xD4A8`, or equipment `0xD4C4`; their only battle-time mutation sites are the authored pickup/crossbow dispatches already mapped. Therefore defeated enemies do not create loot in the owned release. | Damage/death routine `0x4EA40`-`0x4ECD8`; state copier `0x4E39C`-`0x4E418`; completion/removal/victory logic `0x4F49C`-`0x4F5CE`; hostile counter `0x4D8C0`-`0x4D905`; full executable data-reference inventories for `0xD4A4`, `0xD4A8`, `0xD4C4`, and `0xD4CC`; 144-actor death-state census from the owned combat archives. | Death transition, victory criterion, and absence of defeated-enemy rewards Confirmed for the hashed release; `SiegeSession` already removes enemies only after their imported death gate and has no enemy-drop path, with `ResourceAndDefinitionTests.EnemyRewards` locking the absence of reward mutations |
| Placed kind-4 records carrying behavior bit `0x20` (observed masks 33 and 35) are weapon-contact objects. The unique executable bit test at `0x55A02` follows the combat-row range check and calls `0x4C900`, which replaces the contacted map cell with the block index stored at record offset `0x40`. The same immediate replacement is reached for bit-`0x10` explicit actions. Across the 42 owned `MELEE*`/`DEFEND*` combat archives, 1,086 placed door cells carry bit `0x10`: their targets are 664 carpet, 336 grass, 20 flagstones, 14 floor, 12 ground, 26 wall, and 14 water cells. Another 632 placed door cells lack the action bit and do not enter this branch. The runtime now retains every imported door as a stateful scene object, advances actionable doors directly to their exact target block and visual, keeps non-actionable doors closed, and preserves wall/water targets as blocked. Placed barrels, stools, pots, candelabras, and trees likewise use their explicit targets; adjacent damaged definitions are alternate/static records rather than an implicit durability chain. | Whole-code flag-use inventory; focused disassembly at `0x55524`-`0x55744`, `0x559CA`-`0x55A4E`, and `0x4C900`-`0x4CB16`; population-wide offset-`0x40` audit over all 12,784 owned combat-scene blocks; synthetic passable, non-actionable, and blocking door fixtures in `ResourceAndDefinitionTests.Doors`. | Weapon/contact triggers, immediate state replacement, door action gating and target distribution, explicit target field, and placed pickup rewards Confirmed; the former 360 ms adjacent-door animation Disproved |
| Scene animation is scheduled through at most 64 records of `0x40` bytes. Scene load `0x50AAC`-`0x50ACC` reads Scenario offset `0x1C` records of that size from literal resource `SFXDEFS`. Descriptor offsets `+0x0C`, `+0x14`, `+0x18`, and `+0x28` supply tick count, millisecond interval, flags, and a map-block selector. Constructor `0x4C7F4` maps `+0x1C/+0x20` to map-coordinate deltas, `+0x2C/+0x30` to per-tick/loop block indices, `+0x34/+0x38` to per-tick/terminal surface indices, and `+0x3C` to heading delta. The high word at scene-block offset `0x2C` (signed word `+0x2E`) selects the descriptor in actor/state constructor paths, with rows 23-24 doubling the interval. The nominal completion formula is `tick count * interval`; strict deadline comparison delays an equal-time update until the next scheduler call. Actor transitions copy the adjacent `+1` attack, `+2` hit, or `+3` death block through `0x4E39C`, attach its descriptor, and return to actor logic through `0x53D3F`/`0x4F49C` when the gate completes. Every `MELEE0.RES` actor state selects descriptor 0: two ticks at 192 ms, flags 5, with a `+1` block-index step and no coordinate, surface, or heading step, giving a 384 ms gate; attack rows 23-24 double that gate to 768 ms. These are state-completion deadlines, not attack-frame intervals. Renderer `0x46E3B`-`0x46F23` computes `sector = ((((relative heading + 0x80 / divisions) & 0xff) * divisions) >> 8)`, mirrors when behavior bit 2 is set and the sector exceeds half the divisions, and selects `base surface + sector` from the copied state block. Across all 144 placed actor bases in 42 owned combat scenes, the adjacent base/attack/hit/death blocks consistently use angular divisions `8/4/4/2`; every state selects descriptor 0 and has the same 384 or 400 ms nominal gate. The siege loop caps its movement delta at 166 ms (`0x55D1C`-`0x55D41`) but calls scheduler `0x530D0` before actor thinking and rendering (`0x583AA`-`0x583F0`). Pointer input at `0x55DB8`-`0x55DE5` adds `(9,9)` to the stored cursor top-left and passes the centered point to the action raycaster; `0x5590C`-`0x5591D` forwards that pair unchanged to foreground setup. `0x54C07`-`0x54F12` divides combat rows into 0-3, 4-14, 15-22, and 23-24, queries a family-specific CSF frame size, applies random x/y spans `8/16`, `40/40`, `32/24`, or none, clamps target y to viewport bottom minus sprite height, derives vertical or half-viewport horizontal outer anchors plus the fixed 56-pixel high-row offset, and mirrors from target side. Rows 23-24 instead center base frame 30 on pointer x, move it from viewport bottom to bottom minus its height, always render offset 2/frame 32, and remain held because they enter neither melee return nor offscreen teardown. `0x54F98`-`0x54FE5` calculates both signed 8.8 velocities as `(target - outer) << 9 / combat-row-column-3`; values span 380-1,000. `0x54FEC`-`0x5501E` integrates elapsed milliseconds, `0x550F2`-`0x55140` reverses x and reverses/halves y after melee contact, and `0x5529B`-`0x55448` selects approach/contact/return poses and mirrored anchoring from position. Blood increments at `0x552FB`-`0x55395` once per foreground call and stops after four frames. The main loop calls the foreground routine exactly once at `0x5841F`, performs framebuffer copies through `0x58473`, and jumps unconditionally back at `0x584DF`; copy routine `0x5D758` contains no timing gate, and the explicit wait routine at `0x657C0` is absent from this loop. The original effect duration is therefore processor/display-path dependent and has no stable cadence to recover. | Focused executable disassembly of scene load `0x50794`-`0x50B07`, state copier `0x4E39C`-`0x4E418`, actor transitions `0x4EC06`-`0x4ECD8` and `0x502E3`-`0x504AD`, record construction `0x4C7F4`-`0x4C8EC`, scheduler `0x530D0`-`0x53D64`, billboard renderer `0x46E3B`-`0x46F23`, siege loop `0x55D05`-`0x584DF`, framebuffer copy `0x5D758`, explicit wait routine `0x657C0`, actor thinker `0x50524`-`0x50578`, foreground setup/update/render `0x54B68`-`0x55521`, object-2 strings at `0x7D64`, owned `MELEE0.RES`, all 99 owned scene archives plus the 144-actor/42-scene state census, and synthetic family/subdivision fixtures in `ResourceAndDefinitionTests.ForegroundTrajectory.cs` and `ResourceAndDefinitionTests.HitEffect.cs`. | Resource name/count/record size, descriptor mutations, block selector, actor transition binding, MELEE0 completion gate, ranged doubling, live scheduler layout/order, millisecond clocking, deadline behavior, actor-thinker separation, row-family foreground geometry, random spans, velocity/reversal formulas, pose/mirror selection, and four render-tick blood effects Confirmed. The runtime's melee trajectory is active with retained fixed-point remainder and bounded updates. `SiegeHitEffect` preserves the exact four-frame sequence through monotonic 70 ms compatibility steps, independently of render/update frequency; 70 ms is explicitly a clean-room policy, not an original formula. Pointer-to-foreground provenance and crossbow lifetime are Confirmed and active in `SiegeForegroundTrajectory`; exact click-selected world-object targeting remains Provisional; the existence of a processor-independent original blood cadence is Disproved; sequential adjacent-texture playback, universal actor timing, fixed 70 ms melee frames, and texture-count-as-frame-count are Disproved. |
| All 2,131 movies use the `SMK2` container and their frame tables, tree extents, and 182,360 aligned frame extents consume exactly 288,867,980 bytes. | Population-wide bounded parsing emitted by `smacker-report.txt`; 2,095 files declare one packed 8-bit mono track and 36 are silent. All 2,135 palette changes demultiplex safely, all 161,884 audio packets decode to 398,368,812 declared unsigned PCM bytes, and all frames decode through the four adaptive trees. Local `TITLE.SMK` and `TRANDRAG.SMK` frame renders are visually coherent. The runtime binds the title, credits, item previews, dragon-lair approach `TRANDRAG.SMK`, and the `DRJSTWIN.SMK`, `DRJSTLSE.SMK`, and `DRJSTRUN.SMK` encounter outcomes through the file-backed direct MonoGame adapter. | Container, palette, packet, packed-audio, and indexed-video decoding Confirmed for the hashed release; named dragon outcome family Corroborated; remaining event bindings and seeking incomplete |
| `lance1.CSF` contains 25 transparent foreground frames ranging from 198x182 to 416x398 pixels, while `DRJSTWIN.PCX` is a 640x480 indexed screen whose palette renders the lance coherently. Original dialogue says the dragon breathes fire, the knight has one chance to strike, and should aim for its eye. | Population-wide bounded CSF report, local 25-frame contact sheet rendered with the decoded `DRJSTWIN.PCX` palette, and decoded conversation text. | Asset identity, transparency, palette pairing, one-thrust rule, and eye target Corroborated; exact target path, timing, hit window, and frame anchors Provisional |
| All 23 non-placeholder movie filenames in the 40-record `WEAPONS.DAT` store table have exact installed `.SMK` matches. | Cross-check of decoded store records against the imported raw-disc movie IDs; the other 17 records use `#`. | Confirmed for the hashed release; View activates these names directly |

The hashed `C1086.GOB` contains 486 directory entries, 10 of which have equal stored and expanded sizes. Across the GOB and 99 imported `.RES`/`.LOW` scene archives, the installer extracts every supported byte-stored, kind-1, and kind-2 resource through a centralized codec registry. A disposable end-to-end import of the hashed installation produces 29,226 manifest entries, including the five decoded kind-2 resources. Extensions remain hints rather than proof; known formats are validated before runtime decoding.

Pointer-status correction: the later fixed-point candidate and kind-4 billboard rows supersede the animation row's earlier provisional click-targeting clause. Cardinal selection now preserves source actor/object identity through ordered candidates and applies the recovered live-center, depth, ray-relative texture-column, heading-sector, mirror-fold, vertical-projection, and alpha-column formulas. Byte-identical non-cardinal ray acquisition remains open.

### Startup and character-screen findings

| Finding | Evidence | Confidence |
| --- | --- | --- |
| The title composition is the 640x480 `FFTITLE.PCX` named by `TITLE.HAT`. | `TITLE.HAT` directly names the image and declares 640x480; a `CONQUER.EXE` code reference to the HAT's data-object offset calls the common loader. | Confirmed for the hashed release |
| The title art itself contains no New/Load button overlay; input proceeds to a separate character-options screen. | Local pixel inspection of `FFTITLE.PCX` and `CHAR_OPS.PCX`, corroborated by the executable's separate screen-resource table and public gameplay footage. | Corroborated flow; exact input timing remains Provisional |
| `CHAR_OPS.PCX` presents character name, three shield colors, new generation, and pre-generated selection. | Exact decoded 640x480 resource plus `CGOPTS.HAT` region identifiers and coordinates. | Confirmed visible choices and geometry; action semantics Corroborated |
| `PREGEN.PCX` lays out six named profiles in a 3x2 grid matching the six current template names. | Exact decoded resource plus `PREGEN.HAT` region identifiers 0–5 and coordinates. | Confirmed visible names, layout, and geometry; selection semantics Corroborated |
| `LOADGAME.PCX` is a distinct five-slot Load Game screen with a Resume control. The executable names `SAVEGAME\\`, `~~1.SAV` through `~~5.SAV`, `CONQ`, and `.SAV`. | Exact decoded resource, local pixel inspection, and printable strings in the hashed executable. | Confirmed five-slot structure and filenames; exact dispatch/timing remains Corroborated |
| `OPTION.CSF` contains ON/OFF widget pairs (frames 0/1 and 2/3) plus an 80x40 Resume overlay (frame 4), not the primary title menu. The `OPTFIN.PCX` palette renders the widgets coherently; frames 0 and 2 match the owner screenshot's setting states, and frame 4 exactly matches disabled HAT region 11. | Exact CSF decoding, palette-assisted preview, HAT dimensions, and the owner-supplied runtime screenshot. | Palette and static ON/OFF/Resume roles Confirmed; alternate-frame input timing Corroborated |
| `CHARGEN.HAT` identifies `MORALITY.PCX` as screen 3 and defines three enabled 100×150 regions at `(62,310)`, `(268,310)`, and `(478,310)`. The dilemma sequences are 99×149, and rendering them with the morality-screen palette reproduces the screenshot's sepia choice art. | Bounded HAT/PCX/CSF decoding from the hashed archive plus the owner-supplied running-game screenshot. | Confirmed background, regions, dimensions, and palette association; four-frame choice grouping Corroborated |
| Home, farm, and blacksmith use distinct screen layers. `FOPTS.HAT` is the actual `TACTICAL.PCX` Home descriptor; despite its name, `FCASTLE.HAT` describes screen 19's `FIEFMGMT.PCX` table. The executable stores the ten-label Home catalog at file offsets `0xCD4BD`–`0xCD522` in the order Overview, Castle, Farm, Village, Forest, War Planning, Exit, JUMP!!, Map, Orders. Its count matches the ten ordered `FOPTS.HAT` regions, and the known Overview/Castle, three-ledger, and Map positions anchor that ordinal mapping. | Filename-preserving HAT report, exact decoded images, executable label catalog, and owner-supplied screenshot. | Background/descriptor Confirmed; complete ordinal action mapping Corroborated |
| `ICONTEMP.PCX` is the estate/travel shell, while `ICONMAP.HAT` screen 23 defines its main viewport, inset England map, information panel, tabs, and bottom navigation. | Decoded-image comparison to the owner screenshot plus the exact HAT rectangles. | Confirmed shell and geometry; terrain-tile composition remains Provisional |
| `ICA.CSF`, `ICS.CSF`, and `ICW.CSF` are parallel 337-frame, 80x80 isometric estate atlases and render coherently with the `ICONTEMP.PCX` palette. | Complete bounded decode and contact-sheet comparison across all 1,011 frames. | Confirmed dimensions/frame populations; seasonal names and palette Corroborated; semantic frame mapping Provisional |
| `FFMOUSE.CSF` contains six 20x20 interface cursor frames: sword, hourglass, travel arrows, speaking mouth, target, and pointing hand in that order. | Complete bounded decode and palette-rendered frame sheet. | Frame structure and visual identities Confirmed; palette and contextual runtime dispatch Corroborated |
| `PRACTICE.HAT` screen 15 names `PRACTICE.PCX` and defines five regions over the siege engines, jousting lane, melee troops, Exit sign, and castle. The executable's contiguous label table is `War`, `Joust`, `Melee`, `Exit`, `Castle Skirmish`; `jousprac.SMK` is the associated joust-practice movie. | Bounded HAT/PCX decode, direct visual comparison, executable strings at file offsets `0xCF578`-`0xCF594`, and `jousprac.SMK` at `0xCF430`. | Resources, geometry, labels, and movie identity Confirmed; label-to-region ordering and tactical-versus-first-person engine families Corroborated; exact practice parameters Provisional |
| The forge has distinct Blacksmith and Buy/Sell targets: the smith opens conversation and the weapon display opens inventory. | Owner-controlled observation plus `VSMITH.HAT` regions 0 and 2. | Confirmed behavior and geometry |
| The earlier assumption that `FCASTLE.HAT` was the `TACTICAL.PCX` Home descriptor is false; it describes a `FIEFMGMT.PCX` table, while `FOPTS.HAT` contains the ten office regions. | Regenerated filename-preserving HAT report explains the prior failed region-order experiment. | Earlier association Disproved; corrected descriptor Confirmed |
| `WARPLAN.CSF` contains 22 controls whose decoded dimensions partition exactly across `FWARPLAN.HAT`: five army selected/normal/disabled triplets (frames 0–14), Join/blank/Leave Selected Army (15–17), Field Army/blank (18–19), and two Send Out Spy states (20–21). | Bounded CSF decode, rendered local contact sheet, PCX palette, and exact HAT rectangle dimensions. | Frame identities and geometry Confirmed; state mutation semantics Provisional |
| War Planning supports up to five named divisions. Left/right clicks add/remove 100-serf companies of swordsmen, knights, or halberdiers; each army is limited to 60 companies and cannot be changed away from home. Spies are persistent assignments that periodically report troop movement. OK saves the batch and Cancel discards it. | Owned release manual, pp. 36–38, corroborated by `FWARPLAN.HAT`, `WARPLAN.CSF`, and the visible screen fields. | Rules and controls Confirmed; exact spy timing/cost and independent movement remain Provisional |
| The four fief-management variants share ordered executable label catalogs: 17 Castle entries from Wall through Serf, 17 Village entries from Clear Land through Granary, four Farm crops, and seven Forest entries from Cut Timber through Prospector. Their HAT files expose 18, 14, 9, and 8 visible row regions respectively. The common upper-right region overlays the words “Full Screen”; it is not a wealth field. | Contiguous printable strings in the hashed executable, filename-preserving HAT report, and direct inspection of decoded `FIEFMGMT.PCX`. | Label order, visible capacities, and fullscreen identity Confirmed; individual mutation rules incomplete |
| `WEAPONS.DAT` contains 40 six-line store records with an optional movie, unknown numeric value, image-frame index, item identifier, price, and description. `SWORDS.CSF` has 39 corresponding frames and uses the `SWDTEMP.PCX` palette; record/frame 2 is the screenshot-confirmed Battle Sword priced at 500 shillings. `BUYSELL.CSF` frames are, in order, blank View, labelled View, Sell, and Purchase overlays using the same palette. | Bounded parsing of the complete resource, CSF/palette rendering, and the owner-supplied store screenshot. | Table structure, indices, prices, palettes, overlay pixels/labels, and Battle Sword binding Confirmed; overlay state selection Corroborated; unknown field and duplicate final record semantics Provisional |
| The supplied Sabine's Keep village exterior corresponds to `V66_1111.PCX`. | Visual inspection plus normalized 640x480 comparison against every decoded `V*_????.PCX` and `TOWN_[YN].PCX`; normalized RMSE 0.05645 versus 0.16899 for the next candidate. | Confirmed scene association; world-location index mapping Provisional |
| `DILEM0.DAT` through `DILEM29.DAT` are structured dilemma definitions with stable identifiers, declared ages, CSF references, three scored choices, win/draw/lose text, and counted attribute modifiers. They form five-definition groups for each age from 12 through 17. | All 30 decoded kind-1 resources pass the same bounded marker/row parser; metadata-only reports record structure and rules without prose. | Confirmed for the hashed release |
| At each age 12–17, the executable chooses one of the five contiguous definitions with `number = (age - 12) * 5 + random(0..4)`. | LE code at VAs `0x12616`–`0x12636` subtracts 12 from age, multiplies by five, calls the inclusive 0–4 range helper at `0x22190`, and adds the result. Equivalent first/later paths call the same helper at `0x11E49` and `0x127EC`. | Confirmed for the hashed release |
| A scored choice resolves to win when its attribute is at least the high breakpoint, draw when it is below high but at least low, and lose below low. | The choice handlers obtain the scoring attribute and call the resolver at VA `0x167DC`; its ordered comparison loop returns the first breakpoint whose value is less than or equal to the score. Resource ordering and handler tables bind indices 0/1/2 to win/draw/lose. | Confirmed for the hashed release |

Owner-supplied runtime screenshots additionally establish the visible dilemma composition, estate/travel panel hierarchy, post-title options hub, village exterior, castle office, farm manager, blacksmith workshop, dialogue, and inventory sequence. Resource correlations, confidence limits, and implementation follow-ups are maintained in [`screenshot-findings.md`](screenshot-findings.md); the images themselves remain untracked.

### Runtime coverage

The runtime additionally activates validated startup and character art, installed HAT geometry, `OPTION.CSF` held/released setting widgets, the five-slot load screen, all six classified `FFMOUSE.CSF` pointers with contextual travel/talk/target/pressed-hand selection, and the `PRACTICE.PCX` menu with its five original regions and joust movie. War launches an isolated tactical practice battle; Melee and Castle Skirmish launch isolated first-person sessions, so their damage, loot, and survivors never mutate a campaign. The estate view uses `ICONTEMP.PCX`, typed `ICONMAP.HAT` panels, and locally imported seasonal `ICA`/`ICS`/`ICW` tiles; exact terrain data, frame assignments, cursor timing, and practice combat parameters remain provisional. Home/farm and blacksmith scenes use their distinct original layers, with the inventory driven by `WEAPONS.DAT`, `SWORDS.CSF`, and `BUYSELL.CSF`. Imported dilemmas use the verified selection and outcome rules while keeping original prose local. A verified official-resource extraction is required at startup; no no-media gameplay path is supported.

“Installed” means the importer safely made owned resources available under ignored local storage. The runtime now uses validated `fftitle.pcx`, `char_ops.pcx`, `pregen.pcx`, `fluff.pcx`, `loadgame.pcx`, `engmap1.pcx`, and `richard.pcc` images for the title, character flow, one-time campaign briefing, five-slot load/resume flow, strategic map, and Richard's tournament-opponent screen. Existing users must rerun `Install Original Resources.bat` once so older manifests gain all decoded entries, including kind 2. Direct SMK title/credits/store-preview playback is active; remaining event bindings, palette associations, and dialogue containers remain incomplete.

## GameFAQs secondary-source consistency audit

The [Conqueror 1086 A.D. FAQ by mikel123456](https://gamefaqs.gamespot.com/pc/574792-conqueror-1086-ad/faqs/66730) is a valuable observation-based starting point, not primary proof. This audit records whether current provisional behavior agrees with it; a match remains Corroborated until the owned executable, resources, manual, or controlled observation independently establishes it.

| Area | FAQ comparison with the current implementation | Status and next corroboration |
| --- | --- | --- |
| Character creation | The five fixed profiles, custom 240-shilling start, and baseline equipment agree. The FAQ says Chaunce Norman's attributes and wealth are randomized, while `Balance.Templates` currently fixes both. | Fixed profiles and custom start Consistent; Chaunce is a known provisional conflict requiring character-generation disassembly or repeated controlled starts. |
| Estate economy | Crop, forest, and unit endpoint tables; staffing bonuses; July bean/housing/servant penalties; one food and house tile per 100 people; population trial bands; and the roughly 7% high-tax growth reduction agree. Castle construction is count-based, so placement has no simulated rule effect, also agreeing with the FAQ. | Consistent. Exact intermediate interpolation, construction costs, and settlement ordering still require executable proof. |
| Productivity ordering | The FAQ reports that building the same productivity set in a different order can yield 90% instead of 95%. The runtime deliberately sums active bonuses deterministically and therefore does not reproduce that reported quirk. | Known behavioral gap. Trace the original mutation/order logic before deciding whether this is a reproducible rule, a display bug, or an observation artifact. |
| Tournament and courtship | Three jousts and one skirmish per tournament, five skirmish opponents, typical 20-80 shilling wagers, simultaneous courtships, and the broad reward/dragon quest routes agree. Current exact opponent tolerances and unit mixes remain synthetic. | Broad flow Consistent; exact opponent records, reward thresholds, and fallback shortcuts need executable/dialogue corroboration. |
| Equipment and armor | Prices, 75% resale, armor values, castle-only crossbows, breakable weapons, and Gambeson's extra five armor while another body armor is equipped agree. The FAQ's weapon-length values were observational surrogates and are not treated as damage formulas; executable combat rows supersede them. | Consistent where independently table- or executable-backed. |
| First-person combat | Radar, secret-door interaction, food healing, treasure, champions, crossbows, retainers, and weapon breakage agree at a feature level. The FAQ's observation that very high sword experience can make the player unhittable is consistent with the recovered defender-skill term. Maximum health now follows the executable's `strength + stamina + honor`, which is compatible with but more specific than the FAQ's stamina observation. Its report that an army of one supplies one companion and disappears if he dies agrees independently with the recovered campaign fallback and one-for-one strategic loss path. The FAQ does not specify low-level heading quantization, collision bands, effect-flag rewrites, actor convergence, or collision response, so it neither corroborates nor conflicts with those executable-derived rules. | Broad flow Consistent; the campaign retainer cap, authored friendly selection, strategic casualty conversion, four command intents, and mode-12 direct aim/bit-2 blocker/retained-offset stop-and-retry path are Confirmed. Shared destinations preserve single-cell occupancy without automatic fan-out; authored order supplies a deterministic compatibility winner because executable scheduling disproves a processor-stable original tie winner. Exact ground-ray coordinates and movement in other modes remain Provisional. The executable death-path trace disproves defeated-enemy loot for the supported release, while placed pickup rewards are Confirmed. |
| Unrepresented observations | The FAQ reports occasional critical hits with a red flash, enemies consuming food, actors following through stairways, and structure-dependent siege scenery. These behaviors are not currently modeled. | Explicit gaps requiring executable dispatch traces and controlled observation; do not infer their formulas from the FAQ alone. |
| End conditions | Capturing London for the crown, the March 1096/age-30 deadline, and the armor/shield/lance dragon route agree. The FAQ is uncertain whether the dragon stone and high strength are independently required; current strength and item gates are based on decoded dialogue/runtime evidence rather than that uncertainty. | Consistent at the route level; retain the current evidence-backed gates pending direct final-condition trace. |

## Promotion rule

A provisional value is promoted only when we can cite one of: an unambiguous static table and its code reference, a decoded resource with known semantics, a controlled repeated in-game observation, or agreement between executable behavior and independent documentation. Every promotion should update this file, `docs/fidelity.md`, and the relevant executable specification together.
