# Findings from the owned original

This register records compact, non-copyrightable facts derived from the user's installed GOG release. It deliberately does not contain extracted binaries, resource dumps, long dialogue, or media.

## Confidence scale

| Grade | Meaning | Can be treated as factual? |
| --- | --- | --- |
| Confirmed | Directly measured in the identified files, or unambiguous executable text/structure | Yes, for the hashed GOG build below |
| Corroborated | Independent documentation agrees with binary/resource evidence, but the exact executing code path has not been recovered | Usually; not necessarily an exact formula or table |
| Provisional | Plausible implementation chosen to make the game playable while exact original behavior remains unknown | No |

Confidence applies narrowly to the stated claim. Finding a name near joust resources, for example, proves that the identifier is present; it does not prove an opponent's odds or wager.

## Provenance

| Artifact | Size | XXH3-128 | Confidence |
| --- | ---: | --- | --- |
| `game.gog` | 667,316,496 bytes | `b915491c5bdce934ca216d2ceebec0ce` | Confirmed |
| Track 1 ISO payload | 244,445 sectors | derived from track 2 index `54:19:20` in `game.ins` | Confirmed |
| `CONQUER.EXE` | 919,107 bytes | `5106f53f8201761cb5112034f6c594d4` | Confirmed |
| `C1086.GOB` | 35,361,714 bytes | `741826412e307510f394d5e774519320` | Confirmed |

These hashes identify the evidence source and are not claims that every retail or localized release is byte-identical.

## Other executable findings

| Finding | Evidence | Confidence |
| --- | --- | --- |
| Castle skirmish uses `SKIRMISH.RES`, `SKIRMISH.CSF`, and `SKIRMISH.PCX`. | Literal resource names in `CONQUER.EXE`. | Confirmed |
| The configured original supports a `WAR_MODE` setting and the shipped CD configuration uses `640`. | Extracted `CONQUER.INI` in the hashed disc image. | Confirmed for this disc configuration |

## Resource archive findings

| Finding | Evidence | Confidence |
| --- | --- | --- |
| `C1086.GOB` and all 99 scene `.RES`/`.LOW` files use the same indexed Dynamix container family. | Every file begins with `.RES`, followed by a little-endian directory offset whose target contains a bounded entry count; all 26,276 scene entries decode or copy through the common codec registry. | Confirmed for the hashed build |
| Directory records are 52 bytes: a 32-byte null-padded name followed by two 32-bit fields, stored size, expanded size, and data offset. | All 486 GOB records and representative 222-entry scene archives fit exactly within their files; every tested extent remains before the directory. | Confirmed structure; semantics of the first two fields remain provisional |
| An entry with equal stored and expanded sizes is stored byte-for-byte. | Representative entries have equal lengths and readable resource payloads; synthetic parser tests verify bounded extraction. | Corroborated pending broader format sampling |
| `CONFONT.CSF` is the globally loaded proportional byte-text face: generic renderer `0x644D4` selects frame `byte * 4` from face `+0x0C`, draws it, then advances by the frame's initial signed width word returned from `0x6E0D0`. | Object-2 string `+0x3A78` (`CONFONT.CSF`); load sequence `0x2A758 -> 0x644B0 -> 0x18430`, stored global `+0xE03C`; every direct caller of `0x644D4`; `0x644D4 -> 0x7CEB0 -> 0x8A564`; bounded decode of GOB entry 483 and alpha-mask comparison with fixed sibling `font.CSF`. | The loader identity, 256 direct byte/ASCII frame entries, 13-pixel height, observed 2..10 proportional widths, frame-table lookup, and exact width advance are **Confirmed**. The CSF glyph is an alpha-style mask: `0x644D4` forwards the caller palette index to `0x8A564`, which fills opaque runs with that index; direct calls include `0xF7`, `0xF9`, and `0xB2`. `font.CSF` is a separate fixed 11x13 sibling with no direct executable load established. `ImportedFonts`, `OriginalUiFontDefinition`, and `OriginalUiFont` now require/validate `CONFONT.CSF`, preserve its relative width advances, alpha-mask shapes, and caller color control, and use a documented host-canvas conversion. Active palette association and screen-specific vertical/layout rules remain **Provisional**. |
| The first field distinguishes stored (`0`), block LZ/RLE (`1`), and adaptive LZW (`2`) entries. | The hashed GOB contains 10 equal-size kind-0 entries, 471 unequal-size kind-1 entries, and five unequal-size kind-2 entries; the two decoder branches were recovered from the executable and validated against their complete populations. | Confirmed for this build |
| All 26 `.666` resources are rate-tagged unsigned 8-bit mono PCM banks: magic `0x004A5031`, then repeated `UINT32LE` byte length, `UINT32LE` sample rate, and payload. | A bounded decoder consumes all 26 GOB/scene banks exactly; waveform centering distinguishes unsigned from signed PCM. The same 2,159-byte/11,025-Hz sample occurs identically in 17 interface banks. | Framing/rates/PCM Confirmed; shared activation role Corroborated; other event bindings Provisional |
| Every kind-1 payload is a sequence of `UINT16LE length` plus exactly that many bytes. | Bounded traversal of the 471 top-level GOB kind-1 entries consumes every stored byte exactly, producing 2,963 blocks with no zero or out-of-range lengths; the same framing validates all scene entries. | Confirmed framing for this build |
| Kind-1 blocks expand independently in 16,384-byte slices; marker `0x80` stores the slice verbatim and marker `0x40` selects an MSB-first controlled LZ/RLE token stream. | Every GOB and `.RES`/`.LOW` scene kind-1 entry has `ceil(expanded size / 16384)` blocks. All 32,089 blocks decode to their exact expected slice size; strict follow-on parsing identifies 188 PCX-compatible images. | Confirmed for the hashed release |
| Kind-1 LZ copies encode a 12-bit distance and a 3-18 byte length; distance zero instead encodes a 16-4111 byte repeated-value run. | The owned executable's decoder branches directly on these fields; an independently written bounded decoder accepts the complete GOB/scene kind-1 population. | Confirmed for the hashed release |
| Kind 2 is a continuous MSB-first LZW stream with clear `256`, end `257`, first dictionary code `258`, and adaptive widths from 9 through 14 bits. | Static code at LE virtual addresses `0x45C6E`–`0x45E04` establishes the controls and width ceiling; an independent bounded implementation expands all five entries exactly, and all four PCX results validate strictly as 640x480 images. | Confirmed for the hashed release |
| Kind-1 outer blocks use the same raw LZW stream as documented Dynamix inner chunks. | A bounded raw-LZW probe rejects the real streams at their first codes. | Disproved; do not apply the inner-chunk decoder to outer blocks |
| Kind-1 outer blocks are independently reset 16 KiB classic LH1/LZHUF streams. | A bounded probe under both bit orders produced no valid header among all 187 kind-1 `.PCX`/`.PCC` entries. | Disproved; block count and compression ratio alone do not identify the codec |
| `richard.pcc` contains a standard single-plane 8-bit RLE PCX image with a 256-color trailer palette. | The byte-stored entry passes strict header, scanline, RLE, palette, and exact-consumption validation and decodes to 195×203 pixels with index hash `bee27150…fcd36`. | Confirmed for this resource |
| `engmap1.pcx` is a kind-1 compressed 640x480 PCX and depicts the labeled parchment map of England used by the strategic screen. | Exact decoding, strict PCX validation, and local visual inspection of the owned image. | Confirmed payload and dimensions; runtime role Corroborated |
| `FLUFF.PCX` is the one-time campaign briefing shown after character preparation and before estate play. | Direct visual match to the owner-supplied 640x480 briefing capture, including baked parchment text and decoration; normalized screenshot RMSE 0.07360. `MESSAGE.PCX` is a visibly distinct blank writing screen. | Confirmed resource association and new-campaign sequence |
| `VINN.HAT` screen 12 names `INNPEOPL.PCX`; regions 0-9 bind Frederick, Gerard, Barkeep, Otto, Hugh, Gilbert, Nellie, Richard, Ivo, and Albert in that order. Region 10 is Exit and overlapping region 11 is the bottom hover-label surface. The matching named PCC portraits are all 195×203 except Nellie's 194×202 image. `INN.CSF` contains two small face patches whose event semantics remain unknown. | Direct visual match to the owner-supplied inn capture; exact descriptor decode; contiguous executable labels at file offsets `0xD1FA8`-`0xD1FFB` followed by `INN.CSF`; matching named PCC resources; and palette-assisted CSF rendering. | Background, ordinal patron identities, portrait bindings, exit, and footer geometry Confirmed; dialogue records and animation timing Provisional |
| `ALL.CIF` indexes 1,311 unique conversation nodes in `ALL.CBF`. Each node has a fixed `0x348`-byte header, byte `0x08` exactly counts prompt variants, byte `0x48` counts up to five responses, and targets begin at `0x4C`. Zero-response nodes use the first target slot as an automatic continuation: 55 link onward and 8 terminate; all nonzero response and continuation targets resolve. | Complete bounded decode of every indexed record, 2,062 prompt variants, 2,696 response records, and executable interpreter at `0x17498`-`0x174FE`. | Record framing, visible-string roles, counts, targets, continuations, and complete graph integrity Confirmed; remaining conditions and mutations Provisional |
| The ten populated-inn patrons enter through selector roots `1900,1100,3200,3500,1600,1400,3000,1698,3300,3600` in screen order. Their continuations resolve respectively to named Frederick, Gerard, Bartender, Otto, Hugh, Gilbert, Nellie, Richard, Ivo, and Albert portrait nodes. | Metadata-only probe of each root/target pair against decoded `ALL.CIF`/`ALL.CBF`; portraits and speakers match the executable-ordered `VINN.HAT` hotspot catalog. | Confirmed |
| Conversation node `3201` is the blacksmith prompt with `BLACKSMI.PCC`; root `3200` is instead the populated-inn bartender selector whose normal continuation is bartender node `3298`. | Exact decoded node speakers, portraits, prompts, continuations, and the `VINN.HAT` Barkeep association. | Confirmed |
| Generic parish selector root `3100` invokes action group `3102`, whose sole operation redirects to priest node `3101`. That node offers an offering, joust blessing, fief blessing, information, or exit. Selector `3149` instead runs group `3101` and enters Father Hyacinth's dragon-proof-armor chain, whose own text places the armor and Duchess Valletta in Cambridgeshire; Cambridge therefore uses `3149`. Six structurally parallel regional priest roots `5000`-`5500` use `PRIEST2.PCC` through `PRIEST7.PCC`. The executable's object-2 selector pool at `+0xA998` contains those six roots consecutively, followed by known selector roots including `3149` and the ten inn patrons; it establishes that the regional roots are registered conversation selectors, not an ordinal extension of the 18 reimplementation travel locations. The reproducible `Conqueror.Inspect --conversation-selector-pool` report validates this fixed 17-entry prefix without retaining original bytes. The decoded 176 person records expose groups and map coordinates, but no field or executable reference connects any one of the six entries to a regional root. | Complete decoded conversation graph and action groups `3101`/`3102`; the `3149` branch text repeatedly identifies Cambridgeshire; matching repeated prompt/response structures at each regional root; LE data-object census at `+0xA998` through the bounded `--conversation-selector-pool` report; person-table census at `+0xBA50`. | Generic and Cambridge roots Confirmed; regional root registration Confirmed; remaining regional location association incomplete |
| Conversation prompt variants are selected randomly when a node declares more than one; all declared responses are rendered in five possible 558x35 rows beginning at `(41,302)`, with mouse activation and number keys 1-5. Zero-response spoken nodes remain visible for 2,000 ms before continuing. | Interpreter paths `0x176BA`-`0x176E7`, `0x17E74`-`0x17FBA`, `0x17E3C`-`0x17E5B`, and the `0x7D0` delay passed at `0x174BC`. | Confirmed |
| Conversation headers carry 30 action-ID slots per node and per each of five possible responses. The interpreter exhausts the applicable node and response slots before consuming the last redirect written by function 3. The complete database contains 423 node and 2,318 response action references; every inn selector root invokes one action before its default continuation. `ALL.TMI` contains an unknown leading dword and 689 unique ID/offset records; the bounded `ALL.TMB` graph decoder validates 2,267 actions, 10,373 expressions, and 13,352 typed values. All distinct conversation action IDs resolve except original-data reference `5011`. | Header-copy routine `0x13BDC`; the two unconditional executor loops at `0x17507`-`0x17577`, followed by the redirect read at `0x17579`; executor/evaluator paths `0x67F38`-`0x687E4`; and complete owned-data graph traversal with metadata-only `action-tree-report.txt`. | Slot order, redirect timing, recursive grammar, population, and anomaly Confirmed; semantic bindings detailed below |
| Action function IDs 3-9 respectively redirect dialogue, assign a variable/attribute, add to one, read one, increment an item counter, clear one, and test possession. The relocated operator table maps codes 1-8 to `!=`, `<=`, `>=`, AND, OR, `>`, `<`, and `==`; only codes 3-8 occur in the reachable data. `ALL.VTB` initializes 190 signed 32-bit script variables to zero. | Dispatcher `0x1FB04`, handlers `0x17698` and `0x1F82C`-`0x1FAA4`, operator evaluator `0x68302` with relocation sources `0x6ABBC`-`0x6ABD8`, bounded full-data reports, and the exact 772-byte VTB shape. | Function/operator semantics and script-variable initialization Confirmed |
| Scope-1 action selectors `0`, `2`, `3`, `5`, `6`, `7`, and `8` address wealth, honor, fame, piety, strength, stamina, and intelligence. Produced/consumed item selectors `0`-`23` cover the Holy Chalice, tournament gifts, dragon quest objects, the Orchid, armor title, Book of Hours, and Gilbert's note; multiple generic dagger, sword, and hammer selectors intentionally share visible names while retaining distinct counters. A lone `161` possession test has no producer and occurs only in Victoria's final shield-offer gate. | Selector-to-character-index switch at `0x1F86F` and clamped character accessor at `0x13448`/`0x13464`; complete metadata action trace; selected owner-local dialogue correlation; contiguous executable item-name catalog at file offsets `0xD02B4`-`0xD03AF`. | Selectors and item IDs `0`-`23` Confirmed; `161` identity remains Provisional and is preserved raw |
| Anna Lisa's response that leads directly to node `24261`, where she discloses the lair in northwestern Wales, invokes action group `2447`; its sole operation increments global variable `2`. Action group `3202` separately tests variable `2 == 1` and redirects, while group `3203` provides the only other writer by assigning it to 1. Tournament selector roots are `2200` Adela, `2900` Jane, `2400` Anna Lisa, `2100` Victoria, `2000` Wendessa, and `2500` Valletta. Global variable `42` stores current lady colors as 1 Wendessa, 2 Victoria, 3 Anna Lisa, 4 Valletta, or 5 Jane; variable `3` distinguishes the latest joust outcome in each lady selector. | Exact conversation targets/text plus complete action-tree writer/reader audit in `conversation-text-report.txt` and `action-group-report.txt`; each root resolves to the matching named portrait. | Lair discovery variable, lady roots, colors selector, and joust-result bridge Confirmed for the hashed release; exact external call sites of groups `3202`/`3203` remain Provisional |
| Courtship prizes are dialogue actions, not an unconditional joust reward. The decoded lady action groups include Jane's item-9/item-10 reward paths, Valletta's item-20 armor path, and other lady-specific item or wealth actions. A won joust only supplies its result through global variable `3` while current colors remain global `42`; the selected lady's root evaluates those globals and quest state before applying a prize. | Action-tree report groups `2948`/`2954`/`2957`/`2959` (Jane), `2453` (Anna Lisa), `2154`/`2156`/`2158`/`2159` (Victoria), `2051`/`2060`/`2063` (Wendessa), and `2551` (Valletta); selector roots and global slots above. | Dialogue-owned prize mutation Confirmed; exact paths and eligibility predicates are retained in the imported action graph |
| Sir Frederick's repeated raids on the lair while the dragon is away are backstory, not a player plunder loop. The player-facing flow enters the dragon encounter on arrival at the discovered lair. | Anna Lisa and Frederick's decoded branches describe only Frederick taking treasure; no conversation action awards the player lair treasure. The owned executable contains the approach movie name `TRANDRAG.SMK` and the three encounter outcomes, while the surviving detailed playthrough says entering the lair cuts directly to the dragon joust. | Narrative-only absent-dragon raids Corroborated; direct arrival-to-encounter flow Corroborated; exact executable call graph remains incomplete |
| Drogo arrives to collect the exact outstanding shilling debt. The player is asked whether to pay; inability or refusal leads to combat. Killing Drogo permanently ends moneylender harassment, while losing is fatal. `DROGO.PCC` is his 195x203 portrait. The owned release also contains valid first-person scene archives `MONEY.RES` (249 entries) and `MONEY.LOW` (183). The high-resolution archive places one template-0 player base (block 114, combat row 9) and four hostile `thug` bases: block 97/template 9/row 16, block 101/template 8/row 17, and two block-134/template-3/row-17 placements. Calendar pass `0x2B060` accepts zero-based month 6 only when the outstanding-debt accessor `0x2D5A8` is nonzero, then calls debt branch `0x1074C`. Its two refusal/combat branches at `0x10828` and `0x108D1` each resolve object-2 `+0xA4` (`MONEY.RES`) through `0x21E88` before common first-person setup `0x1070C`. The desktop runtime binds this decoded map, actor metadata, collision, backdrop, color maps, texture source, and template stats for that refusal fight; its layout-required campaign API leaves no production one-enemy fallback. | Six contiguous outcome/prompt strings at executable file offsets `0xC9293`-`0xC948C`; strict decoded portrait dimensions and index hash in `stored-image-report.txt`; bounded scene-archive and actor-block census in `scene-res-report.txt` and `scene-block-report.txt`; template table at `0x542F8`; executable control flow `0x2B060 -> 0x1074C`, with object-2 `+0xA4` relocations at `0x10829` and `0x108D2`; runtime binding in `ImportedSiegeLayouts.ForDrogo`, `Campaign.CreateDrogoBattle(SiegeLayout)`, and `ConquerorGame.UpdateDrogoDemand`. | Debt choices and consequences, archive validity, placed actor metadata, `MONEY.RES` moneylender-scene dispatch, and annual month-six recurrence while debt remains Confirmed. |
| Defeating William and usurping the throne plays `CROWNL30.SMK`. Defeating the dragon first plays its combat victory and then `CHAMPL30.SMK`, whose accompanying text invests the player as King's Champion. | The William-victory text and movie name are contiguous at executable file offsets `0xCE455`-`0xCE50C`; the dragon victory, `CHAMPL30.SMK`, and investiture text are contiguous at `0xC976C`-`0xC987D`. Five evenly spaced decoded frames identify `CROWNL30.SMK` as the throne ceremony. | Crown ceremony and dragon investiture sequence Confirmed for the hashed release |
| A campaign that reaches its age limit without either victory uses `AVG_END.SMK`, distinct from fatal Drogo and dragon outcomes. | The movie name and the text “The faiths have been unkind” / “I am not the dragon slayer nor the conqueror” are contiguous at executable file offsets `0xCCE0B`-`0xCCE88`. | Non-victory age-limit sequence Confirmed for the hashed release |
| Original text controls expose an explicit `WORDWRAP` property, and conversation prompt/response controls are assigned bounded widths and heights. | `WORDWRAP` printable identifier at executable file offset `0xD2498`; conversation control construction at `0x17E74`-`0x180D3`. | Word-boundary behavior Corroborated; exact original proportional-font metrics remain incomplete |
| `fftitle.pcx` is a kind-1 compressed 640x480 PCX depicting the Conqueror A.D. 1086 title composition. | Exact decoding, strict PCX validation, and local visual inspection of the owned image. | Confirmed payload and dimensions; runtime role Corroborated |
| Byte-stored `.CSF` resources begin with magic `0x4A32`, a 32-bit frame count, and one 32-bit size per frame; concatenated frames consume the remainder exactly. | All five stored CSFs validate: counts are 723, 32, 337, 337, and 337, with each size-table sum exactly matching its remaining bytes. | Confirmed for these resources |
| CSF frames contain width/height followed by per-row literal, transparent-skip, and repeated-color segments. | Every one of 1,766 frames decodes exactly to its declared dimensions and consumes its complete chunk; only operation values 0, 1, and 2 occur. | Confirmed for these resources |
| Stored CSFs contain 119,292 literal, 254,433 transparent-skip, and 19,573 fill segments. | Reproducible `csf-report.txt` totals emitted by the bounded decoder. | Confirmed for the hashed release |
| Five stored scene `.PAL` entries are raw 256-color RGB tables with 8-bit components. | Each is exactly 768 bytes; observed maxima are 252–255 and stable hashes are emitted by `stored-palette-report.txt`. | Confirmed for these resources; screen association remains Provisional |
| Nested `SKIRMISH.RES/skirmish.csf` contains 53 frames: 24 weapon-item views, three shields, five foreground weapon groups at frames 27-41, a dagger frame at 42, and large and small five-frame blood-effect groups at 43-47 and 48-52. | Bounded nested-CSF decode with exact chunk consumption and local contact-sheet inspection under the archive's raw 768-byte `SKIRMISH.PAL`. | Frame count, dimensions, grouping, and visible identities Confirmed |
| `SKIRMISH.RES/SKIRMISH.PCX` is a headerless 64,000-byte 320x200 indexed screen paired with the raw 768-byte `SKIRMISH.PAL`. Its stone interface contains a black combat aperture at X 26-192/Y 24-140, message/status panels, four visibly labeled command buttons, a health strip, and a lower-right dynamic well. | Exact byte count, exhaustive zero-index run measurement, palette application, and local visual inspection of the decoded owned resource. | Dimensions, row-major indices, palette association, aperture, and visible static roles Confirmed; current dynamic field placement Corroborated; exact control dispatch Provisional |
| This DOS/16M-bound executable's LE enumerated page offset is relative to the embedded MZ module, not the LE header. The module begins at raw `0x26654`, its LE header is at relative `0x2AA8`/raw `0x290FC`, and its data pages begin at raw `0x4C254`. | Correct mapping yields coherent entry-point disassembly and makes all 14 fixups targeting object 2 offset `0xADB8` correlate exactly. It places `Pal%d` at object 2 offset `0x7D28`, referenced by the two recovered loading paths. | Confirmed for the hashed release; inspector mapping corrected |
| The scene population contains three five-member families, `MELEE00`-`MELEE04`, `MELEE10`-`MELEE14`, and `MELEE20`-`MELEE24`. Each family shares its architectural texture/backdrop population while its five members vary Viewer start, map, block state, and actor placement. Unsuffixed `MELEE0`, `MELEE1`, and `MELEE2` are separate base scenes. Separately, initialization `0x21DFC` registers trigger dispatcher `0x21EEC` through `0x59C24` in engine callback slot `+0xB0`; the sole indirect invocation of that slot is at `0x59BC7`. The dispatcher acknowledges slot 128 and loads literal `MELEE0.RES` at `0x22226`; there is no other executable `MELEE` filename stem. | Exact resource cardinality and ordering; hashes and structural decoding distinguish every member while grouping their shared art; executable filename references/builders, and complete direct-call plus relocation inventory of the scene loader and its wrapper. | Resource structure and common campaign `MELEE0` callback chain Confirmed |
| In placed `MELEE*`/`DEFEND*` map records, behaviour value 83 marks the observed `exit`/`gate` boundaries. | Bounded population report over the owned scene archives, including placement counts, block kinds, behavior masks and names. | Confirmed for the placed population |
| All 2,131 movies use the `SMK2` container and their frame tables, tree extents, and 182,360 aligned frame extents consume exactly 288,867,980 bytes. | Population-wide bounded parsing emitted by `smacker-report.txt`; 2,095 files declare one packed 8-bit mono track and 36 are silent. All 2,135 palette changes demultiplex safely, all 161,884 audio packets decode to 398,368,812 declared unsigned PCM bytes, and all frames decode through the four adaptive trees. Local `TITLE.SMK` and `TRANDRAG.SMK` frame renders are visually coherent. The runtime binds the title, credits, item previews, dragon-lair approach `TRANDRAG.SMK`, and the `DRJSTWIN.SMK`, `DRJSTLSE.SMK`, and `DRJSTRUN.SMK` encounter outcomes through the file-backed direct MonoGame adapter. | Container, palette, packet, packed-audio, and indexed-video decoding Confirmed for the hashed release; named dragon outcome family Corroborated; remaining event bindings and seeking incomplete |
| `lance1.CSF` contains 25 transparent foreground frames ranging from 198x182 to 416x398 pixels, while `DRJSTWIN.PCX` is a 640x480 indexed screen whose palette renders the lance coherently. The executable relocates `LANCE%1d.CSF` at object 2 `+0x1B98` from loader `0x1B250`. Wrapper `0x1BB5C` calls that loader with one, samples a selector from table/object 0x10 and clamps it to `0..20`, passes it by reference to `0x1B3AC`, restores the possibly changed selector, and maps returned values 1 or 2 to zero while all other values return one. Its direct caller `0x13328` immediately follows `0x3CED8`, invokes common route `0x1070C`, and branches to source Victory/Death text plus `champl30.smk`; this is a Confirmed dragon encounter path. `0x1B3AC` itself resolves `DRJSTRUN.SMK`, `DRJSTLSE.SMK`, `DRJSTWIN.SMK`, and `DRRIDBYE.SMK`. Its interaction worker `0x1B584` forms a five-row selector at `0x1B86C`-`0x1B880`, subtracts a horizontal band at `0x1B888`-`0x1B89F`, and clamps the resulting frame index to `0..24` at `0x1B8A3`-`0x1B8B4` before blitting a selected lance frame. `Dragon.Lance` therefore imports and renders that routed foreground. The runtime uses the confirmed uneven frame selector and source local paint origin with a host canvas scale; it does not claim to reproduce the original processor-sensitive interaction cadence. Original dialogue says the dragon breathes fire, the knight has one chance to strike, and should aim for its eye. | Population-wide bounded CSF report, local 25-frame contact sheet, decoded conversation text, local bounded dragon palette preview, and LE-aware static `--xref-string=LANCE%1d.CSF`, `0x1B250-0x1B584`, and `0x13318-0x134D9` reports from hashed executable `5D723175...B79D6`. | Frame count, dimensions, transparency, dragon loader/wrapper/caller control flow, outcome movie family, and five-row/reversed-horizontal 0..24 frame-selection structure Confirmed; palette pairing and one-thrust/eye statements Corroborated; host pointer event timing and processor-dependent interaction cadence Provisional; local motion, frame origin, target path, timing, and score arithmetic mapped below |

| All 23 non-placeholder movie filenames in the 40-record `WEAPONS.DAT` store table have exact installed `.SMK` matches. | Cross-check of decoded store records against the imported raw-disc movie IDs; the other 17 records use `#`. | Confirmed for the hashed release; View activates these names directly |

### Dragon run target timeline

The hashed `C1086.GOB` contains 486 directory entries, 10 of which have equal stored and expanded sizes. Across the GOB and 99 imported `.RES`/`.LOW` scene archives, the installer extracts every supported byte-stored, kind-1, and kind-2 resource through a centralized codec registry. A disposable end-to-end import of the hashed installation produces 29,226 manifest entries, including the five decoded kind-2 resources. Extensions remain hints rather than proof; known formats are validated before runtime decoding.

Dragon input is further **Confirmed** by static control flow. The mouse handler at `0x849C1`–`0x849C7` writes interrupt-supplied x/y directly to object-2 globals `+0x207E8/+0x207EC` when the position flag is set. Setter `0x7234C` invokes mouse interrupt `0x33` function 4 to set a position, then function 3 to read those same coordinates back into the globals. Event classifier `0x63114` can return code 3 to the dragon worker at `0x1BA83`, which ends its active input pass, but the precise mouse-button sequence, cursor artwork, and processor-dependent timing remain **Provisional**. The runtime's gold pointer crosshair and keyboard/controller aim are host presentation and input policy, not recovered original graphics.

Dragon event timing has a narrower **Confirmed** numerical map. Mouse callback `0x849E6`–`0x84A2C` queues the first four set button-event bits in a 20-byte record with timestamp, pointer x/y, and event type `(bit index - 1) & 3`. Initialization calls `0x6306C` with `(4,4)` at `0x72329`–`0x7232D` and `0x8492C`–`0x84930`, setting both classifier windows to four source timer units. For queued type 1, `0x6317A`–`0x631D0` returns code 3 only when `eventTime - lastType0Time < 4` and `eventTime - lastType1Time >= 4`; the shorter repeat returns 4 and the longer first interval returns 2. Worker `0x1BA83`–`0x1BAB6` clears its active-event flag on return code 3, then continues the movie-boundary path. This establishes the source event predicate and, together with the timer-chain map below, a nominal four-unit interval of about 220 ms from an aligned zero phase, but no separate score mutation. The runtime samples the host's current pointer on the first dragon update, matching the already-populated raw source globals, then uses host movement/controller events. Reproducing the source's timer-unit and busy-loop event throughput remains Provisional.

Dragon entry and victory dispatch are **Confirmed** by the executable path at `0x13168`–`0x134D9`. After movement/contact processing, the distinguished player record named by `AE6C` tests its grid pair against `(63,116)`, `(62,114)`, `(64,115)`, `(63,114)`, and `(63,113)`. A match reaches `0x1330C`, calls `0x1BB5C` at `0x13328`, then tests only that wrapper's return value at `0x13338` before its victory/failure branches. The wrapper reads lance experience and the three possession slots for the worker's score; this entry path has no independent strength or full-equipment predicate. The former minimum-strength/all-three-items entry and post-score victory gates are **Disproved**. `OriginalStrategicMovement.IsDragonEntryCell`, `OriginalStrategicPlayerPassResult.DragonEntryTriggered`, `Campaign.BeginDragonBattleFromStrategicMap`, and the map host now activate this path. The named-location travel UI still requires its decoded lair-discovery flag, while the source-shaped map trigger follows the executable grid test directly. `Campaign.FinishDragonBattle` records victory from the completed score instead of reevaluating the obsolete requirements row. Host-only withdrawal suppresses immediate retrigger until the distinguished record leaves the five cells. The exact approach-movie and post-result presentation timing remain **Provisional**; the direct grid branch itself does not call the dated travel adapter. Tests cover all five cells, off-cell rejection, live inventory/experience scoring, and victory independent of the dated location and synthetic strength threshold.

Dragon outcome media routing is **Confirmed** by `0x1B3AC`–`0x1B57B` and `0x1BB5C`–`0x1BC0C`. The wrapper opens `DRJSTRUN.SMK` for the active worker. A zero worker result at `0x1B4D4` plays the `DRJSTWIN.SMK` handle; a nonzero result at `0x1B4FF` plays `DRJSTLSE.SMK`. Wrapper `0x1BBE1` maps returned values 1/2 to strategic failure and every other value to success, matching the caller's sole outcome branch at `0x13338`. The same setup formats `DRRIDBYE.SMK` at `0x1B485`, but this function does not open or play that fourth name. The executable route provides no separate successful-withdrawal movie. `ConquerorGame.ResolveDragonBattle` retains Escape withdrawal as an explicit host option and now returns to the map without replaying the active run movie; `ImportedMovies.Definitions` no longer presents that replay as an original retreat asset. The host-only withdrawal choice and its exact original counterpart remain **Provisional**.

The original dragon call runs synchronously inside player pass `0x13168`, before caller `0x3C290` can reach hostile scheduler `0x3C088`. The replacement's movie/battle route spans later UI updates, so `Campaign.AdvanceOriginalStrategicPass` defers hostile scheduling when `DragonEntryTriggered` is emitted, as it does for a newly emitted player/enemy encounter. `DragonMapEntryDefersTheHostileSchedulerUntilTheBattleRouteReturns` pins the stable handoff. This is a **Provisional host ordering adaptation** to the **Confirmed source call order**, not a claim that the original always skipped the scheduler after a failed dragon run.

The host's successful Escape withdrawal has no mapped source branch. Its trigger latch now passes `SuppressDragonEntry` through `OriginalStrategicHostRuntime`, `Campaign.AdvanceOriginalStrategicPass`, and `OriginalStrategicMovement.AdvancePlayerPass` only while the distinguished record remains in the five-cell region. That pass skips the host-only reentry check but still scans all later player records and reaches ordinary hostile scheduling. Once the record leaves the region, the latch clears and a later genuine entry can run the dragon again. The unsuppressed path retains `0x1328D`–`0x134F0`'s early return. This is a **Provisional host recovery policy** constrained by the **Confirmed source early-return position**; the paired regression checks both the stopped entry pass and the resumed suppressed pass.


### Startup and character-screen findings

| Finding | Evidence | Confidence |
| --- | --- | --- |
| The title composition is the 640x480 `FFTITLE.PCX` named by `TITLE.HAT`. | `TITLE.HAT` directly names the image and declares 640x480; a `CONQUER.EXE` code reference to the HAT's data-object offset calls the common loader. | Confirmed for the hashed release |
| The title art itself contains no New/Load button overlay; input proceeds to a separate character-options screen. | Local pixel inspection of `FFTITLE.PCX` and `CHAR_OPS.PCX`, corroborated by the executable's separate screen-resource table and public gameplay footage. | Corroborated flow; exact input timing remains Provisional |
| `CHAR_OPS.PCX` presents character name, three shield colors, new generation, and pre-generated selection. | Exact decoded 640x480 resource plus `CGOPTS.HAT` region identifiers and coordinates. | Confirmed visible choices and geometry; action semantics Corroborated |
| `PREGEN.PCX` lays out six named profiles in a 3x2 grid matching the six current template names. | Exact decoded resource plus `PREGEN.HAT` region identifiers 0–5 and coordinates. | Confirmed visible names, layout, and geometry; selection semantics Corroborated |
| `LOADGAME.PCX` is a distinct five-slot Load Game screen with a Resume control. The executable names `SAVEGAME\\`, `~~1.SAV` through `~~5.SAV`, `CONQ`, and `.SAV`. | Exact decoded resource, local pixel inspection, and printable strings in the hashed executable. | Confirmed five-slot structure and filenames; exact dispatch/timing remains Corroborated |
| `OPTION.CSF` contains ON/OFF widget pairs (frames 0/1 and 2/3) plus an 80x40 Resume overlay (frame 4), not the primary title menu. The `OPTFIN.PCX` palette renders the widgets coherently; frames 0 and 2 match the owner screenshot's setting states, and frame 4 exactly matches disabled HAT region 11. | Exact CSF decoding, palette-assisted preview, HAT dimensions, and the owner-supplied runtime screenshot. | Palette and static ON/OFF/Resume roles Confirmed; alternate-frame input timing Corroborated |
| `CHARGEN.HAT` identifies `MORALITY.PCX` as screen 3 and defines three enabled 100×150 regions at `(62,310)`, `(268,310)`, and `(478,310)`. The dilemma sequences are 99×149, and rendering them with the morality-screen palette reproduces the screenshot's sepia choice art. | Bounded HAT/PCX/CSF decoding from the hashed archive plus the owner-supplied running-game screenshot. | Confirmed background, regions, dimensions, and palette association; four-frame choice grouping Corroborated |
| Home, farm, and blacksmith use distinct screen layers. `FOPTS.HAT` is the actual `TACTICAL.PCX` Home descriptor; despite its name, `FCASTLE.HAT` describes screen 19's `FIEFMGMT.PCX` table. The executable stores the ten-label Home catalog at file offsets `0xCD4BD`–`0xCD522` in the order Overview, Castle, Farm, Village, Forest, War Planning, Exit, JUMP!!, Map, Orders. Setup `0x302A0`-`0x30492` registers three callback families for regions 0-6 and 8-9, deliberately omitting region 7 each time. Region 7 remains enabled in the HAT and indexed by the hover-label table, so `JUMP!!` is visible label-only scenery rather than an action. The nine registered handlers route to the corresponding overview, management, War Planning, exit, map, and orders destinations. | Filename-preserving HAT report, exact decoded images, executable label catalog and relocation-backed callback setup, plus the owner-supplied screenshot. | Background, descriptor, complete ordinal mapping, nine actions, and inactive `JUMP!!` region **Confirmed**; the former assumption that `JUMP!!` required an unrecovered action is **Disproved**. `SceneHotspot.Interactive` retains the label while suppressing activation. |
| `ICONTEMP.PCX` is the estate/travel shell, while `ICONMAP.HAT` screen 23 defines its main viewport, inset England map, information panel, tabs, and bottom navigation. | Decoded-image comparison to the owner screenshot plus the exact HAT rectangles. | Confirmed shell and geometry; terrain-tile composition remains Provisional |
| `ICA.CSF`, `ICS.CSF`, and `ICW.CSF` are parallel 337-frame, 80x80 isometric estate atlases and render coherently with the `ICONTEMP.PCX` palette. | Complete bounded decode and contact-sheet comparison across all 1,011 frames. | Confirmed dimensions/frame populations; seasonal names and palette Corroborated; semantic frame mapping Provisional |
| `FFMOUSE.CSF` contains six 20x20 interface cursor frames: sword, hourglass, travel arrows, speaking mouth, target, and pointing hand in that order. | Complete bounded decode and palette-rendered frame sheet. | Frame structure and visual identities Confirmed; palette and contextual runtime dispatch Corroborated |
| `PRACTICE.HAT` screen 15 names `PRACTICE.PCX` and defines five regions over the siege engines, jousting lane, melee troops, Exit sign, and castle. The executable's contiguous label table is `War`, `Joust`, `Melee`, `Exit`, `Castle Skirmish`; `jousprac.SMK` is the associated joust-practice movie. | Bounded HAT/PCX decode, direct visual comparison, executable strings at file offsets `0xCF578`-`0xCF594`, and `jousprac.SMK` at `0xCF430`. | Resources, geometry, labels, and movie identity Confirmed; label-to-region ordering and tactical-versus-first-person engine families Corroborated; exact practice parameters Provisional |
| The forge has distinct Blacksmith and Buy/Sell targets: the smith opens conversation and the weapon display opens inventory. | Owner-controlled observation plus `VSMITH.HAT` regions 0 and 2. | Confirmed behavior and geometry |
| The earlier assumption that `FCASTLE.HAT` was the `TACTICAL.PCX` Home descriptor is false; it describes a `FIEFMGMT.PCX` table, while `FOPTS.HAT` contains the ten office regions. | Regenerated filename-preserving HAT report explains the prior failed region-order experiment. | Earlier association Disproved; corrected descriptor Confirmed |
| `WARPLAN.CSF` contains 22 controls whose decoded dimensions partition exactly across `FWARPLAN.HAT`: five army selected/normal/disabled triplets (frames 0–14), Join/blank/Leave Selected Army (15–17), Field Army/blank (18–19), and two Send Out Spy states (20–21). | Bounded CSF decode, rendered local contact sheet, PCX palette, and exact HAT rectangle dimensions. | Frame identities and geometry Confirmed; state mutation semantics Provisional |
| War Planning supports up to five named divisions. Left/right clicks add/remove 100-serf companies of swordsmen, knights, or halberdiers; each army is limited to 60 companies and cannot be changed away from home. `Send Out Spy` stages an 80-shilling assignment; the executable has one live-spy flag, rejects another live assignment, waits for an eligible troop movement, reports it, and consumes the spy. Strategic dispatcher `0x3C290` calls the report before movement update `0x3C088`; that updater generates/advances at most five `0x118`-byte records, dispatching modes 1 and 3 to `0x38D78` and `0x3908C`. OK saves the batch and Cancel discards it. | Owned release manual, pp. 36–38; War Planning setup/action/commit `0x35C60`-`0x36CF7`; spy action `0x36720`-`0x367D4`; live flag and report path `0x38C68`-`0x38D75`; generator `0x3BE58`-`0x3C086`; movement dispatcher `0x3C088`-`0x3C28E`; strategic caller `0x3C290`-`0x3C2A7`; `FWARPLAN.HAT` and `WARPLAN.CSF`. | Cost, single-live lifetime, five-slot authored scan order, pre-movement report boundary, and controls **Confirmed**. `StrategicEnemyMovement`, `StrategicSpyReport`, `Campaign.EnemyMovements`, and tests `ActiveSpyReportsTheFirstMovementSlotAndIsConsumedBeforeMovementAdvances` / `EnemyMovementReinforcesItsDestinationAndReleasesItsSlotOnArrival` preserve those identities. The exact original clock and route choices are now mapped, but runtime scheduling and replacement of the dated reinforcement adapter remain **Provisional**. |
| The four fief-management variants share ordered executable label catalogs: 17 Castle entries from Wall through Serf, 17 Village entries from Clear Land through Granary, four Farm crops, and seven Forest entries from Cut Timber through Prospector. Their HAT files expose 18, 14, 9, and 8 visible row regions respectively. The common upper-right region overlays the words “Full Screen”; it is not a wealth field. | Contiguous printable strings in the hashed executable, filename-preserving HAT report, and direct inspection of decoded `FIEFMGMT.PCX`. | Label order, visible capacities, and fullscreen identity Confirmed; individual mutation rules incomplete |
| The strategic movement record at object-2 `+0x1AB48` has stride `0x118`: active `+0x00`, mode-3 target player-movement slot `+0x14`, troop counts `+0x1C/+0x20/+0x24`, origin property `+0x28`, lord `+0x2C`, mode `+0x34`, integer destination/grid coordinates `+0x3C..+0x48`, and floating position/direction `+0x5C..+0x68`. Force initializer `0x38A8C` writes equal counts as `active resolvable household + floor(unsigned lord byte / 4)`, with a `0/0/1` zero-result minimum; location 7 has a separate `176 - active-list-count` source. Generator `0x3BE58` checks a 5,000-unit accumulator before adding strategic speed `+0xAEEC` and requires `random(100) > 96`. Reset initializes that scalar to 1; four handlers constrain it to 1-15. Candidate helper `0x3BDC0` scans all 14 property records in authored order, retains rows with nonzero `+0x00` and `+0x0D == 1`, returns `-1` when none qualify, and otherwise selects uniformly by compact-list ordinal. Completed motion probes `(x,y)`, `(x,y-2)`, `(x+1,y)`, `(x-1,y-1)`, then `(x,y+1)` before encounter, transfer, or retarget resolution; it is not a dated arrival. | Force initializer `0x38A8C`; constructors `0x3A764/0x3AC5C`; generator `0x3BE58`-`0x3C086`; speed initialization/handlers `0x110D7`, `0x3D781`-`0x3D7DA`, `0x3E41E`-`0x3E468`; candidate helper `0x3BDC0`-`0x3BE22`; movement dispatcher/contact resolution `0x3C088`-`0x3C28E`; location/person helpers `0x43004`, `0x43150`, `0x43558`, `0x43628`, `0x43764`-`0x43928`. | Record layout, mode-3 target identity, ordinary force formula, zero-result minimum, 5,000-unit/strict-roll gate, speed identity/bounds, candidate population/order/filter/empty result, state dispatch, and contact probe order **Confirmed**. The former `+0x14` target-location label and elapsed-millisecond interpretation are **Disproved**. `OriginalStrategicMovement` preserves the corrected layout, formula, speed scalar, and candidate selection. Lord-byte meaning, special location-7 population, and replacement of the runtime's daily dated-travel adapter remain **Provisional**. |
| Strategic mode 2 owns a heap route pointer at record `+0x6C`, waypoint count/index at `+0x18/+0x30`, and completion flag at `+0x0C`; `0x4A300` advances packed coordinate pairs and uses strict per-axis tolerance 6. On handler completion, `0x3C088` probes five fixed offsets for the first person. Eligible assignment-zero contact enters encounter handling first. Otherwise troop transfer requires a nonzero person whose assignment matches the origin property's state byte; it adds all three troop fields to the origin garrison and destroys the record. Every other contact retargets. | Mode-2 constructor `0x3AC5C`/route builder `0x4A070`; route update `0x4A300`; completion resolver `0x3C088`-`0x3C278`; person/property helpers `0x43640`, `0x43658`, `0x43764`, `0x437CC`, `0x437EC`, `0x43868`; destroy/retarget `0x3A6A0`, `0x3A9BC`. | Route storage/construction, completion signal, probe order, encounter precedence, matched-origin transfer, and fallback retarget are **Confirmed** and represented by `OriginalStrategicMovement` plus tests. Generic destination reinforcement is **Disproved**. Alternate route-resource identity and runtime consumption remain **Provisional**. |
| Dispatcher `0x3C088` calls generator `0x3BE58` first, then visits physical slots 0-4 once. Mode 1 handler `0x38D78` tests crossing against `truncate(current + normalized direction)`, samples terrain one normalized step ahead, and advances by `direction * 0.9 * terrain speed * strategic speed`; object-2 double `+0x4F97` is exactly `0.9`. It leaves the normalized direction stored, refreshes grid coordinates after movement, and on kind 9 returns the troop total to the origin's byte garrison modulo 256 before deactivating. Mode 3 handler `0x3908C` reads target player-army slot `+0x14`, signals completion if that target is inactive, otherwise aims from `truncate(current + prior direction)` toward the target's live rounded position and advances by terrain speed without the 0.9 factor. Kind 9 instead aims at origin property map coordinates `+5/+7`, changes mode 3 to mode 1, and immediately takes one unscaled normalized step. Zero-length normalization falls back to `(1,1)`. | Direct handler `0x38D78`-`0x3908B`; pursuit handler `0x3908C`-`0x39426`; truncation helper `0x63EC0`; property helpers `0x43640/0x43658/0x438F8`; object-2 constant `+0x4F97`; dispatcher `0x3C088`-`0x3C0EA`. | Call order, target-slot identity, truncation points, direct 0.9 factor, terrain/profile/speed arithmetic, byte-garrison return, target disappearance signal, mode-3 live retarget, kind-9 mode switch, and `(1,1)` fallback are **Confirmed**. `AdvanceMovementPass` implements all three modes in physical slot order and reports completion separately from active state; focused tests cover crossing equality, byte wrap, target disappearance, pursuit fallback, and ordering. Generator construction and completion/contact side effects are not yet connected to this kernel. |
| Route matrix object-2 `+0xC9D4` selects canonical `rt_<low>_<high>.rat` waypoint resources for 180 directed property pairs: low-to-high uses stored order, high-to-low reverses it. Self-pairs and Cambridge/Dunster (one-based groups 7 and 13) in either direction are the only zero entries. Each route file begins with a little-endian signed 32-bit point count followed by exactly that many signed 32-bit x/y pairs. The 90 matrix-selected owned files contain 13-197 points; three additional `rt_*` self-route files exist in the archive but are not selected. Retargeter `0x3A9BC` chooses the first active field-army slot strictly within 300 map units; with none, it chooses whichever of the player or origin property is nearer, favoring the player on equality. | Route builder `0x4A070`-`0x4A2FC` allocation/copy/reversal flow; initialized 14-by-14 selector matrix; structural census of the 93 owned `rt_*` resources, including present `rt_1_13.rat` and absent `rt_7_13.rat`; retargeter `0x3A9BC`-`0x3AC5A`; object-2 float `+0x537D = 300.0`; property helper `0x438F8`. | Resource binary layout, naming/direction, 90 canonical resources / 180 directed routes, unsupported pair, first-slot range rule, and fallback tie are **Confirmed**. `StrategicRouteDecoder`, `PropertyRouteResources`, startup completeness checks, `TryGetPropertyRoute`, and `SelectRetarget` preserve these results with focused tests. The earlier York/Dunster identification is **Disproved**. Live use of decoded points remains **Provisional**. |
| Route flag 0 is the new-game starting-home route family. Sole caller `0x1110A` invokes helper `0x43670`, which chooses one of seven person indices at object-2 `+0xB8C8`, stores its selector at `+0xC9D0`, resolves and returns the selected person, and clears that person's assignment after using its coordinates. Initializer `0x110E8` stores that return directly to fallback target global `+0x1B0C4`, passes it to `0x4389C`, and stores that result to fallback origin global `+0x1B0CC`; the absent-property-list timed branch of `0x3BE58` later passes those exact globals to its route-flag-0 constructor attempts. Route builder `0x4A070` indexes `+0xCA98` with the same selector and loads `sc_0.rat` through `sc_6.rat` without reversal. The bindings are persons 17/18/24/29/86/144/166: Scott's Keep, Anne's Castle, Stonetree Castle, MacGibbon on the Hill, Leecastle, Damron Castle, and Sabine's Keep. Their groups/origin properties are 0/0/1/1/4/12/13, assignments 1/1/2/2/6/13/14, and grid positions `(94,40)/(129,23)/(142,82)/(120,100)/(125,152)/(110,264)/(27,306)`. Each decoded route begins near its associated property seat and terminates at the selected home castle. | New-game caller/initializer `0x110E8`-`0x11133`; selector helper `0x43670`; origin resolver `0x4389C`; generator `0x3BE58`; object-2 tables `+0xB8C8/+0xC9D0/+0xCA98/+0x1B0C4/+0x1B0CC`; person records at `+0xBA50`; route builder `0x4A070`; decoded owned `sc_0.rat`-`sc_6.rat` endpoints and structural census; derived `--strategic-terrain-table` report. | Selector lifetime, seven identities, initial fallback-global writes, resource names, no-reversal behavior, person metadata, and starting-home purpose are **Confirmed**. `OriginalStrategicMovement.StartingRoutes`, `OriginalStrategicCampaignState.SchedulerFallbackTargetPerson`/`SchedulerFallbackOriginProperty`, startup completeness checks, and focused tests preserve the mapping. The later write paths for these globals remain **Provisional**; the earlier claim that route-flag-0 identity was unknown is superseded. |
| The movement support tables are executable-initialized and persisted by `default.dat`: 14 property records of 15 bytes at object-2 `+0xB8EC`, and 176 person records of 18 bytes at `+0xBA50`. Property helpers expose coordinate pairs `+1/+3` and `+5/+7`, lord `+9`, 16-bit linked-list next `+0x0A`, and garrison `+0x0C`. Person rows contain name pointer `+0x00`, group `+0x04`, state byte `+0x05`, flags `+0x06`, assignment `+0x07`, 16-bit x/y `+0x08/+0x0A`, lord rating `+0x0C`, byte list-next `+0x0D`, and four further state bytes `+0x0E..+0x11`. Save/load `0x42E4C`-`0x43000` resets the two list heads and every person `+0x0D` to `0xFF`, transfers overlapping four-byte person slices rooted at `+0x06` and `+0x07`, and transfers every complete property row. Following each lord index to its person name identifies York through Okehampton. Household counter `0x43004` requires matching group, nonzero assignment, a nonzero record index, and flags bit `0x01`; initial eligible assigned-household counts for groups 0-13 are `4,6,7,5,2,2,4,3,3,1,9,3,3,6`. | Initialized executable data and person-name strings; save/load `0x42E4C`-`0x43000`; property helpers `0x43164`, `0x431D0`, `0x43640`, `0x43658`, `0x4377C`, `0x438C8`, `0x438F8`; person helpers `0x43048`, `0x43064`, `0x43080`, `0x43134`, `0x43248`, `0x43794`, `0x437EC`, `0x43868`, `0x4389C`, `0x43928`; complete derived `--strategic-terrain-table` census. | Layouts, all 176 numeric rows, names, save/reset widths, filters, and initial counts **Confirmed**. The compact authored census has XXH3-128 `3ee3a20a2dcf7a7e8c5155e6eb4bad55`; `OriginalStrategicMovement.Persons` preserves every row, and tests bind all property lords, starting castles, and household counts back to it. Semantic labels for ambiguous state bytes remain **Provisional**. |
| Instruction-level recheck corrects the ordinary force initializer and closes the scheduler shell. `0x38A8C` computes `household + floor(floor(unsigned lord rating / 4) / 3)` for each troop type, with `0/0/1` only when that value is zero. For property 7 it obtains household support as `0xB0 - 0x43558()`: `0x43558` follows `PersonListHead` and each person `+0x0D` link until `0xFF`, so this is 176 minus the current list length rather than the normal group/assignment/eligibility count; pursuit initializer `0x38B40` repeats that special branch before its cap of 30. The former single-division expression and screen-supplied property-7 count are **Disproved**. Pursuit initializer `0x38B40` detaches `min(origin byte garrison, target three-type total + 3)`, combines that with household support capped at 30, emits exactly three swordsmen when the combined value is at most three or `floor(combined / 3)` of each type otherwise, and subtracts only the detached component. Dispatcher `0x3C088` runs generation first and then interleaves handler/completion for physical slots 0-4, so a new record may move immediately and an encounter may stop later slots. | Ordinary initializer `0x38A8C`, especially `0x38AD0`-`0x38B03`; pursuit initializer `0x38B40`; person-list counter `0x43558`; destroy `0x3A6A0`; constructors `0x3A764/0x3AC5C`; dispatcher/contact resolution `0x3C088`-`0x3C28E`; grid identity helpers `0x629B0/0x437CC/0x3E708`; hashed executable `5D723175...B79D6`. | Both force rules, property-7 list arithmetic, first-free construction, constructor field sources, generator-first ordering, immediate new-slot motion, five-probe auxiliary-person contact, encounter stop, matched-origin reinforcement, teardown, and retarget are **Confirmed** and mapped by `AdvanceSchedulerPass`, `TryGridCell`, and focused tests. Generator `0x3BE58` has one original defect: after `0x3BB4C` returns false without writing its output pointers, timed branch `0x3BFBD` may read a stale property local. The clean implementation requires an explicit current-pass detection; that deterministic safety policy is not claimed as byte-for-byte reproduction of undefined stack state. |
| Generator `0x3BE58` has a reactive path before its timed path. Property 7 delays non-player-slot pursuit until counter 1000. An existing mode-3 pursuer blocks another except for the player slot after counter 50. Successful pursuit rolls 0-5 and values 0/1 may add a route-flag-0 mode-2 movement, falling back to mode 1. At the timed boundary, an absent property-list head uses the global origin and route flag 0; a present list independently selects eligible properties for active-slot pursuit attempts, then uses a selected property and route flag 1 for mode 2 with mode-1 fallback. Constructors always use the first inactive movement slot. | Generator `0x3BE58`-`0x3C086`; reactive finder `0x3BB4C`; pursuer scan `0x3BE24`; pursuit constructor `0x3A764`; free-slot helper `0x3A738`; ordinary constructor `0x3AC5C`; list-head test `0x43150`; candidate selector `0x3BDC0`. | Branch gates, strict boundaries, authored slot order, constructor argument roles, mode order, and route flags are **Confirmed**. The seven route-flag-0 identities are closed in the preceding row; finder property-state side effects remain **Provisional**. `OriginalStrategicMovement` maps the confirmed decisions and tests their boundary cases. |
| Reactive finder `0x3BB4C` scans nonzero-state properties 0-13, then active player army records 0-4, both in authored order. It truncates player floats `+0x5C/+0x60`, compares them with property words `+0x05/+0x07`, and accepts per-axis distance strictly below 30; property 7 uses 40 and also accepts the rectangle `x=[700,12640)`, `y=[600,4560)`. Exact property-lord identity in the current grid cell's auxiliary word is also near. Near contact calls `0x435E8` to set property byte `+0x0D=1`; a zero garrison then skips the rest of that property's army scan, while a nonzero garrison returns the first pair. Non-near approach uses strict per-axis limits 250, or 200 for property 7, and calls `0x42F38` to set one-shot byte `+0x0E=1` after presentation/centering. | Finder `0x3BB4C`; property helpers `0x42F38/0x42F4C`, `0x435E8/0x43628`, `0x43640`, `0x43764`, `0x4377C`; grid auxiliary helper `0x3E708`; rectangle predicate `0x64164`; x87 truncation wrapper `0x63EC0`; hashed executable `5D723175...B79D6`. | Scan order, strict thresholds, half-open London bounds, lord-cell identity, state mutations, empty-garrison continuation, and first-match return are **Confirmed**. `FindReactiveDetection` now derives detections from live pursuit records and emits `OriginalStrategicPropertyAlert`; focused tests cover authored ordering, boundary/one-shot behavior, London bounds, lord identity, and empty-garrison mutation. This supersedes the preceding row's Provisional finder-side-effect clause. |
| Strategic caller `0x3C290` invokes estate update `0x3AFB4`, secondary update `0x3B0E4(0)`, spy report `0x38C84`, and player movement/collision `0x13168` unconditionally. It checks modal global `AEF8` only after that player pass: a nonzero value skips enemy scheduler `0x3C088`, while a clear value runs it before the remaining maintenance. Player records begin at object-2 `+0x1A4B8`, use the same `0x118` stride, and contain six records: indices 0-4 are player armies and the only valid pursuit targets (`0x3A764` rejects target >=5); index 5 is a special player-avatar record. `AE64` is the selected record across all six. `AE6C` is a distinct distinguished-player selector: initialization writes 5 for the avatar, while join/leave paths `0x1133C/0x113A4` exchange it with an army index 0-4. It is not the selected-record field or a sixth army. | Strategic caller `0x3C290`; initialization `0x110E8`; join/leave helpers `0x1133C/0x113A4`; player pass `0x13168`; pursuit constructor `0x3A764`; object-2 globals `+0x1A4B8`, `+0xAE64`, `+0xAE6C`, and `+0xAEF8`; hashed executable `5D723175...B79D6`. | Update order, post-player modal gate, table address/count/stride, five-army target domain, special-avatar slot, and both global index identities/ranges are **Confirmed**. `OriginalStrategicCampaignState` persists all six records plus selected/distinguished indices, while `Campaign.AdvanceOriginalStrategicPass` exposes the `AEF8` gate without suppressing the earlier report/player work. The earlier descriptions of `AE6C` as the selected/current record and as army-only are **Disproved**. Named campaign locations remain an invalid substitute. |
| Player record handler `0x11554` skips inactive or completed records, follows either inline route pairs at `+0x70` (21-pair capacity) or target handle `+0x14`, whose bit `0x1000` selects an enemy movement and whose low byte is the target index. It recomputes normalized direction for live targets, advances at most one waypoint under the same strict per-axis `<6`/direction-crossing test, and clears count `+0x18`, cursor `+0x30`, and target on completion without clearing stale inline pairs. Movement uses the current terrain kind `+0x58`, selected profile, and speed multiplier. It samples a prospective cell from truncated direction/speed; direction outside inclusive `[-40,40]` stops. When the current kind is 9, a second 50-unit direction probe must also remain kind 9 before the record is stopped, selected, and marked complete. Otherwise it stores the prospective kind, applies both floating components, and refreshes grid `+0x44/+0x48`. | Complete disassembly `0x11554`-`0x11CAA`; truncation helper `0x63EC0`; projection `0x629B0`; tile lookup `0x3E72C`; object-2 constants `+0x351=40.0f`, `+0x355=-40.0f`, `+0x359=50.0`; terrain tables `+0xB610/+0xB704`; hashed executable `5D723175...B79D6`. | Layout, target tag, waypoint capacity/order/retention, arrival test, current-terrain speed, prospective lookup, direction guard, two-stage kind-9 probe, movement, and grid refresh are **Confirmed**. `OriginalStrategicPlayerMovementSlot` and `AdvancePlayerMovementPass` persist and execute this kernel. Indexed non-enemy world-target population semantics remain **Provisional**, exposed through an identity-preserving target boundary rather than guessed names. |
| Outer player pass `0x13168` scans active player records 0-5. It decrements cooldown `+0x54`, runs `0x11554`, then scans active enemy records 0-4. Contact requires both absolute floating coordinate deltas strictly below object-2 double `+0x65A=30.0`; contact selects the player record. Army records invoke encounter handoff `0x39428`, whose entry independently returns while global `ADC0` is nonzero. Avatar record 5 instead emits a proximity alert only when its post-decrement cooldown is zero, then stores 120. The distinguished `AE6C` army enters the dragon route when its grid coordinate is one of `(63,116)`, `(62,114)`, `(64,115)`, `(63,114)`, or `(63,113)`. | Complete `0x13168`-`0x1351E`; encounter handoff entry `0x39428`-`0x3943E`; movement handler `0x11554`; camera/presentation adapter `0x130E4`; object-2 globals `+0xAE64/+0xAE6C/+0xADC0`; hashed executable `5D723175...B79D6`. | Physical player/enemy scan order, movement-before-contact, strict threshold, selection, independent `ADC0` contact guard, avatar identity/cooldown, and five-cell dragon entry are **Confirmed**. `AdvancePlayerPass` exposes typed contact, avatar-alert, and dragon-entry outputs with ordering/boundary tests, and `Campaign.AdvanceOriginalStrategicPass` keeps the contact and `AEF8` scheduler gates independent. Presentation and encounter settlement remain application handoffs. |
| Encounter wrapper `0x35924` reads player swordsmen/halberdiers/knights via `0x29E58` and hostile movement counters from record offsets `+0x1C/+0x20/+0x24`. If hostile total exceeds 60, it calculates `floor((total - 60) / 3)` and, for every category satisfying strict `reduction + 1 < count`, subtracts that reduction before `0x258FC`. For player total above 60 it instead divides the same excess by the count of categories initially at least 20, then applies the same strict guard to every category; after resolver return it adds each removed player quantity back to the associated stored pool. `0x258FC` receives all six staged counters by reference. Its `0x256FC` gate allocates five rectangles in input order: `(30,30,215,185)`, `(400,30,205,200)`, `(25,250,185,185)`, `(375,250,185,185)`, and `(220,417,148,38)`. Shared selector `0x6FF10` scans those entries in order with left/top-inclusive, right/bottom-exclusive containment. Its dispatch table at `0x156E8` writes menu values `2,3,1,0` for the first four regions respectively; the fifth exits and returns zero. The four values drive distinct later formation paths in `0x28C38`, but their player-facing meanings are not yet recovered. `0x28C38` allocates `0x34 * sum(all six counters)` unit bytes and appends player swordsmen/halberdiers/knights before the enemy's same three categories. Each entry has immutable category column `+0x00` of `0/0x78/0xF0`, category value `+0x24` of `10/20/40`, side lane `+0x2C` of `0/0x168`, type code `+0x14` of `3/7`, and strength `+0x20=100`; menu value selects a later formation path that changes the mutable coordinates at `+0x04/+0x08`, not those category fields. Code 0 at `0x2904B` writes each player-prefix entry `i` as `x = 60 * floor(i / floor(verticalSpan / 60)) + 60`, `y = 60 * (i % floor(verticalSpan / 60)) + 30`. Code 1 at `0x290BA` keeps y and writes `x = 60 * floor(playerCount / floor(verticalSpan / 60)) + 60 - 60 * floor(i / floor(verticalSpan / 60))`; both use signed division and leave enemy entries for later paths. Code 2 at `0x29132` finds the smallest `r` where `r * (r + 1) / 2 >= playerCount`, emits player rows of lengths `1..r`, begins at `(45r, floor(verticalSpan / 2))`, advances each entry y by 60, and begins each next row at x minus 45/y minus 30. At terminal completion, `0x263C4` first zeroes all six caller counters and scans the table in order, incrementing the matching player/enemy category only if strength is strictly positive. The zero branch at `0x259D0` compares player staged total plus argument 7 divided by three against hostile staged total plus argument 8 divided by three (the `0x35924` caller passes zero as argument 8). At equality or loss it zeroes the three staged player counters. On a win it calls raw random helper `0x6B3F1` three times in player category order; each signed nonnegative remainder modulo the current hostile total is subtracted from that category, clamped at zero, and also subtracted from the divisor before the next draw. `0x35924` writes its resulting hostile values back to the movement record and calls `0x29DE0`, which increments player-record `+0x50`, writes the three pools at `+0xA4/+0x100/+0x15C`, and clears its associated state fields `+0x1B8/+0x214/+0x270`. Back in `0x39428`, all-zero resolved player pools send a non-`AE6C` record through `0x29D24`; that helper clears `+0x50/+0x27C/+0xA4/+0x100/+0x15C/+0x1B8/+0x214/+0x270` and calls field-record removal `0x12DD0`. The distinguished record instead enters the loss/modal route and sets `ADC0`. | Complete handoff `0x39428`-`0x398FC`; wrapper `0x35924`-`0x35C59`; choice gate `0x256FC`-`0x258FA`; menu dispatch table `0x156E8`; selector `0x6FF10`/predicate `0x64164`; interactive unit constructor `0x28C38`-`0x2964A`, including all code-0/1/2/3 formation paths `0x2904B`-`0x29643`; terminal unit scan/write-back `0x263C4`-`0x26477`; resolver entry/result writes `0x258FC`-`0x264F7`; player force accessors `0x29DE0/0x29E58`; removal helper `0x29D24`; hashed executable `5D723175...B79D6`. | Six-counter staging, choice-region geometry/order/half-open selection, non-semantic selection codes, interactive unit table construction/classification, all four formation geometries, terminal survivor write-back, automatic-fallback arithmetic, both record layouts, write-back/removal order, and the distinguished/non-distinguished empty-force split are **Confirmed**. `OriginalStrategicEncounterMenu`, `OriginalStrategicInteractiveEncounter`, and focused regressions preserve the mapped menu/survivor/layout boundaries without guessing tactical labels. `OriginalStrategicEncounterStaging` and focused threshold/fallback regressions implement the automatic arithmetic while preserving separate player reserves. `Campaign.ResolveAutomaticOriginalStrategicEncounter` verifies a captured handoff before applying the known counters and exact `0x29DE0` post-resolution state reset; `Army.OriginalStrategicEncounterCount` and `OriginalStrategicEncounterStates` map the preserved player fields. The resolver's interactive tactical outcomes, choice meanings, and conversion to a first-person session remain **Provisional**. `OriginalStrategicPlayerEnemyEncounter` captures the six pre-scheduler counter values and slot identities at the application handoff, without selecting a tactical engine; `FieldBattleSession` must not be used as a silent substitute. |
| Strategic map input `0x122AC` resolves an overlapping click in strict table order: active player records 0-5, active enemy records 0-4, then active temporary division-force records 0-2. A player hit selects it through `AE64` and enables route-input global `AE58`. Confirmed enemy and division hits instead write `0x1000 | index` or `0x10000 | index` to the selected record's target `+0x14`, clear route count/cursor and completion, and disable route input; declining either confirmation consumes the hit. An empty click starts or extends the selected record's fixed inline route, replacing target/count/cursor/completion when `AE58` was clear. Although 21 pairs physically fit before the `0x118` boundary, input accepts only counts below `0x14` (20 points). First-point helper `0x120E4` sets destination/direction and primes terrain from a one-direction-unit probe. Undo `0x12044` decrements the count and, at zero, marks complete, clears target, and disables route input without erasing stale pairs; `0x12284` only exits route-input mode. | Complete disassembly `0x12044`-`0x12A48`, especially dispatcher `0x122AC`; player/enemy/division bases `+0x1A4B8/+0x1AB48/+0x1A150`; globals `+0xAE58/+0xAE64`; hashed executable `5D723175...B79D6`. | Click precedence, both target tags, three-record division target table, confirmation consumption, 20-point input cap versus 21-pair storage, first-point priming, undo, and stale-byte retention are **Confirmed**. `DispatchPlayerMapCommand`, typed target commands, route append/undo/end operations, and persisted `PlayerRouteInputActive` map this constructor state. Screen-space hit rectangles and modal presentation remain application concerns. |
| New-game initializer `0x110E8` clears all six player records and marks every record complete, chooses one of seven starting people through `0x43670`, clears that person's assignment, and activates only record 5. Both `AE64` and `AE6C` become 5; record 5 is selected. Its grid is the chosen person's stored `(x,y)`, while destination and floating current coordinates are helper `0x63E20`'s exact anchor `(80*(x+1), 20*(y+1))`. The other five records remain inactive but complete. | Complete disassembly `0x110E8`-`0x1127F`; chooser `0x43670`; cell-anchor helper `0x63E20`; seven bindings at object-2 `+0xB8C8/+0xCA98`; globals `+0xAE64/+0xAE6C`; hashed executable `5D723175...B79D6`. | Starting-person assignment, avatar-only activation, all-record completion initialization, both global values, grid, and anchor formula are **Confirmed**. `CreateForNewGame` constructs this bootstrap exactly, while `Campaign.InitializeOriginalStrategicNewGame` selects and persists one of its seven routes through the campaign-owned random source for new Game sessions. Legacy schema-1 migration retains selector `-1`, no active record, and the neutral distinguished/selected value 5 until migration supplies a position. |
| Join helper `0x1133C` writes the requested army index to `AE6C`, activates that army record, clears the old selected marker, selects it through `AE64`, and deactivates avatar record 5. Leave helper `0x113A4` refuses while the avatar is already active; otherwise it restores `AE6C=5`, keeps the army active but unselected, copies the army's floating and grid coordinates into avatar 5, clears the avatar target/count/cursor, marks it complete/active/selected, clears `AE58`, and writes `AE64=5`. It does not copy or clear the army's movement command. | Complete disassembly `0x1133C`-`0x1146B`; player table `+0x1A4B8`; globals `+0xAE58/+0xAE64/+0xAE6C`; hashed executable `5D723175...B79D6`. | Join/leave record identity, activation, selection, exact coordinate transfer, avatar-only command clearing, and army retention are **Confirmed**. `JoinPlayerArmy`, `LeavePlayerArmy`, and `Campaign.ToggleArmyMembership` preserve these transitions; field-record construction remains the next live binding. |
| Player field-record constructor `0x12CAC` accepts while counter `AE5C <= 5`, increments it, anchors the requested record at home globals `1B0D4/1B0D0` plus slot offsets at `A564`, clears state `+0x08` and cooldown `+0x54`, and activates/completes/selects the record. It clears target/count/cursor on the formerly selected record rather than the constructed record. Initializer `0x110E8` writes `AE5C=0` independently of active avatar 5. Removal `0x12DD0` requires a positive counter, selects the first other active record in physical order, restores avatar 5 at the removed distinguished record's exact position/grid when needed, clears the removed active/selected/state/cooldown fields, marks it complete, and decrements the counter. | Complete disassembly `0x12CAC`-`0x12F25` and initializer `0x110E8`; player table `+0x1A4B8`, home globals `+0x1B0D4/+0x1B0D0`, count `+0xAE5C`, formation table `+0xA564`; hashed executable `5D723175...B79D6`. | Six-success boundary, independent count, exact six offsets, initialized and deliberately stale fields, first-active selection, distinguished-avatar restoration, and teardown are **Confirmed**. `ConstructPlayerMovementRecord`, `RemovePlayerMovementRecord`, persisted globals/state, and focused tests preserve the paths. |
| Routed strategic motion uses the 331-byte tile-to-terrain-kind lookup at object-2 `+0xAF78`, four profile pointers at `+0xB704`, and two 30-float speed tables at `+0xB614/+0xB68C`. Calendar helper `0x3866C` returns zero-based month; setter `0x3E894` indexes object-2 `+0xB720`, whose values are `2,2,3,3,3,0,0,0,1,1,1,2`. Profile movie pointers at `+0xB750` and atlas pointers at `+0xB760` identify 0 Summer (`tran4.smk`, `ics.csf`), 1 Autumn (`tran2.smk`, `ica.csf`), 2 Winter (`tran3.smk`, `icw.csf`), and 3 Spring (`tran1.smk`, `ics.csf`). Only Winter selects the reduced table; Spring and Summer deliberately share both terrain speeds and the ICS atlas while retaining distinct transition movies. `0x4A300` checks strict `< 6` tolerance or direction-aware waypoint crossing, advances at most one waypoint per call, uses the truncated integer square root for normalization, and rejects a prior stored component outside inclusive `-50..50`. Terrain is sampled at truncated current plus the prior stored direction. Kind 9 deactivates. The new normalized displacement is terrain speed times the bounded 1-15 scalar at `+0xAEEC`; both scaled components are stored, but applied only while the absolute unrounded x component is strictly below object-2 double `+0x7389 = 50.0`. The y component has no independent same-call limit. | Route updater `0x4A300`-`0x4A61B`; integer length helper `0x2FC70`; truncation helper `0x63EC0`; speed initialization/handlers `0x110D7`, `0x3D781`-`0x3D7DA`, `0x3E41E`-`0x3E468`; calendar/profile flow `0x3866C/0x3E894`; coordinate/terrain lookup `0x629B0/0x3E72C`; executable-initialized tables `+0xAF78/+0xB614/+0xB68C/+0xB704/+0xB720/+0xB750/+0xB760`; derived `strategic-terrain-table-report.txt`. | Profile identities, month mapping, resource bindings, tile-kind and speed values, waypoint/crossing order, integer normalization, prior-direction guard, prospective sample, arithmetic, kind-9 deactivation, and strict asymmetric application limit are **Confirmed**. `AdvanceMovementPass` now consumes decoded forward/reverse routes and camera-projected terrain for this exact motion kernel. The original's processor-dependent call frequency is deliberately replaced by one explicit caller-controlled pass; generator/contact integration and activation on MonoGame's fixed update remain pending. |
| Strategic terrain originates in C1086.GOB resource 292 (`icon.jp`), not the seasonal atlas. New-game path `0x3E660` allocates 200 row pointers of 400 dwords with 80-by-80 projection dimensions and loads resource `0x124` through `0x3EE50`. The exact 320,016-byte resource begins with signed dwords `80,80,200,400`; its cells follow column-major, with low 16-bit tile identity, an independent bits-16..23 field, and a preserved upper byte. Strategic exit `0x3E6CC` writes the full grid to `temp.jap` through `0x6E980`; reload `0x3E928` restores it through `0x6E320`; `0x6E928` releases the row allocations. Cell-to-route helper `0x63E20` returns `(80*(row+1),20*(column+1))`. Inverse adapter `0x629B0` shifts a virtual origin `(80*cameraRow+40,20*cameraColumn+20)` until the point is visible, then picker `0x6E5A0` scans wrapped rows/columns in rendered order. Its exact inclusive raster is `abs(x-centerX)+2*abs(y-centerY)<=40`, with even/odd column centers at x `80*row+40/80` and y `20*column+20`. Shared-edge ownership is camera ordered: among all 8,304 owned `rt_*/sc_*` point occurrences, 210 are edges and 34 occurrences at six coordinates differ between opposite camera extremes; none fail projection. | Allocation `0x6E1D0`; resource load `0x3E660/0x3EE50`; low/secondary accessors `0x3E708/0x3E72C/0x3E74C`; scratch save/load/free `0x3E6CC/0x3E928/0x6E320/0x6E928/0x6E980`; coordinate helpers `0x63E20/0x629B0/0x6E5A0`; owned `icon.jp` hash; complete owned route census; derived `strategic-world-grid-report.txt`. | Source, dimensions, column-major disk order, row-first live order, field masks, scratch lifecycle, forward anchor, inverse viewport adjustment, inclusive diamond geometry, wrap, and scan precedence are **Confirmed**. `StrategicWorldGridDecoder`, `StrategicWorldProjection`, and `ImportedContentCatalog.DecodeStrategicWorldGrid` preserve these facts without committing original bytes. Schema-2 mutation/camera persistence and live decoded-route use remain **Provisional**. |
| Character-options initializer `0x14230` writes zero to dynamic `CHARACTR.DAT` row 0 field 19 (`COLOR`). Its HAT-region dispatcher `0x145A4-0x1464A` maps region 3 Red to 0, region 4 Green to 3, and region 5 Blue to 5 before calling setter `0x15F0C`. Marker initializer `0x12A77-0x12AA5` later uses that same row/field through `0x15EF0(0,0x13) << 3` as the common player-record frame base. | Complete initializer/dispatcher disassembly, `CGOPTS.HAT` region identities, setter call-context report, marker initializer `0x12A77-0x12AA5`, and configuration helper `0x15EF0`; hashed executable `5D723175...B79D6`. | The exact three control values and their frame-base relationship are **Confirmed**. `OriginalStrategicCharacterColor`, `Player.OriginalStrategicCharacterColor`, and the character-creation definitions preserve them independently of heraldic names and combat color maps. Palette binding and strategic marker presentation remain **Provisional**. |
| Strategic focus adapter `0x130E4` passes the player record's `+0x44/+0x48` grid pair to `0x3C6E0`. Given the live 80-pixel span and `20..403` by `7..441` viewport, `0x3C6E0` sets row `1B104 = (gridX - 2) mod 200` and column `1B108 = max(0, gridY - 11)`. The caller then clamps row to inclusive `10..192` and column to inclusive `2..300`. It next refreshes the strategic surface (`0x3C770`), player markers (`0x12F28`), and presentation (`0x3A63C`). Terrain renderer `0x3C770` traverses the `20..403` by `7..441` viewport beginning at y `-53`; each 20-pixel scanline uses its wrapped grid-column parity to begin x at `-20` or `20`, advances rows in 80-pixel steps, selects only the grid cell's low word, and retains partial top/left/right tiles for clipping. Initializer `0x12A77-0x12AA5` stores `0x15EF0(0, 0x13) << 3` at each player record's `+0x38`, loads `icon_men.CSF` from object-2 string `+0x4AC` at `+0x110`, and clears `+0x04`; paired object-2 string `+0x4BC` is `marker.CSF`. `0x15EF0` performs a two-level lookup through object-2 `+0xA650` into dynamic `CHARACTR.DAT`; its zero-based field `0x13` is `COLOR`. The owned default row 0 color is 5, making its initial base 40, but this is dynamic rather than a universal marker constant. Loader `0x18430` calls `0x6A190`, a DOS file-attribute probe, with the string address before opening and decoding the named CSF. For each active player record, marker pass `0x12F28` reads records in physical slot order, adds that source base to offset `1/6` for ordinary unselected/selected records, `5/0` for the distinguished `AE6C` record, or `2/7` for avatar slot 5; source record `+0x04` selects those pairs. Its two `0x63EC0` calls temporarily set x87 rounding toward zero before `frndint`; normal branches therefore pass truncated current coordinates to `0x3F0A0`, while the avatar branch may take the paired coordinate from the selected `AE64` record. With 80-pixel dimensions and map viewport `(20,7)-(403,441)`, that helper admits inclusive source `x = 80*row+40..+383` and `y = 20*(column+1)..+474`, supplies blitter origin `x - (80*row+40) - floor((20*(column+1)+434)/2) + 20, y - 20*(column+1) - imageHeight + 7`, then delegates cropping to the selected image blitter. Contact wrapper `0x39428` supplies the selected player record's `+0x44/+0x48` grid pair immediately before resolver `0x35924`; avatar-alert and special-trigger branches in `0x13168` use the same adapter. | Complete `0x12A4C-0x12AE9`, `0x130E4-0x13165`, focus helper `0x3C6E0`, renderer `0x3C770`, marker pass/projection `0x12F28/0x3F0A0`, resource loader/probe `0x18430/0x6A190`, configuration helper `0x15EF0`, truncation helper `0x63EC0`, viewport setter `0x6E4B0`, grid initializer `0x6E1D0`, object-2 string decode at `0x4AC/0x4BC`, decoded `CHARACTR.DAT` structure, C1086.GOB directory, and callers `0x13168-0x1327F`/`0x39428-0x39532`; hashed executable `5D723175...B79D6`. | Adapter call order, focus transform/clamp, source globals, inclusive clamp bounds, terrain traversal/order/coordinates, wrapped row/column selection, low-word tile source, dynamic common marker-base lookup and default value, exact CSF bindings, active-marker order/frame offsets, marker-coordinate truncation, and pre-blit marker origin are **Confirmed**. `OriginalStrategicMapCamera.FocusOnGridCell`, `OriginalStrategicMapTerrainRendering.BuildDraws`/`BuildTileDraws`, and `OriginalStrategicTerrainPresentation.BuildBlits` preserve terrain selection/order and source clipping before host scaling; the latter selects the source-confirmed profile atlas and rejects a grid tile outside the loaded atlas. `OriginalStrategicMapMarkerPresentation.FrameBaseForCharacterColor`/`FrameFor`/`BuildDraws` and `OriginalStrategicMapMarkerProjection.TryProjectBlitOrigin`, with focused regressions, preserve the corresponding marker choices. The current replacement profiles lack the numeric source `COLOR` field, so runtime marker activation remains pending rather than guessing a mapping from heraldic labels; downstream marker pixel clipping and the complete input-event producer are still **Provisional**. |
| **Implementation correction (current)**: the previous row's statement that replacement profiles lacked source `COLOR` is superseded. | `OriginalStrategicCharacterColor`, persisted `Player.OriginalStrategicCharacterColor`, `OriginalStrategicMarkerPresentation.BuildBlits`, MonoGame strategic draw path, and focused clipping regression. | The runtime now persists 0/3/5, selects active `icon_men.CSF` frames in physical source order, projects and clips them after the source terrain pass. The campaign-shell palette is **Corroborated**; `marker.CSF` overlay semantics plus strategic input/scheduler integration remain **Provisional**. |
| Map-host integration now focuses new campaigns on the source home grid and sends map viewport clicks through the source dispatcher; pointer-edge panning receives one deterministic host fixed update instead of the original unrestricted-loop rate. | Focus adapter `0x130E4`/`0x3C6E0`, panning handler `0x3D83C`, pointer dispatcher `0x122AC`, modal boundary `0x123FD-0x12515`, host `EnterCampaignMap`/`UpdateOriginalStrategicMapInput`, and focused core/host regressions. | Focus transform, edge inequalities, map click conversion, player-hit precedence, route construction, persisted three-slot temporary targets, and target-confirmation acceptance boundary are **Confirmed**. The host's fixed-update cadence and its Enter/Escape dialog controls are clean-room deterministic policy. The source dialog text/physical controls and temporary-force producer remain **Provisional**. |
| Source dispatcher `0x3C290` performs report handling, player-record update `0x13168`, then—unless its modal guard is active—hostile scheduler `0x3C088`, before terrain/player/movement presentation. The original caller loops at processor-dependent throughput. | Complete `0x3C290-0x3C2D1`; modal guard at `+0xAEF8`; recovered `AdvanceOriginalStrategicPass`; MonoGame `IsFixedTimeStep` and 60 Hz `TargetElapsedTime`; host-runtime regressions. | Source pass order and the original unstable cadence are **Confirmed**. `OriginalStrategicHostRuntime` now runs one recovered pass per fixed 60 Hz map update, using the campaign's seeded random stream rather than draw or input frequency. It projects persisted temporary-force targets rather than supplying artificial inactive entries, and blocks only the hostile scheduler for schema-one migrations whose original fallback globals are explicitly unavailable; no fallback is guessed. Typed player/enemy contact hands off to the source-backed resolver. Temporary-force production remains **Provisional**. |
| The source strategic presentation contains two sequential `icon_men.CSF` marker passes. After player pass `0x12F28`, `0x3A63C` scans the five physical `0x118`-byte movement records at `+0x1AB48`, skips inactive `+0x00`, truncates each live `+0x5C/+0x60` position, and passes direct frame `+0x38` plus the initialized `+0x110` handle to the common `0x3F0A0` projection/blitter. Movement constructor paths `0x3A764` and `0x3AC5C` seed that frame as `word[object2+0xAE88 + 2 * originProperty] << 3`; properties 0--13 map to `88,8,16,72,32,56,48,56,64,72,80,88,96,64`. `0x12A4C` initializes the movement handle from `icon_men.CSF` and clears `+0x38`; `marker.CSF` is loaded alongside it but does not supply this pass. | Complete `0x3A63C`, `0x3A764`/`0x3AC5C`, and callers `0x3C2CC`/`0x3BE58`; object-2 static table `+0xAE88`; initializer `0x12A4C`; shared projection/blitter `0x3F0A0`; hashed executable `5D723175...B79D6`. | Pass order, physical active scan, record offsets, toward-zero conversion, direct frame use, property-keyed constructor table, exact reachable values, and `icon_men.CSF` binding are **Confirmed**. `OriginalStrategicMovementSlot.MarkerFrame`, exact constructor assignment, `BuildMovementDraws`/`BuildMovementBlits`, host draw order, and focused clipping regression preserve them. Palette binding remains **Corroborated**; `marker.CSF` semantics remain **Provisional**. |
| Strategic panning handler `0x3D83C` first marks `AEF8` during its work, then tests raw pointer `207E8/207EC` in source order. It increases camera row `1B104` only when x is strictly above `0x274` and row is below `0xC0`, decreases it only when x is strictly below `8` and row is above `10`, increases column `1B108` only when y is strictly above `0x1DA` and column is below `0x12C`, and decreases it only when y is strictly below `8` and column is above `2`; it clears `AEF8` before return. The four source helpers alter a single axis, so a corner can alter both in one pass. | Complete handler `0x3D83C-0x3D8B5`; axis helpers `0x6E500/0x6E524/0x6E558/0x6E57C`; camera globals `+0x1B104/+0x1B108`, pointer globals `+0x207E8/+0x207EC`, and modal global `+0xAEF8`; hashed executable `5D723175...B79D6`. | Branch order, strict inequalities, limits, independent corner updates, and transient `AEF8` writes are **Confirmed**. `OriginalStrategicMapCamera.ApplyMappedEdgeScroll` preserves the camera mutation without exposing the transient global beyond its source call; physical-input conversion and redraw scheduling remain **Provisional**. |
| Player-map dispatcher `0x122AC` calls coordinate adapter `0x640A0` with raw pointer globals `207E8/207EC` before all player/enemy/division hit tests and route insertion. The adapter maps the raw pair through the live camera exactly as `routeX = rawX + 80 * 1B104 + 40` and `routeY = rawY + 20 * (1B108 + 1)`; its branch on column parity reduces to that same `20 * (column + 1)` offset. It scans active player records 0-5 first, testing a truncation-derived `(currentX + 11, currentY - 30, 24, 31)` rectangle, then active hostile records 0-4 and temporary division records 0-2 with `(currentX, currentY - 20, 40, 40)`. All rectangles use `0x64164`'s left/top-inclusive, right/bottom-exclusive rule, and the first table hit wins. A player hit selects/enables route input; accepted hostile/division hits replace the selected record's target, and rejected target prompts consume the click. Otherwise the dispatcher starts or appends its route point. Mouse callback `0x84980` writes its event `CX/DX` directly to those globals when event bit 0 is set, while cursor helper `0x7234C` invokes mouse services 4 then 3 and writes the returned `CX/DX` pair to the same globals. | Complete `0x640A0-0x64163`; dispatcher `0x122AC-0x1265A`; containment helper `0x64164`; mouse callback `0x84980-0x84A48`; cursor helper `0x7234C-0x72374`; map constructor `0x6E1D0-0x6E317` establishes the 80-unit cell dimension and 200-by-400 limits; hashed executable `5D723175...B79D6`. | Raw-pointer conversion, arithmetic, ordered hit rectangles, half-open bounds, post-hit command order, position before route insertion, and untransformed mouse-service provenance are **Confirmed**. `OriginalStrategicMapPointer.ToMappedRoutePoint`, `OriginalStrategicMapHitTesting.HitTest`, and `OriginalStrategicMapCommands.DispatchRawPointer` map the source-owned conversion/selection/constructor boundary, while `ConquerorGame.OriginalPoint` supplies the equivalent 640-by-480 logical input after host-window scaling. |
| `WEAPONS.DAT` contains 40 six-line store records with an optional movie, unknown numeric value, image-frame index, item identifier, price, and description. `SWORDS.CSF` has 39 corresponding frames and uses the `SWDTEMP.PCX` palette; record/frame 2 is the screenshot-confirmed Battle Sword priced at 500 shillings. `BUYSELL.CSF` frames are, in order, blank View, labelled View, Sell, and Purchase overlays using the same palette. | Bounded parsing of the complete resource, CSF/palette rendering, and the owner-supplied store screenshot. | Table structure, indices, prices, palettes, overlay pixels/labels, and Battle Sword binding Confirmed; overlay state selection Corroborated; unknown field and duplicate final record semantics Provisional |
| The supplied Sabine's Keep village exterior corresponds to `V66_1111.PCX`. | Visual inspection plus normalized 640x480 comparison against every decoded `V*_????.PCX` and `TOWN_[YN].PCX`; normalized RMSE 0.05645 versus 0.16899 for the next candidate. | Confirmed scene association; world-location index mapping Provisional |
| `DILEM0.DAT` through `DILEM29.DAT` are structured dilemma definitions with stable identifiers, declared ages, CSF references, three scored choices, win/draw/lose text, and counted attribute modifiers. They form five-definition groups for each age from 12 through 17. | All 30 decoded kind-1 resources pass the same bounded marker/row parser; metadata-only reports record structure and rules without prose. | Confirmed for the hashed release |
| At each age 12–17, the executable chooses one of the five contiguous definitions with `number = (age - 12) * 5 + random(0..4)`. | LE code at VAs `0x12616`–`0x12636` subtracts 12 from age, multiplies by five, calls the inclusive 0–4 range helper at `0x22190`, and adds the result. Equivalent first/later paths call the same helper at `0x11E49` and `0x127EC`. | Confirmed for the hashed release |
| A scored choice resolves to win when its attribute is at least the high breakpoint, draw when it is below high but at least low, and lose below low. | The choice handlers obtain the scoring attribute and call the resolver at VA `0x167DC`; its ordered comparison loop returns the first breakpoint whose value is less than or equal to the score. Resource ordering and handler tables bind indices 0/1/2 to win/draw/lose. | Confirmed for the hashed release |

Owner-supplied runtime screenshots additionally establish the visible dilemma composition, estate/travel panel hierarchy, post-title options hub, village exterior, castle office, farm manager, blacksmith workshop, dialogue, and inventory sequence. Resource correlations, confidence limits, and implementation follow-ups are maintained in [`screenshot-findings.md`](screenshot-findings.md); the images themselves remain untracked.

### Runtime coverage

The runtime additionally activates validated startup and character art, installed HAT geometry, `OPTION.CSF` held/released setting widgets, the five-slot load screen, all six classified `FFMOUSE.CSF` pointers with contextual travel/talk/target/pressed-hand selection, and the `PRACTICE.PCX` menu with its five original regions and joust movie. War launches an isolated tactical practice battle; Melee and Castle Skirmish launch isolated first-person sessions, so their damage, loot, and survivors never mutate a campaign. The estate view uses `ICONTEMP.PCX`, typed `ICONMAP.HAT` panels, and locally imported seasonal `ICA`/`ICS`/`ICW` tiles; exact terrain data, frame assignments, cursor timing, and practice combat parameters remain provisional. Home/farm and blacksmith scenes use their distinct original layers, with the inventory driven by `WEAPONS.DAT`, `SWORDS.CSF`, and `BUYSELL.CSF`. Imported dilemmas use the verified selection and outcome rules while keeping original prose local. A verified official-resource extraction is required at startup; no no-media gameplay path is supported.

“Installed” means the importer safely made owned resources available under ignored local storage. The runtime now uses validated `fftitle.pcx`, `char_ops.pcx`, `pregen.pcx`, `fluff.pcx`, `loadgame.pcx`, `engmap1.pcx`, and `richard.pcc` images for the title, character flow, one-time campaign briefing, five-slot load/resume flow, strategic map, and Richard's tournament-opponent screen. Existing users must rerun `Install Original Resources.bat` once so older manifests gain all decoded entries, including kind 2. Direct SMK title/credits/store-preview playback is active; remaining event bindings, palette associations, and dialogue containers remain incomplete.

## GameFAQs secondary-source consistency audit

The [Conqueror 1086 A.D. FAQ by mikel123456](https://gamefaqs.gamespot.com/pc/574792-conqueror-1086-ad/faqs/66730) is a valuable observation-based starting point, not primary proof. This audit records whether current provisional behavior agrees with it; a match remains Corroborated until the owned executable, resources, manual, or controlled observation independently establishes it.

| Area | FAQ comparison with the current implementation | Status and next corroboration |
| --- | --- | --- |
| Character creation | The five fixed profiles, custom 240-shilling start, and baseline equipment agree. The FAQ says Chaunce Norman's attributes and wealth are randomized, while `Balance.Templates` currently fixes both. | Fixed profiles and custom start Consistent; Chaunce is a known provisional conflict requiring character-generation disassembly or repeated controlled starts. |
| Estate economy | Crop, forest, and unit endpoint tables; staffing bonuses; July bean/housing/servant penalties; one food and house tile per 100 people; population trial bands; and the roughly 7% high-tax growth reduction agree. Castle construction is count-based, so placement has no simulated rule effect, also agreeing with the FAQ. | Consistent. Exact intermediate interpolation, construction costs, and settlement ordering still require executable proof. |
| Productivity ordering | The FAQ reports that building the same productivity set in a different order can yield 90% instead of 95%. The runtime deliberately sums active bonuses deterministically and therefore does not reproduce that reported quirk. | Known behavioral gap. Trace the original mutation/order logic before deciding whether this is a reproducible rule, a display bug, or an observation artifact. |
| Courtship | Simultaneous courtships and the broad reward/dragon quest routes agree. | Broad flow Consistent; exact reward thresholds and fallback shortcuts need executable/dialogue corroboration. |
| Equipment and armor | Prices, 75% resale, armor values, castle-only crossbows, breakable weapons, and Gambeson's extra five armor while another body armor is equipped agree. The FAQ's weapon-length values were observational surrogates and are not treated as damage formulas; executable combat rows supersede them. | Consistent where independently table- or executable-backed. |
| End conditions | Capturing London for the crown, the March 1096/age-30 deadline, and the armor/shield/lance dragon route agree. The FAQ is uncertain whether the dragon stone and high strength are independently required and contains no internal score evidence. | The executable-derived five-cell entry and lance-experience/item-bonus score supersede the former independent strength/full-gear gate; dragon-stone semantics remain unverified. |

## Encounter formation amendment

Menu selection code 3 in resolver constructor `0x28C38` is no longer provisional. Its branch `0x29209-0x29643` divides the player swordsman count by two. With `rows = floor(verticalSpan / 60)`, the first `floor(swordsmen / 2)` sword records use `x = 60 * (floor(floor(swordsmen / 2) / 4) + 1) + 60 * floor(nonSwordsmen / rows) - 60 * floor(i / 4) + 120` and `y = 60 * (i % 4) + 30`; the remaining sword records retain that x formula and use `y = 60 * (i % 4) + 480`. The player halberdier/knight suffix uses the same base without 120, divides i by rows for x, and uses the regular y grid. A single raw helper `0x6B3F1` result is divided by two; for its non-negative remainder zero, enemy records use `x = horizontalSpan - (60 * floor(i / rows) + 60)`, while remainder one uses `x = horizontalSpan - (60 * floor(enemyCount / rows) + 60) + 60 * floor(i / rows)`; both y coordinates are `60 * (i % rows) + 30`. The compiler-generated four-entry switch after this divisor-two remainder has cases 2 and 3 that cannot occur for a non-negative source, so those dead paths are not reproduced. Source: complete regenerated executable disassembly of `0x28C38-0x2964A` from hashed executable `5D723175...B79D6`. These formulas and the two reachable raw-choice outcomes are **Confirmed**; `OriginalStrategicInteractiveEncounter.ApplyMappedMenuCodeThreeFormation` and `StrategicInteractiveMenuCodeThreePreservesItsCategorySplitAndRawEnemyLayoutChoice` map them with an explicit raw random boundary. This supersedes the code-3-Provisional statements in the prior encounter row. Tactical resolution semantics and user-facing choice labels remain **Provisional**.

## Interactive encounter loop initial amendment

The initial control and death-completion portions of interactive resolver loop `0x26B88` are **Confirmed**. Every loop pass writes an eight-byte presentation rectangle for each unit into `19CB0`: a non-positive `+0x20` strength writes four zero words, and a positive entry maps its coordinates to `(+0x04 - 15, +0x08 - 20, 25, 30)`. The loop calls `0x264F8` immediately afterward and returns one when that call returns nonzero. At live state `+0x1C = 0x50`, it advances `+0x18` modulo five; when the result is four it decrements the matching player/enemy aggregate (`19C70` or `19C94`), sets strength `+0x20` to zero, and rewrites a knight category column `+0x00=0xF0` to halberdier `0x78`. This supersedes the prior description of `+0x00` as permanently immutable: it remains stable for every positive survivor, so `0x263C4` survivor classification is unchanged. Source: regenerated complete static disassembly beginning at `0x26B88`, with constructor layout at `0x28C38`; hashed executable `5D723175...B79D6`. `CompleteMappedDeathAnimation` and `StrategicInteractiveKnightDeathCompletionZeroesStrengthAndDowngradesItsLiveCategory` implement/test only this post-animation record effect. The input event, state meanings, target selection, collision, and cadence of the rest of this tactical loop remain **Provisional** and are not inferred by this narrow mapping.

## Interactive encounter timing amendment

Interactive loop `0x26B88` samples timer helper `0x18420` before and after its gate. `0x18420` returns object-2 counter `A688 << 2`; callback `0x183C0` increments `A688`. Setup `0x183E8-0x1840D` registers that far callback with timer service `0x69AAC` at requested frequency `0xFA = 250` Hz. The service computes divisor `0x1234DC / frequency` at `0x69B3A-0x69B59`, and `0x80661-0x806A6` writes it to timer port `0x40` when its hardware path is active. Dispatcher `0x6A025-0x6A0EA` invokes registered callbacks from its timer interrupt. Thus each `A688` increment represents nominally four ms, and the helper advances in four-unit steps. The loop skips while unsigned `current <= previous + 0xC8`, then stores its second clock sample as the new previous value before walking live units. The strict 200-unit threshold first passes at 204 sampled units from an aligned baseline. Registration, nominal cadence, one-pass late baseline, and no-catch-up behavior are **Confirmed** from executable control flow in hashed release `5D723175...B79D6`; exact host timer phase and hardware oscillator drift are **Provisional**. `OriginalStrategicInteractiveEncounterTiming` quantizes its default monotonic clock to four-ms steps and uses the recovered 200-ms threshold with a strict comparison; caller-supplied `Cadence` remains an explicit host calibration. `StrategicInteractiveEncounterTimingUsesSourceTicksAndDoesNotCatchUp` and `StrategicInteractiveTimingAcceptsAnExplicitStableHostCalibration` cover tick boundaries, delayed frames, and custom cadence.

## Interactive encounter rectangle-selection amendment

The tactical neighbor query's lower-level selection primitive is **Confirmed**. `0x6FF10` receives a point, rectangle count, and contiguous eight-byte `(left, top, width, height)` list, scans records in increasing address order, and returns the first matching entry as one-based index; zero means no match. Its predicate `0x64164` is half-open: `left <= x < left + width` and `top <= y < top + height`. At the start of every `0x26B88` pass, a positive-strength unit writes `(record +0x04 - 15, record +0x08 - 20, 25, 30)` to its matching selector row, whereas a non-positive unit writes four zero words. Source: complete static disassembly `0x6FF10-0x6FF4C`, `0x64164-0x6419B`, and `0x26BA5-0x26C18` in hashed executable `5D723175...B79D6`. `OriginalStrategicInteractiveEncounterGeometry` and `StrategicInteractiveEncounterGeometryUsesPositiveStrengthBoundsAndFirstExclusiveRectangleHit` implement and pin these rules. The complete `0x28408` transformed-corner order is confirmed separately below; it deliberately preserves its dead-first-hit fallback asymmetry rather than becoming a generic nearest-neighbor query.

The interactive control preamble's viewport movement is **Confirmed**. In source order at `0x264FF-0x265CC`, `inputX <= 5` subtracts 10 from global horizontal offset `19CAC` only when it is positive; `inputX >= viewportWidth - 5` adds 10 only when `contentWidth - viewportWidth - 10 > horizontalOffset`. The matching vertical tests use input Y and `19CA8`; their lower boundary has a distinct reserve: `contentHeight - viewportHeight - 50 > verticalOffset`. Every successful branch sets `19CC0` to one. These are signed comparisons and strict far-edge limits; no generic clamping or input-action interpretation is inferred. Source: regenerated static disassembly of `0x264F8-0x265CC` from hashed executable `5D723175...B79D6`. `OriginalStrategicInteractiveEncounterViewport.ApplyMappedEdgeScroll` and `StrategicInteractiveViewportUsesSourceOrderedEdgeBandsAndStrictContentLimits` preserve/test the four ordered checks. The UI event source, button semantics, and later tactical transition remain **Provisional**.

## Interactive encounter dispatcher amendment

Mouse callback `0x8497C` masks DOS mouse event bits `0x1E`, takes each set bit in low-bit order, and maps bits 1-4 to raw kinds 0-3 by `(bit index - 1) & 3`; these are primary down/up and secondary down/up. It appends at most 30 entries of 20 bytes at object-2 `+0x20530`, writing timer counter `+0x16830`, pointer X/Y, and raw kind at offsets `+0x00/+0x04/+0x08/+0x0C`. Callback `0x84A4C` increments that timer counter before chaining the previous INT 1Ch vector. Setup `0x84887-0x848A5` obtains and replaces INT 1Ch through DOS vector helpers `0x732E5/0x713B4`. Timer setup `0x6999E` installs a special old-IRQ0 callback; on registering the 250-Hz battle callback, `0x69BDD-0x69C03` derives its 16.16 increment as `floor(0x123333 / 250) = 4771`. Dispatcher `0x6A025-0x6A0EA` calls that callback when the accumulator crosses `0x10000`; callback `0x6A0EB` reaches `0x8083D`, which chains the saved IRQ0 vector. The [IBM PS/2 and PC BIOS Interface Technical Reference, p. 2-7](https://bitsavers.trailing-edge.com/pdf/ibm/pc/ps2/PS2_and_PC_BIOS_Interface_Technical_Reference_Apr87.pdf) confirms that this vector calls INT 1Ch once per BIOS timer tick. Four pointer units first accumulate at about 220 ms from an aligned zero phase; an arbitrary entry phase can shift that boundary. Pointer event queue pop `0x63098` removes the first 20-byte entry from the queue at object-2 `+0x20530`, shifts the remaining fixed 0x244-byte region, and decrements count `+0xDF38`. Classifier `0x63114` copies event timestamp `+0x00`, X `+0x04`, and Y `+0x08` to its caller and dispatches kind `+0x0C` through the four-entry jump table at object-1 `+0x53104`. Kinds 0/2 record primary/secondary down times at object-2 `+0xDF3C/+0xDF40` and return codes 1/5. Kinds 1/3 classify primary/secondary release: unsigned held duration `>= +0xDF4C` returns 2/6 and clears the corresponding short-release timestamp `+0xDF44/+0xDF48`; a shorter hold returns 4/8 when time since the previous short release is strictly below `+0xDF50`, otherwise 3/7 and stores the release time. Both thresholds are set to four source timer units by calls `0x72329`–`0x72332` and `0x8492C`–`0x84935` to setter `0x6306C`. This code and the strict boundaries are **Confirmed** from the hashed executable; the executable timer chain and IBM BIOS contract **Corroborate** the nominal approximately 18.2-Hz pointer timestamp rate; exact startup phase and behavior under a nonstandard BIOS remain **Provisional**. `OriginalStrategicPointerEventClassifier` preserves these code identities and thresholds on supplied monotonic units, using absent timestamps for unmatched releases instead of reproducing startup-zero ambiguity. `OriginalStrategicPointerEventQueue` preserves the callback's low-bit order, 30-entry cap, and captured timestamp/X/Y across later pops; `ConquerorGame.StrategicEncounter` dispatches one queued code with its captured coordinates per stable update. The controller destination shortcut enters that queue as host code 7. `ConquerorGame.StrategicEncounter` now supplies primary and secondary button transitions; `OriginalStrategicPointerClock` reproduces the 250-Hz IRQ0 divisor and 16.16 BIOS-chain accumulator against monotonic host time; an aligned host epoch reaches four units at 220 ms. The controller's secondary action supplies the corresponding short-release code 7. Focused regressions cover long/equal hold, short, repeat, independent buttons, and unmatched releases.

The input-code dispatch and control-strip geometry inside `0x264F8` are now **Confirmed**. After its source-ordered edge-scroll checks, the dispatcher obtains an integer input code. Code 2 alone enters player-hit selection at `0x26999`; code 3 alone selects among the three control rectangles; codes 6 and 7 both enter the selected-list destination write at `0x26A7F`; all other values return to the loop without one of those mapped record mutations. Setup routine `0x258FC-0x25FB5` allocates those three eight-byte control rectangles immediately after the unit rectangle table. With resolution-dependent margin `m` from `19C74` and viewport height `H`, they are `(m+400, H-34, 84, 31)`, `(m+524, H-34, 41, 31)`, and `(m+566, H-34, 41, 31)`, in selector order; their shared half-open hit predicate is `0x64164`. `0x25F20` stores margins `0xA0` and `0x50` on its `0x400` and `0x320` setup paths, while the 640 path keeps the initializer zero. This margin is distinct from edge-scroll globals `19CA8/19CAC`: the code-3 control selector at `0x267D8` uses raw input coordinates, whereas player selection at `0x26999` and destination write at `0x26ACF-0x26AF9` add the scrolling pair. In the code-3 branch, a miss falls through to player selection. The first rectangle, when confirmation flag `19C8C` is zero, writes `19C7C=0`, sets `19C8C=1`, and draws its pending presentation; the initial `19C7C=1` gate therefore changes to enable rather than skip timed tactical passes. On a later hit, `0x26832-0x2685B` invokes generic confirmation with the object-2 string `Do you want to retreat?`; only a nonzero dialog result returns one from the dispatcher and exits the resolver, while a zero result falls through to unit selection without clearing either confirmation word. Thus the first control is a **Confirmed** tactical activation followed by a retreat-confirmation route; the exact affirmative/negative input mechanics remain Provisional. The second rectangle writes control code one to the selected list, and the third writes it to every positive-strength record. Source: static control flow `0x267A4-0x26B67`, setup writes `0x25F20-0x25FB5`, tactical gate `0x26C2D-0x26C58`, selector `0x6FF10`, object-2 string references `+0x339C/+0x33A8/+0x33B0/+0x33B8/+0x33C0`, and predicate `0x64164` from hashed executable `5D723175...B79D6`. `OriginalStrategicInteractiveEncounter.RouteMappedInputCode`, `RouteMappedControlStripHit`, `OriginalStrategicInteractiveEncounterGeometry.CreateMappedControlStripRectangles`, and session route/confirmation helpers expose only these recovered branches; focused regressions pin codes, order, dimensions, gaps, confirmation persistence, edge-scroll/control separation, and half-open fallback. The event producer and downstream tactical state transitions remain intentionally unmapped.

The pre-dispatch hover/status path in `0x264F8` is also **Confirmed**. It searches the same temporary unit-rectangle table through `0x6FF10` using input coordinates after the source-ordered scroll offsets. A player-lane hit formats `OUR %3d%%` with record `+0x20` strength; an enemy-lane hit prints `FOE`. Either hit sets global `AA88`. On the first subsequent miss only, the branch compares enemy live count `19C94` to player live count `19C70`: enemy `<=` player prints `WINNING`, otherwise `LOSING`, then clears `AA88`; later misses draw neither status. The status panel begins at `(19C74 + 160, viewportHeight - 25)` and uses the source's 83-by-20 panel primitive. Source: complete `0x265CC-0x26787` control flow, lane-count death updates `0x26D0C-0x26D24`, and object-2 strings `+0x339C` (`OUR %3d%%`), `+0x33A8` (`FOE`), `+0x33B0` (`WINNING`), and `+0x33B8` (`LOSING`) from hashed executable `5D723175...B79D6`. `OriginalStrategicInteractiveEncounterSession.CaptureMappedHoverPresentation` and `AdvanceMappedFrame` preserve the scroll-adjusted selection, one-pass aggregate status, and exact count comparison; `StrategicInteractiveHoverUsesSourceLabelsAndOnePassAggregateStatus` covers all output states. The rest of the panel art and event producer remain **Provisional**.

Resolver setup `0x258FC-0x26065` is also the owned presentation boundary for the interactive encounter. Its static object-2 resource references name `BATTLE.PCX` at `0x3250`, `MEN8.CSF` at `0x32CC`, and `WAR_MODE` at `0x32C0`; terminal branches reference the three distinct texts `You Have WON! You can survey the victory.`, `You Have RETREATED! You can survey your battle.`, and `You Have LOST! You can survey your loss.` at `0x32F8`, `0x332C`, and `0x3368`. The complete direct-call census for resolver `0x258FC` contains `0x2A80B`, `0x35ADC`, `0x3B468`, and `0x42363`; only `0x35ADC` is presently mapped to the campaign wrapper `0x35924`. The owned resource census decodes `BATTLE.PCX` as a 1024x728 indexed grass battlefield and `MEN8.CSF` as 723 frames (720 at 90x90 plus 3x9, 39x13, and 57x14); bounded rendered samples show transparent foot and mounted unit imagery against the `BATTLE.PCX` palette. Setup reads `WAR_MODE` as an integer setting, tries display modes only when it is neither absent nor `640`, and after a successful mode change stores `19CC8` as `1024` or `800`. The subsequent control setup at `0x25F08-0x25F36` maps that resolved span to `19C74=160` at 1024, `80` at 800, and leaves it zero otherwise; its three control rectangles then use that separate margin. Resource names, dimensions, string identities, call census, and this resolved-span/margin mapping are **Confirmed** by executable/data extraction and decoding from hashed release `5D723175...B79D6`; the exact `WAR_MODE` capability-selection semantics, backdrop scaling, unit-frame labels, and the three call contexts other than `0x35ADC` remain **Provisional**. `ImportedArt` maps the background as `Encounter.Strategic.Background` and `ImportedAnimations` maps the unit sequence as `Encounter.Strategic.Units`, so source art is verified and available without falsely wiring it through `FieldBattleSession`.

The renderer `0x28AD8-0x28B48` resolves ordinary `MEN8.CSF` frame index as `lane + category + 5 * heading + (phase % 5) + state`: lane is record `+0x2C` (`0` or `0x168`), category is `+0x00` (`0`, `0x78`, or `0xF0`), heading is `+0x14`, phase is `+0x18`, and state is `+0x1C`. The six lane/category combinations, eight headings, five phases, and three observed state blocks account for frames 0-719. Before that pass, `0x28A48` builds a pointer array and invokes the `0x289CC-0x28A47` comparator. It orders non-positive record `+0x20` before positive records, then sorts ascending record `+0x08`, then ascending `+0x04`; exact ties return zero. `UnitDrawsFor` uses authored materialization order as its deterministic compatibility tie-breaker rather than inherit the C library's unspecified equal-item ordering. The renderer calls the CSF painter with `x = record +0x04 - horizontalScroll - 45` and `y = record +0x08 - verticalScroll - 45`, centering each 90x90 ordinary frame on its live coordinates. After that ordinary pass, `0x28B5C-0x28B9D` visits the byte-indexed selected list in list order and draws frame `0x2D0` (720) at `x = record +0x04 - horizontalScroll - 5`, `y = record +0x08 - verticalScroll - 7`; it does not recenter the 9x9 overlay. Setup `0x25FCD-0x26000` draws 57x14 frame `0x2D2` (722) at first control rectangle origin plus `(20,6)`, while armed first-control path `0x268C8-0x268E4` draws the 39x13 frame `0x2D1` (721) there plus `(15,6)`. The first rectangle is `(controlMargin + 400, viewportHeight - 34, 84, 31)`, so these are not generic centered frames. `OriginalStrategicInteractiveEncounterPresentation` preserves these exact frame identities, placement math, ordered ordinary pass, and selected-overlay layer. The first control's retreat route is independently Confirmed by its dialog string; the semantic names of the overlay frames and remaining control artwork remain **Provisional**.

`19C7C`/`19C8C` first-control state is **Confirmed** at `0x26C2D-0x26C53` and `0x26820-0x26887`. Initial `19C7C=1` skips the tactical pass; a first rectangle hit while `19C8C=0` stores `19C7C=0` and `19C8C=1`, enabling the strict timer pass. A later first-rectangle hit instead enters the `Do you want to retreat?` dialog, whose true reply exits and false reply falls through to selection without clearing either global. The earlier claim that activation armed a confirmation and suspended tactical work is **Disproved**. `ActivateMappedFirstControl`, `ResolveMappedRetreatConfirmation`, and corrected frame regressions preserve this state boundary without inventing host event production.

`ConquerorGame.StrategicEncounter` now binds that already-armed first-control dialog instead of leaving its accepted exit unreachable: it retains the clicked control coordinates, pauses the host loop while the synchronous source dialog is pending, then replays raw dispatcher code 3 with the stored coordinates after the host response. Enter supplies the source nonzero reply and reaches the existing resolver settlement; Escape supplies zero and therefore preserves the Confirmed fall-through-to-selection behavior. The replacement's Enter/Escape mapping and text are **Provisional** host policy, while the retained-coordinate replay, true/false dispatcher outcomes, and settlement route are the Confirmed `0x26820-0x26887`/`0x26C1A-0x26C28` behavior described above.

Outer raw dispatcher `0x27F50-0x28087` maps `0x50` and `0x70` to one identical branch: it tests `19C8C`, does nothing while that word is zero, and otherwise XORs `19C7C` by one. Since `0x26C2D` skips tactical work exactly when `19C7C` is nonzero, these values toggle pause/resume only after first-control activation. `ToggleMappedTacticalAdvancementForInputCode` and `StrategicInteractiveRawPauseInputsToggleOnlyAfterFirstControlActivation` preserve that Confirmed state transition with the stable host cadence; the original input producer remains Provisional.

## Interactive encounter live-record amendment

Constructor `0x28C38`'s full remaining initialization is **Confirmed**: every 52-byte unit record begins with `+0x0C/+0x10 = -1`, `+0x18/+0x1C = 0`, and `+0x30 = -1`; `+0x28` begins as zero for player entries and one for enemy entries. The interactive loop uses `+0x18` as a modulo-five phase counter and reaches known death state `+0x1C=0x50`; control path `0x264F8` later overwrites `+0x28`, writes `+0x0C/+0x10` in a pair, and treats `+0x30` as a table-relative record index. Source: constructor loops `0x28D15-0x29049`, death branch `0x26C89-0x26D46`, and control path `0x26918-0x26B4E` in hashed executable `5D723175...B79D6`. `OriginalStrategicInteractiveEncounterUnit` now carries these exact fields as `AuxiliaryX`, `AuxiliaryY`, `PhaseCounter`, `StateCode`, `ControlCode`, and `TargetUnitIndex`; its materialization regression locks their initial values. The semantic meanings of the non-death state codes and the UI actions that mutate these fields remain **Provisional**, so no generalized selection or movement behavior is inferred from their numeric values.

## Interactive encounter destination-order amendment

One selected-unit control path is **Confirmed**. Event branch `0x26A7F-0x26B64` iterates the byte-indexed selected-record list in stored order. It clamps the local y coordinate first to a minimum of 45 and then to a maximum of `verticalSpan - 85`; it does not clamp local x. For every selected record it writes `+0x28 = 0`, `+0x0C = localX + horizontalOffset`, and `+0x10 = clampedLocalY + verticalOffset`, then invokes its presentation update. Source: complete static control flow `0x264F8`, especially `0x26A7F-0x26B64`, and constructor layout `0x28C38`; hashed executable `5D723175...B79D6`. `ApplyMappedDestinationOrder` and `StrategicInteractiveDestinationOrderClampsOnlyYAndWritesSelectedLiveRecordsInOrder` preserve the exact pair of writes, selected-list ordering, and asymmetric bounds. The event identity, selected-list creation/toggle logic, presentation updates, and the later per-unit movement state machine remain **Provisional** and are not substituted with a nearest-target or generic formation order.

## Interactive encounter player-selection amendment

Player selection append is **Confirmed** at `0x26999-0x26A77`. After `0x6FF10` returns a unit hit, the path rejects every non-player lane (`+0x2C != 0`) and non-positive strength (`+0x20 <= 0`). It searches the current byte-indexed selected-unit list for the unit's zero-based index, leaves an existing entry unchanged, and only otherwise increments the count/appends the index. Thus this hit path is not a selection toggle. Source: complete static control flow `0x264F8`, selector `0x6FF10`, and unit layout `0x28C38`; hashed executable `5D723175...B79D6`. `TryAppendMappedPlayerSelection` and `StrategicInteractivePlayerSelectionAppendsOnlyLivingPlayerUnitsWithoutToggle` implement and test those rules. The selected-list presentation, capacity allocation, removal/control commands, and display event identities remain **Provisional**; no unproven click-toggle behavior is introduced.

The keyboard-driven category append loops are **Confirmed** at `0x2814F-0x283CC`. The three independent branches scan every 52-byte record in authored order, first reject records already named in the byte-indexed selection list, then require player lane `+0x2C = 0`, category `+0x00` of swordsman `0`, knight `0xF0`, or halberdier `0x78` respectively, and strictly positive `+0x20` strength. Every accepted index appends to the existing list; no branch clears it or applies a screen-position sort. Outer raw dispatcher `0x27ED2-0x27F9C` reaches those loops from raw return values `0x48/0x68` for swordsmen, `0x4B/0x6B` for knights, and `0x53/0x73` for halberdiers. Source: complete static control flow `0x27ED2-0x283CC`, constructor record layout `0x28C38`, and selected-list writes through `19C84` from hashed executable `5D723175...B79D6`. `RouteMappedCategorySelectionInputCode`, `AppendMappedPlayerCategorySelectionForInputCode`, and focused regressions preserve those distinct raw routes, filters, and order. The operating-system/input-library event producer and selection-capacity fault behavior remain **Provisional** and are deliberately not guessed or reproduced.

The interactive resolver's terminal contract is **Confirmed**. After dispatcher `0x264F8` returns, `0x2808E` first returns `2` only when enemy-lane total `19C94` equals zero; otherwise `0x283E2` returns `1` only when player-lane total `19C70` equals zero, and `0x283FA` returns zero to continue. An accepted already-armed retreat confirmation also returns `1` directly from `0x26C1A-0x26C28`. Caller `0x260D7-0x26169` decrements that raw result before branching: raw `2` selects its enemy-exhausted result path, while raw `1` inspects `19C70` again to distinguish surviving withdrawal from player defeat. Thus simultaneous zero totals take the enemy-exhausted path because `19C94` is tested first. Source: resolver loop `0x26B88-0x283FC` and caller `0x258FC-0x26169` in hashed executable `5D723175...B79D6`. `OriginalStrategicInteractiveEncounterOutcome`, session terminal latching, and `StrategicInteractiveFrameReturnsMappedLaneOutcomesInSourceOrder` preserve the three application-facing outcomes without pretending the unrecovered caller text/presentation is a direct game-screen binding.

Wrapper `0x35924:0x3593F-0x35967` supplies resolver argument seven rather than leaving it as a generic host policy. It reads dynamic `CHARACTR.DAT` fields 5 (`HONOR`) and 6 (`FAME`) through `0x15EF0(0, field)`, adds `0x14` only when its player-record argument equals source global `AE6C`, then performs signed division by six. Its `0x35ABB-0x35ADC` call supplies that quotient as the player modifier and literal zero as the enemy modifier. `OriginalStrategicEncounterStaging.PlayerScoreModifier` and `Campaign.OriginalStrategicEncounterPlayerScoreModifier` preserve this formula using the persisted distinguished slot; the encounter screen captures it once before either automatic or interactive resolution. Source: direct disassembly of `0x35924`, the decoded 30-attribute `CHARACTR.DAT` labels, and the `AE6C` player-record contract in hashed executable `5D723175...B79D6`. This formula and its `+20` boundary are **Confirmed**.

Raw outer dispatcher values `0x12D` and `0x16B` both branch to `0x283CE`: that path writes `ADC0=1` and returns raw result one. The caller then checks player-lane total `19C70`: a nonzero total presents withdrawal, while zero selects player defeat. `ResolveMappedRawResolverExit` therefore exposes these Confirmed values as `PlayerWithdrew` only for a surviving player lane and `PlayerDefeated` otherwise; session latching supplies the same no-re-entry boundary without leaking a global into a future screen binder. Their physical event source remains Provisional.

Four additional raw branches in outer dispatcher `0x27ED2-0x2814A` are **Confirmed** but remain deliberately unnamed. Values `0x57` and `0x111` enter `0x280AA`; only when object-2 bytes `20C36`, `20C2A`, and `20C38` are all nonzero does the branch set every living enemy-lane record (`+0x2C = 0x168`) strength `+0x20` to 20 and store `19C98 = 1`. Values `0x0C` and `0x4C` enter `0x28119`; under the same three-byte gate they store `19C98 = -1` without rewriting strengths. Resolver initialization `0x258FC-0x25955` sets `19C98` to zero. Contact arithmetic `0x26DCB-0x26E0C` tests value 1 before player-lane damage and -1 before enemy-lane damage. `OriginalStrategicInteractiveEncounterRawInputGates`, `ApplyMappedRawContactFilterInput`, and the session-owned filter preserve these exact mutations without guessing the gate bytes' higher-level ownership or physical event source. Source: regenerated disassembly of `0x258FC`, `0x26DCB`, and `0x27ED2` plus fixup-backed object-2 data references from hashed executable `5D723175...B79D6`.

Interactive resolver settlement is **Confirmed** through the same wrapper boundary as automatic settlement. Wrapper `0x35924` passes only staged counters to `0x258FC`; after every resolver return it restores each player category's withheld quantity, but writes the three resolver-owned hostile counters directly. Terminal scan `0x263C4-0x26477` counts only positive-strength units by side/category before those wrapper writes. `ResolveInteractiveOriginalStrategicEncounter` applies a terminal `OriginalStrategicInteractiveEncounterSession` through that exact reserve/direct-write split, then retains `0x39428`'s established empty ordinary-field removal versus distinguished-player modal result. `InteractiveStrategicEncounterRestoresPlayerReservesAndWritesTerminalSurvivors` covers the staged-reserve and terminal-survivor mapping. UI/screen binding remains a separate application concern.

Two `ControlCode` writes in the pointer/control dispatcher are **Confirmed** but their UI semantics are not. At `0x26918-0x26966`, a nonempty selected byte-index list is traversed in list order and every named record receives `+0x28 = 1`; it does not test strength or lane again. At `0x26968-0x26999`, the complete 52-byte record table is traversed and every record with `+0x20 > 0` receives `+0x28 = 1`, including enemy-lane records. Outer raw dispatcher `0x27ED2` additionally reaches the selected-list branch only from raw value `0x61` and the living-record branch only from raw value `0x41`. Source: complete `0x264F8` and `0x27ED2-0x28072` control flow plus constructor record layout `0x28C38` from hashed executable `5D723175...B79D6`. `SetMappedControlCodeOneForSelectedRecords`, `SetMappedControlCodeOneForLivingRecords`, `ApplyMappedRawControlCodeOneInput`, and focused regressions preserve/test the distinct source filters without calling either path a particular tactical action. Button regions, operating-system/input-library event production, and the state-machine meaning of `+0x28` values remain **Provisional**.

The resolved damage portion of live state `+0x1C = 0x28` is **Confirmed**. Once its prior neighbor helper has returned a nonzero/non-one result and phase `(prior + 1) % 5` equals two, `0x26DCB-0x26FC2` subtracts raw random remainder modulo 10 for the directed category pairs knight→halberdier, swordsman→knight, and halberdier→swordsman; every other pair uses modulo 7. A target in lane `+0x2C = 0x168` additionally loses `19CA0 / 3`, the resolver's player score modifier, while a player-lane target does not. Global `19C98 = 1` suppresses contacts against lane zero and `-1` suppresses contacts against lane `0x168`; its other values follow the normal path. The subtraction is not clamped. At `0x2704F-0x270B4`, a target strictly below 20 but above zero enters death state `0x50` with phase zero; a non-positive target clears the attacker's `+0x30` and state, and clears the attacker's `+0x0C/+0x10` only when its control code is one. Source: complete static control flow `0x26DCB-0x270B4`, constructor lane/category layout `0x28C38`, and resolver initialization `0x258FC-0x2595F` from hashed executable `5D723175...B79D6`. `OriginalStrategicInteractiveEncounterCombat.ApplyMappedResolvedContact` and focused regressions preserve this exact arithmetic after an explicit caller-provided target; the neighbor probe sequence, phase scheduling, action semantics, and presentation/audio side effects remain **Provisional**.

The preceding state-`0x28` probe branch is also **Confirmed**. `0x26D4B-0x26DA4` calls neighbor helper `0x28408` and treats only return values zero and one as failure: it writes state zero and target index `-1`, clearing auxiliary coordinates only when control code is one. For every other signed return, `0x26DA4-0x26DC5` advances the phase as `(prior + 1) % 5`; resolved contact arithmetic is entered only for resulting phase two. Source: complete static control flow `0x26D4B-0x26DC5` and helper `0x28408-0x289C9` in hashed executable `5D723175...B79D6`. `ApplyMappedContactProbeResult` and `StrategicInteractiveContactProbeUsesOnlySourceValuesOtherThanZeroAndOne` preserve/test this state split with a caller-supplied raw probe result; `ProbeMappedNeighborContact` maps the helper's exact corner returns. The tactical caller's choice of deltas and the higher-level state meanings remain **Provisional**.

The constructor's record `+0x14` is now identified as a mutable eight-way heading, not an immutable type code. `0x28D65`/`0x28EFA` initialize it to three for player-lane records and seven for enemy-lane records. Heading helper `0x2964C` maps source-to-target coordinates to octants `0..7` in clockwise screen order beginning at lower-left: it uses the vertical-axis result for equal or target-X-minus-one coordinates, and the horizontal-axis result for equal or target-Y-minus-one coordinates. Target helper `0x296B4` reads the actor's `+0x30` target record, enters state `+0x1C=0x28` with phase `+0x18=0` when heading already equals that result, or otherwise changes `+0x14` by one cyclic octant. It chooses increment only when its unsigned distance is strictly shorter; equal distances decrement, and decrement wraps zero to seven. Source: constructor `0x28D15-0x29049`, and complete helpers `0x2964C-0x2979F`, from hashed executable `5D723175...B79D6`. `HeadingOctant`, `DetermineMappedHeadingOctant`, `AdvanceMappedTargetHeading`, and `StrategicInteractiveTargetHeadingUsesExactOctantsAndTieBreaking` preserve this **Confirmed** record correction. State-zero acquisition is now also **Confirmed** at `0x270CB-0x27474`: it temporarily replaces the source rectangle with one-by-one geometry at `(contentWidth + 1, contentHeight + 1)`, probes its four corners top-left/bottom-left/top-right/bottom-right, and accepts only a living opposite-lane record. A valid hit alone writes `+0x30`; same-lane and non-positive hits do not. After a first-corner hit, a miss at a later corner terminates; after a first-corner miss, later misses continue. `TryAcquireMappedOpposingTarget` and `StrategicInteractiveStateZeroTargetAcquisitionUsesItsOwnCornerAndLaneRules` preserve that precise boundary. The no-target paired-destination path is now **Confirmed** at `0x27479-0x27C59`: if either `+0x0C/+0x10` remains, it first turns one octant toward that coordinate and, once aligned, processes Y before X in the same pass. A near axis uses the exact remaining displacement only when its absolute value is strictly below 10; otherwise knights probe/move 10 pixels and other categories probe/move 5. Every probe uses the retained pre-pass selector table and `0x28408`; a living opposing contact advances the coordinate, retains the target, and invokes `0x296B4`, while a same-lane contact writes control code one and either clears a near axis or reverses a far target by five. Positive reversals below zero reset to dimension minus 90; negative reversals reaching dimension minus 90 reset to 90. Successful coordinate work advances phase modulo five, but the heading helper may then reset phase to zero upon immediate contact. `AdvanceMappedDestinationOrder` and `StrategicInteractiveStateZeroDestinationWalksAxesAndPreservesCollisionBranches` preserve these conditions. The automatic no-target/no-destination branch is also **Confirmed** at `0x27CE7-0x27E16`: only a living control-code-one record enters when both lane counters `19C94/19C70` are positive. It scans all 52-byte records in authored order, skips self, same lane, and non-positive strength, computes `truncate(sqrt(dx*dx + dy*dy))` using `0x63EC0`, and replaces the selection only for a strictly smaller value than initial `0x7FFF`; ties retain the first record. If selected record `+0x0C` is minus one or equals the source live X, source `+0x0C` receives its current X; otherwise the selected record's `+0x0C` becomes the source current X. Source `+0x10` receives selected live Y only when its `+0x10` is minus one, otherwise its stored Y destination. The complete state-zero ordering is **Confirmed**: `0x270CB` always runs its corner acquisition, then a non-minus-one `+0x30` calls `0x296B4` and exits the unit pass; only a minus-one target may continue to paired destination handling, and only absent paired coordinates reach `0x27CE7`. `AdvanceMappedStateZero` and `StrategicInteractiveStateZeroComposesTargetDestinationAndAutomaticBranchesInOrder` retain that precedence over one pre-pass rectangle table. `TryAssignMappedAutomaticDestination`, session-owned lane counters, and `StrategicInteractiveStateZeroAutomationSelectsTheFirstNearestOpposingRecord` preserve the automatic path. The source uses stale register state if its global lane gate is inconsistent yet no candidate is found; the clean-room path safely refuses that impossible assignment rather than recreating the fault. Broader scheduler/presentation behavior remains **Provisional**.

The surrounding fixed unit pass is also **Confirmed** at `0x26C81-0x27E16`: it uses the one rectangle table emitted before input dispatch, visits records in authored order, advances a state-`0x50` phase unless already four and completes its zero/category/lane-counter mutation only on resulting phase four, then applies state-`0x28`'s `0x28408(0,0)` probe and due phase-two contact before testing state zero again in the same pass. `OriginalStrategicInteractiveEncounterSession.AdvanceMappedTacticalPass` and `StrategicInteractiveTacticalPassUsesOneSnapshotAndReentersStateZeroOnlyAfterContactClears` preserve this sequence with a caller-owned stable timing gate. Broader input/presentation scheduling remains **Provisional**.

The complete neighbor helper `0x28408` is **Confirmed**. It saves the source rectangle's signed `(left, top, width, height)` words, temporarily replaces it with one-by-one geometry at `(contentWidth + 1, contentHeight + 1)`, restores the saved words on every return, and probes the four offset corners in this order: `(left + deltaX, top + deltaY)`, `(left + deltaX, top + height + deltaY)`, `(left + width + deltaX, top + deltaY)`, then `(left + width + deltaX, top + height + deltaY)`. Any nonzero selector result writes the supplied target word as its zero-based record index before testing strength. A living candidate returns one for the source lane `+0x2C` and two for the other lane. There is a material asymmetric fallback: when the first corner hits a dead record, a miss at any later corner returns zero immediately; when the first corner misses, later misses continue through the remaining corners. Dead hits continue to the next corner in either chain. This is therefore neither an area-overlap nor nearest-unit query. Source: complete static control flow `0x28408-0x289C9`, selector `0x6FF10`, and predicate `0x64164` from hashed executable `5D723175...B79D6`. `OriginalStrategicInteractiveEncounterGeometry.ProbeMappedNeighborContact` accepts the independent source selector table (with a render-table convenience overload), and `StrategicInteractiveNeighborProbeUsesOrderedCornersAndItsDeadFirstHitAsymmetry` tests the temporary source exclusion, corners, zero-based output, lane values, and asymmetry with retained dead-record geometry. The tactical-loop shell's choice of offsets and later movement state remain **Provisional**.

## Strategic target confirmation amendment

Both target-hit branches in map dispatcher `0x122AC` share an exact response boundary. Enemy hit `0x123FD-0x1241B` and temporary-division hit `0x124F7-0x12515` call modal helper `0x256A0` with prompt selector `0x364`; only return value one commits the selected record target. Acceptance writes `slot | 0x1000` for an enemy or `slot | 0x10000` for a temporary force, then clears route-input, waypoint, and cursor state. `OriginalStrategicMapDispatchResult.RequiresTargetConfirmation` carries precisely those two hit families. `ConquerorGame` retains the original raw pointer while the host modal is pending and repeats dispatch with `targetConfirmed: true` only after Enter; Escape declines without mutation. The source prompt text and physical dialog events remain **Provisional**, so the replacement's wording and Enter/Escape bindings are explicit host policy. Source: complete static control flow `0x122AC-0x12559` in hashed executable `5D723175...B79D6`.

## Interactive encounter session integration amendment

The recovered constructor, formation, rectangle, selection, destination, state-zero, contact/death fixed-pass, and timing boundaries now compose in `OriginalStrategicInteractiveEncounterSession`. This is an implementation integration, not a new executable claim: its source facts remain the documented constructor `0x28C38`, formation paths `0x2904B-0x29643`, presentation/control loop `0x264F8-0x26C58`, selector `0x6FF10`, state loop `0x26C81-0x27E16`, and timer helper `0x18420` from hashed executable `5D723175...B79D6`. `StrategicInteractiveSessionCombinesMappedFormationPlayerControlGeometryAndCadence` and `StrategicInteractiveTacticalPassUsesOneSnapshotAndReentersStateZeroOnlyAfterContactClears` verify those parts operate together. Remaining action-state meanings, input/button dispatch, and presentation scheduling remain **Provisional**; the session intentionally does not substitute a generic field battle for them.

The interactive-session construction boundary now explicitly consumes `OriginalStrategicEncounterPreparation`: it materializes only `PlayerResolverForces` and `EnemyResolverForces`, leaving `PlayerReservedForces` outside the live table until `Campaign.ResolveInteractiveOriginalStrategicEncounter` restores them during terminal write-back. `Campaign.BeginInteractiveOriginalStrategicEncounter` first confirms that the captured player/enemy snapshot still matches the live strategic records, then creates that staged session; it is the typed application boundary for a future presentation host. This is an implementation traceability refinement, not a new source claim; it enforces the established `0x35924` six-counter staging and prevents a host from rendering/resolving player reserves as active encounter units. `InteractiveStrategicEncounterRestoresPlayerReservesAndWritesTerminalSurvivors` verifies the 102-force input stages to 60 player units plus three hostile units before the existing reserve-restoring settlement.

## Strategic confirmation scheduler amendment

The encounter handoff spans a different boundary in the replacement. Source player pass `0x13168` calls wrapper `0x39428` synchronously; wrapper `0x3952D` enters resolver `0x35924` and returns only after its six-counter settlement, before caller `0x3C290` tests modal `AEF8` and calls hostile scheduler `0x3C088`. The host must present that encounter in a later UI update. `Campaign.AdvanceOriginalStrategicPass` therefore captures the live player/enemy counters after the player pass and defers the hostile scheduler for the pass that emits a new encounter, preserving those records for `BeginInteractiveOriginalStrategicEncounter`'s snapshot validation. A pass with an already-active contact handoff and no new contact still follows its explicit modal input. This scheduler deferral is a **Provisional host ordering adaptation** to the source's **Confirmed synchronous call order**, not a claim that the original always skipped `0x3C088` after combat. `NewlyCapturedStrategicEncounterKeepsCombatantsStableForTheDeferredModal` covers the new boundary. Source: disassembly `0x39428`–`0x39532` and `0x3C290`–`0x3C2C7` from hashed executable `5D723175...B79D6`.

The host binding for map target confirmation now preserves the caller `0x3C290` ordering already established by direct control-flow analysis: its report and player-record work runs before the `AEF8` modal guard, and that guard skips only hostile scheduler work. During a pending map target confirmation, `ConquerorGame.UpdateMap` therefore performs `OriginalStrategicHostRuntime.AdvanceFixedPass` first with `SchedulerBlockedByModal`, then consumes the host confirmation response; it does not freeze the entire strategic fixed pass. `OriginalStrategicHostRuntime.AdvanceFixedPass` forwards that guard in addition to the independently Confirmed unavailable-fallback and active-contact guards. `HostFixedStrategicPassKeepsReportingAndPlayerUpdatesRunningDuringAModal` pins the application boundary. Source: `0x3C290` control flow and modal global `AEF8` in hashed executable `5D723175...B79D6`; the replacement's Enter/Escape dialog event producer and wording remain **Provisional**.

## Temporary-force record amendment

The three temporary-force records at object-2 `+0x1A150` are **Confirmed** as three `0x118`-byte slots. Map dispatch `0x12492` tests record `+0x00` and a 40-by-40 target rectangle at `+0x5C/+0x60`; player target acceptance retains its index with flag `0x10000`. Lifecycle routine `0x3B0E4` writes active `+0x00`, route completion/count/cursor `+0x0C/+0x18/+0x30`, force counters `+0x1C/+0x20/+0x24`, origin/lord/mode `+0x28/+0x2C/+0x34`, target/grid `+0x3C..+0x48`, and current/direction `+0x5C..+0x68`; its update path passes the same map coordinates to `0x3F0A0`. `OriginalStrategicTemporaryForceSlot` persists that state in physical source order. The fixed pass runs initialized automatic patrols before report/player work, and player division targeting reads those post-update live records rather than a stale host snapshot. `TemporaryAutomaticPatrolUsesTheSourceCursorLoopAndKeepsLifecycleOwnership`, `HostFixedStrategicPassAdvancesTemporaryPatrolsBeforeUsingTheirTargets`, `StrategicPassConsumesTheTwoMappedTemporaryCreatorActionsAfterSchedulerWork`, and `SchemaTwoStrategicStateRoundTripsEveryReplacementStateFamily` pin cursor movement, creation, ordering, and persistence. The source conversation triggers, expiry gate, and marker-frame selector are mapped below; conversation availability and force resolution remain **Provisional**. Source: static references and control flow `0x10B46`, `0x12492`, `0x3B0E4-0x3B890` in hashed executable `5D723175...B79D6`.

Two fixed creator wrappers make their descriptor constants **Confirmed**. `0x3B97C` and `0x3B9B4` each construct a 32-byte descriptor and call `0x3B0E4`; both write dwords `+0x00=1`, `+0x04=1`, `+0x08=0x7D0`, and `+0x10=7`, while `+0x1C` is respectively 1 and 2. `0x3B0E4` copies the descriptor's origin to record `+0x28`, obtains its lord through `0x4377C` into `+0x2C`, writes mode 2 at `+0x34`, calls inclusive random helper `0x24C38(1)` for swordsmen `+0x1C`, calls it again then adds one for halberdiers `+0x20`, and clears knights `+0x24`. Main continuation `0x3C2D6` checks globals `A92C` then `A930`, clears each set flag, and invokes wrappers 1 then 2 after the ordinary strategic pass. `OriginalStrategicTemporaryForces`, `ProcessTemporaryForceActions`, and the campaign pass preserve those raw values, action order, and post-scheduler random boundary. The source conversation triggers for these two UI actions are mapped below. Source: direct disassembly `0x3B0E4-0x3B1E6`, `0x3B97C-0x3B9F0`, `0x3C2D6-0x3C301`, and `0x24C38-0x24C4B` in hashed executable `5D723175...B79D6`.

The pending-creator flags have a further **Confirmed** UI boundary. The strategic UI event routine first polls action id `0x80` through the live object at `+0xA928`. Only for a nonzero response does it write action `0x81 = 1`, action `0x80 = 0`, pass `0x2D84` to `0x21E88`, and call `0x5B0D4(0x18B, 1)`. It then tests action id `0x2B`; a nonzero response writes `A92C = 1` at `0x2225C`. Finally it independently tests action id `0x5D` and writes `A930 = 1` at `0x22280`. Thus the main-pass wrappers consume two one-shot actions raised by the strategic UI, rather than a timer or hostile-scheduler result, and their polls occur after the conditional `0x80/0x81` pair. `OriginalStrategicCampaignPassInput.TemporaryForceActionIds` deliberately exposes the numeric action boundary without attaching an invented label/control. The source conversations that set actions `0x2B` and `0x5D` are mapped below; action availability predicates, actions `0x80`/`0x81`, and selector `0x2D84` remain **Provisional**. Source: regenerated LE-aware static control flow `0x221C4-0x2229F` and consumer `0x3C2D6-0x3C301` in hashed executable `5D723175...B79D6`.

Selector runner `0x21E88` is also now bounded. Its only three direct callers are the strategic action-`0x80` branch above, with selector `0x2D84`, and two paths at `0x1082D`/`0x108D6` in the shared `0x1074C` control routine, both with selector `0xA4`. The runner brackets its work with `0x5B2C0`, first invokes `0x5B0D4(0x17C,1)`, creates the selector interaction through `0x5877C(selector,1,1,2)`, and returns that interaction's result. The two `0xA4` callers both redraw through `0x1070C` before testing whether the returned value equals one; the `0x2D84` caller discards the result before issuing its separate `0x18B` action. This establishes shared selector creation and the observed return-one branch without assigning labels, control geometry, or narrative meaning to either selector. Source: direct-caller census for `0x21E88`, and LE disassembly `0x21E88-0x21EEA` plus `0x1074C-0x109AA`, in hashed executable `5D723175...B79D6`; selector meanings remain **Provisional**.

## Village presentation binding

`VOPTS.HAT` is the 640-by-480 Village exterior descriptor (screen 11) and names `TOWN_Y.PCX` as its background. Its enabled regions 1 through 5 are respectively `(541,347,100,54)`, `(157,166,73,39)`, `(388,229,43,30)`, `(225,257,35,19)`, and `(489,192,52,253)`. Independently decoded `VILLAGE.DAT` entries repeat the ordered Map, Inn, Smith, Lender, Church hotspot vocabulary after each background name, corroborating that regional order without assigning meaning to the descriptor's extra regions 0 and 6. `ImportedArt`, `ImportedLayouts`, `VillagePresentationDefinitions`, and `DrawVillage` therefore require and render `TOWN_Y.PCX`; Map, Inn, Blacksmith, and Church use the decoded region bounds, while the lender stays on its existing keyboard-only shortcut because the original separate lender dialogue/transaction path is not yet mapped. `VillagePresentationUsesTheDecodedTownBackgroundAndHotspotOrder` pins the resource and hit ordering. Source: bounded decode of owned `VOPTS.HAT` and `VILLAGE.DAT` in the hashed release; background and geometry are **Confirmed**, region vocabulary is **Corroborated**, and lender-flow semantics remain **Provisional**.

The complete exterior catalogs are now structurally mapped. `VILLAGE.DAT` has 67 source records and `TVILLAGE.DAT` has 12. Each is seven CRLF-delimited ASCII lines: a `V<decimal>_<four 0/1 digits>.PCX` or `T<decimal>_<four 0/1 digits>.PCX` background name, followed by exactly six `enabled,x,y,width,height ; label` rows. Flags are binary; six rectangles extend one to three pixels beyond the 640-pixel right edge, and 14 referenced backgrounds are absent from the owned import. `VillageSceneCatalogDecoder` and `ImportedContentCatalog.LoadRequired` preserve the framing and validate image presence for the seven mapped starting homes, while a malformed catalog still fails before graphics startup. Static UI flow `0x6076C-0x608C5` calls the common loader after selecting `TVILLAGE.DAT` when object-2 `+0xDED4` is nonzero and `+0x1B0C4 != +0x1C180`, and `VILLAGE.DAT` otherwise. The following call `0x43080` multiplies its person argument by 18, returns byte `object-2:+0xBA5F + person * 18` (person record `+0x0F`), and reports an error above 66. Thus person `+0x0F` is the zero-based VILLAGE exterior index, not an unnamed state: start-home persons 17/18/24/29/86/144/166 select indices 27/21/33/39/36/35/54 respectively. `OriginalStrategicPersonDefinition.VillageSceneIndex`, `OriginalVillageScenePresentation`, and `OriginalStartingHomesUseTheirPersonVillageSceneIndex` retain the exact field and use it only for an imported new-game home at generic location zero; later generic travel locations and the tournament global meanings remain intentionally unmapped. Source: direct bounded decode of owned GOB entries 450/451 plus static disassembly `0x6076C-0x608C5`/`0x43080` in hashed release `5D723175...B79D6`; structural format, VILLAGE selection field, and these starting-home indices are **Confirmed**, while general location/tournament selection remains **Provisional**.

The verified GOG catalog has three separator defects at one-based `VILLAGE.DAT` lines 115 (doubled comma), 131 (trailing comma), and 265 (spaces in place of commas), plus six right-edge extents of 641 or 643. `VillageSceneCatalogDecoder` accepts only these exact source spellings and a maximum authored right edge of 643; its synthetic regression rejects an extent of 644. A complete manifest comparison finds 13 absent `VILLAGE.DAT` backgrounds and one absent `TVILLAGE.DAT` background, while all seven indexed starting-home images exist. `ImportedContentCatalog.ValidateVillageSceneCatalog` therefore checks the complete structures and the seven runtime-mapped starting-home images without claiming closure for the source catalogs. These defects and population counts are **Confirmed** by direct comparison of owned GOB entries 450/451 with the verified local import manifest; the missing source images' intended original fallback behavior remains **Provisional**.

The selected VILLAGE record itself supplies the enabled exterior geometry. Its source action rows carry `Map`, `Tournament`, `Inn`, `Smith`/`Blacksmith`, `Lend`/`Lender`, and `Church` labels with 640-by-480 rectangles. `VillagePresentationDefinitions.HotspotsFrom(VillageSceneDefinition)` preserves the catalog order and coordinates for those mapped labels, excluding only disabled rows. `ConquerorGame` caches those regions by selected background and uses them for both click hit testing and hover labels, rather than retaining `VOPTS.HAT` coordinates over a different background. `SelectedVillageSceneUsesItsEnabledCatalogRegions` pins both label-spelling pairs, enabled Tournament inclusion, exact source bounds, and hit results. The label vocabulary and rectangles are **Confirmed** by direct catalog decoding.

The Tournament exterior row’s gate is now partially mapped. The catalog loop creates six controls in source row order, and the following handlers at `0x60BE0`, `0x60C64`, `0x60CEC`, `0x60D2C`, `0x60D54`, and `0x60D9C` are correspondingly the Tournament, Map, Inn, blacksmith, lender, and church routes. At UI entry, `0x60812-0x6083F` calls `0x5C614(current person)`; this compares the identity with `object-2:+0xDC30+4`, and only equality raises `DED4`. That flag chooses the tournament catalog subject to the separate home/current-person inequality. `0x5C648` saves the value from calendar accessor `0x38678` at `object-2:+0xDC30+8`; Tournament handler `0x60BE0` checks `0x5C62C`, which is nonzero precisely when the current accessor value differs. The eventual target/action code and the meanings of both `DC30` fields and the calendar component are still **Provisional**, so the runtime continues to omit the Tournament hotspot rather than infer a generic availability rule. Source: static control flow `0x6076C-0x60C61`, `0x5C614-0x5C67C`, and `0x38678` in hashed executable `5D723175...B79D6`; control order and gate data flow are **Confirmed**.

Tournament route correction: the previous statement that the runtime omitted the source Tournament hotspot is superseded. The catalog loop creates six controls in source row order, and handlers `0x60BE0`, `0x60C64`, `0x60CEC`, `0x60D2C`, `0x60D54`, and `0x60D9C` are respectively Tournament, Map, Inn, blacksmith, lender, and church. At UI entry, `0x60812-0x6083F` calls `0x5C614(current person)`, which compares that identity with object-2 `+0xDC30+4` and raises `DED4` only on equality. `0x5C648` saves calendar accessor `0x38678` at object-2 `+0xDC30+8`; `0x5C62C` is nonzero exactly when that accessor value changes. On the nonzero-`DED4`, equal-accessor path, Tournament handler `0x60BE0` clears `AAC4`/`DED8` and dispatches source screen action `8`; the other branches invoke common modal helper `0x25004` with distinct source text selectors. The replacement retains the enabled source rectangle and uses `Campaign.IsTournamentHere` as explicit host availability policy. It does not claim the unresolved `DC30` fields, accessor component, or modal text are recovered. Source: static control flow `0x6076C-0x60C61`, `0x5C614-0x5C67C`, `0x60BE0-0x60C61`, and `0x38678` in hashed executable `5D723175...B79D6`; control order, action-8 route, and gate data flow are **Confirmed**; field meanings and modal text remain **Provisional**.

Temporary-force expiry is now **Confirmed**. After the active record's `0x4A61C` route update, lifecycle `0x3B841-0x3B865` calls `0x3866C` and `0x38684`. The helpers return calendar-object `+0x18` and `+0x20`; updater `0x3879C-0x38850` proves the former is zero-based month `0..11` and the latter is year. Each is independently compared with the creator descriptor's `+0x04=1` and `+0x08=2000`. Only month `>= 1` **and** year `>= 2000` clears descriptor-active `+0x14`, force-active `+0x00`, and releases route `+0x6C`; January does not satisfy the month gate even in a later year. This is not a lexicographic date comparison. `OriginalStrategicTemporaryForces.ExpiryMonth`/`ExpiryYear` and `AdvanceTemporaryForcePass` preserve the post-update release operation through the campaign date, while `TemporaryForceExpiryRunsAfterItsMovementAndUsesTheIndependentMonthAndYearGates` covers the February-2000 and January-retention boundaries. The ordinary age-limited campaign cannot reach year 2000, but valid extended saves retain this mapped cleanup. Action labels/availability, force resolution, and descriptor-zero route/payload remain **Provisional**. Source: direct disassembly `0x3B831-0x3B903` and `0x3866C-0x38856` in hashed executable `5D723175...B79D6`.

Temporary-force marker semantics are now **Confirmed**. In its no-descriptor lifecycle branch, `0x3B0E4` scans the three active `0x118`-byte records at object-2 `+0x1A150` in physical slot order, converts each live `+0x5C/+0x60` coordinate toward zero, and calls common marker projection `0x3F0A0` with global `AE80` and fixed frame `3`. Initializer `0x12A4C` assigns `AE80` from `icon_men.CSF`, so this pass uses the ordinary icon sequence rather than `marker.CSF`. `OriginalStrategicMapMarkerPresentation.BuildTemporaryForceDraws`, `OriginalStrategicMarkerPresentation.BuildTemporaryForceBlits`, and MonoGame map presentation preserve the active filter, source ordering, truncation, frame, and shared clipping. `TemporaryForceMarkerDrawPlanUsesFrameThreeAndPhysicalSourceOrder` and `StrategicTemporaryForceMarkersUseTheSharedIconFrameAndClipping` cover the mapping. Descriptor-zero behavior and original inter-pass draw ordering remain **Provisional**. Source: static control flow `0x12A4C-0x12AAE` and `0x3B904-0x3B943` in hashed executable `5D723175...B79D6`.

`marker.CSF` overlay semantics are now **Confirmed** for player route input. When route-input global `AE58` is set and modal `AEF8` is clear, `0x11EE3` renders the selected player record's current position and its ordered inline waypoints through helper `0x11CC0`. Each segment shifts its destination x coordinate by `0x12`, rasterizes by major axis with the integer error update in `0x11CC0`, and emits a marker only when either coordinate differs by more than `0x14` from the prior emitted point. The marker image handle is `AE84`; initializer `0x12A4C` loads it from `marker.CSF`, and each dot chooses `frameCounter % 4`. Source increments the initial counter every third unrestricted display pass, so its wall-clock animation rate is processor-dependent. `OriginalStrategicRoutePreview`, `BuildRoutePreviewBlits`, and the host marker-overlay pass preserve the input gate, route order, offset, rasterization, spacing, frame sequence, and common clipping; host phase advancement instead occurs every third explicit fixed update. `StrategicRoutePreviewUsesSourceOffsetSpacingAndFrameCycle` and `StrategicRoutePreviewUsesTheFourFrameMarkerSequenceAndSharedProjection` cover the recovered mapping. Source: static control flow `0x11CC0-0x11EE2`, caller `0x11EE3-0x12040`, and initializer `0x12A4C-0x12AAE` in hashed executable `5D723175...B79D6`.

The two fixed temporary-force creators have executable route identities. `0x3B97C` passes descriptor index 1 to constructor `0x3B0E4`, and `0x3B9B4` passes index 2. That constructor forwards the index as the third argument of route loader `0x49ED0`: index 1 selects `scot.rat`, index 2 selects `wales.rat`. Both decode as the standard `count + signed (x,y)` route framing, with respectively 44 and 42 points; their first/last pairs are `(6656,405)`/`(6607,379)` and `(5668,2201)`/`(5618,2174)`. Loader `0x49ED0` stores the route allocation at record `+0x6C`, count at `+0x18`, current coordinates from its first pair at `+0x5C/+0x60`, and projected grid at `+0x44/+0x48`, while clearing completion `+0x0C`, path-direction `+0x10`, and cursor `+0x30`. Update helper `0x4A61C` advances with the recovered strategic terrain/speed math, loops from the final point back to cursor zero, and on its terminal route failures sets completion then clears cursor/count without deactivating the record; surrounding `0x3B0E4` lifecycle remains responsible for expiry. `OriginalStrategicTemporaryForces`, the required-import manifest, and `ImportedOriginalStrategicResources` bind both named resources eagerly. `AdvanceTemporaryForcePass` runs those initialized slot-1/2 paths at the source pass boundary; slot zero and uninitialized records are retained as live targets/markers without a guessed route. `TemporaryAutomaticPatrolUsesTheSourceCursorLoopAndKeepsLifecycleOwnership` and `HostFixedStrategicPassAdvancesTemporaryPatrolsBeforeUsingTheirTargets` cover the state transition and ordering. The source action producers, force resolution, and descriptor-zero route/payload remain **Provisional**; the fixed creators and expiry are mapped separately. Source: direct static control flow `0x3B0E4-0x3B1E6`, `0x3B97C-0x3B9EB`, `0x49ED0-0x4A06C`, and `0x4A61C-0x4A955`; bounded decoded owned resources `scot.rat`/`wales.rat` in hashed release `5D723175...B79D6`.

Creator replay is **Confirmed** to be inert while its descriptor is active. Constructor `0x3B0FA-0x3B10B` indexes the 32-byte descriptor by input `+0x1C`, compares descriptor `+0x14` with exactly 1, and returns zero before copying the payload, loading the route, or calling either `0x24C38` random draw. After source expiry clears descriptor `+0x14`, a later action can construct the same slot again. `ProcessTemporaryForceActions` retains this guard; `StrategicPassConsumesTheTwoMappedTemporaryCreatorActionsAfterSchedulerWork` now replays both source action IDs through the campaign pass and checks that neither force is replaced. Source: direct disassembly `0x3B0E4-0x3B1DC` and expiry `0x3B841-0x3B865` in hashed executable `5D723175...B79D6`.

The two creator action producers are now **Confirmed** by joining decoded owned conversation records to the executable's strategic UI poll. In `all.cbf/all.cif`, Albert node 2439's exit response runs action group 2423 after the player accepts the northern campaign against Scottish renegades; `all.tmb/all.tmi` group 2423 is exactly `F4(0,43,1)`. Valletta node 2593's acceptance response runs groups 2552 and 2538 after she assigns the Welsh marauder task; group 2552 is exactly `F4(0,93,1)`. Function 4 writes scope-zero variable values through `DynamixActionInterpreter`/`ImportedConversationActionState`, and the strategic UI at `0x22242-0x2228A` polls decimal 43 (`0x2B`) then 93 (`0x5D`) from the shared `all` action object, accepting any nonzero result. Main continuation `0x3C2D6-0x3C301` consumes their one-shot flags in that order and invokes the `scot.rat`/`wales.rat` fixed creators. `OriginalStrategicHostRuntime.AdvanceFixedPass` now reads the persisted conversation variables and passes those action IDs to `ProcessTemporaryForceActions` on the explicit fixed map pass; the active-descriptor guard prevents repeated UI polling from replacing a live patrol. `HostFixedPassCreatesBothPatrolsFromTheConversationActionVariables` covers both nonzero values, action order, and repeat behavior. The response-to-patrol trigger and order are **Confirmed** for the hashed release; original conversation availability predicates, patrol encounter resolution, and exact UI polling frequency remain **Provisional**. Sources: bounded `all.cbf/all.cif` node and `all.tmb/all.tmi` group decodes, executable `0x22564` shared-object loader, `0x221CC-0x2228A` poll, and `0x3C2D6-0x3C301` consumer.

Temporary-force contact and settlement are now mapped at the executable boundary. Main pass `0x3C293-0x3C2A7` calls `0x3B0E4(null)` before the six-record player update and ignores its return value. The no-descriptor branch visits three `0x20`-byte creator descriptors in physical slot order; for each active descriptor it scans the six `0x118`-byte player records at object-2 `+0x1A4B8` in slot order. A contact requires an active player and both `abs(player.currentX - force.currentX) < 30.0` and `abs(player.currentY - force.currentY) < 30.0`; object-2 double `+0x5654` is exactly `30.0`. Equality on either axis fails. Player slot 5 takes the avatar path: when record cooldown `+0x54` is zero, `0x3B318-0x3B360` focuses the contact, issues its separate brigand notice through `0x256A0`, writes cooldown `0x78 = 120`, and continues to the next player record. A nonzero cooldown suppresses that notice. After the scan, `0x3B831` advances that patrol's route and then checks expiry, so the avatar notice alone does not consume or halt the patrol. Army slots 0-4 instead enter `0x3B365-0x3B468`: the code focuses the contact, writes selected player slot `AE64`, increments scope-zero variable 5, reads force counters `+0x1C/+0x20/+0x24` and the army's three counters through `0x29E58`, and passes all six by reference to shared resolver `0x258FC` with both score-modifier arguments zero. This resolver is the same tactical/automatic subsystem already represented by `OriginalStrategicEncounterStaging` and `OriginalStrategicInteractiveEncounter`, but the patrol caller uses its own direct counter staging and settlement. These order, identity, strict-distance, cooldown, and call-argument facts are **Confirmed** for hashed executable `5D723175...B79D6`; there is no source-grounded processor-stable contact frequency, so the replacement should evaluate them on its explicit fixed pass. `AdvanceTemporaryForceContactPass` now scans before route movement; `OriginalStrategicTemporaryForceEncounter` and `Campaign.OriginalStrategicPatrolEncounter` bind the direct counters and settlement to the existing tactical screen. Focused contact and settlement tests preserve strict distance, slot order, cooldown, and the fixed slot-1 reward. Source: `0x3B2A0-0x3B468`, `0x3B816-0x3B841`, `0x3C290-0x3C2AC`, and decoded object-2 double `+0x5654`.

The patrol battle's source write-back is **Confirmed** and bound to the runtime. `0x3B4BB-0x3B547` clamps each returned counter below zero to zero; resolver return 1 zeroes the patrol's three counters, an empty patrol forces success, and an empty player army forces failure. On success, `0x3B54C-0x3B565` writes the surviving player counters through `0x29DE0`; `0x3B581-0x3B591` writes the returned patrol counters to record `+0x1C/+0x20/+0x24`. The terminal success branch `0x3B687-0x3B787` adds `random(100) + 50` to scope-zero variable 17, overrides that award to 40 for patrol slot 1, increments variable 24, clears descriptor-active `+0x14` and patrol-active `+0x00`, and releases its route pointer `+0x6C`. The failure branch `0x3B78D-0x3B810` increments variable 25 and marks descriptor `+0x18`; it distinguishes the engaged player record `AE6C` from an ordinary field record, routing the former to a loss modal and the latter through `0x29D24` removal. These are source mutations, not a license to identify variable 17 with the modern campaign wallet or to substitute the dated field-battle adapter. The runtime preserves player/force counter ownership, result-dependent route release, and the distinguished-record split; exact loss-modal presentation remains **Provisional**. Source: direct control flow `0x3B468-0x3B816`, shared resolver `0x258FC`, and source counter helpers `0x29D24/0x29DE0/0x29E58` in the same hashed executable.

## Promotion rule

A provisional value is promoted only when we can cite one of: an unambiguous static table and its code reference, a decoded resource with known semantics, a controlled repeated in-game observation, or agreement between executable behavior and independent documentation. Every promotion should update this file, `docs/fidelity.md`, and the relevant executable specification together.

`FieldBattlePointerControls` supplies mouse selection and visible order buttons to the replacement overhead battle; no original mouse-order equivalence is claimed. Source UI resource decoding confirms `CONFONT.CSF` masks, while the replacement generic labels use `PixelFont` because the imported ornate glyphs are illegible in the current synthetic layout.

Patrol terminal binding (2026-09-23): `0x3B4BB-0x3B810` shows returned force counters written on either resolver outcome and surviving player counters written only on success. The award path consumes `random(100)` before overriding slot 1 to 40. `0x3B5D5-0x3B645` clears the engaged player route and stores truncated current coordinates as its destination; `0x63EC0` sets x87 rounding toward zero. Descriptor `+0x18` is reset by creator `0x3B1E6` and set on terminal resolution. `Campaign.OriginalStrategicPatrolEncounter`, `OriginalStrategicTemporaryForceSlot.EncounterResultMarked`, and patrol settlement tests now carry these facts. The distinguished-player loss dialog contents remain **Provisional**.

Patrol distinguished-player loss presentation (2026-09-23): after setting descriptor result marker `+0x18`, source `0x3B7C5-0x3B7F2` calls modal helper `0x256A0` with object-2 arguments `0x55CC` and `0x4FD0`, invokes `0x106C0`, passes 2 to `0x1BF54`, and sets global `ADC0=1` before returning. This proves a modal boundary but does not establish a modern game-over state or the dialog text. `OriginalStrategicTemporaryForceSettlement.DistinguishedPlayerLossRequiresModal` now selects a dismissible `Screen.PatrolLoss` in `ConquerorGame.StrategicEncounter` for both automatic and interactive resolver exits. The host wording, Enter/Escape/click dismissal, and map return are **Provisional** presentation policy; no `CampaignEndReason` is invented.
