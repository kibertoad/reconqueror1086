# fn_00049124

A neutral name: the function at `0x00049124` in BLD-GOG-EN. It has no neutral names in other builds. It encodes a container entry as kind 3 and returns the stored length, or 0 when that fails [FND-RES-005].
