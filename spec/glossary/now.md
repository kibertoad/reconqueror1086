# now

A value from outside the game: a millisecond clock, `UINT32`, which the effect scheduler and the effect constructor read. What the original reads it from is (unknown); it advances as real time passes.
