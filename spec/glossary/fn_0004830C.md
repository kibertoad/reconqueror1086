# fn_0004830C

A neutral name: the function at `0x0004830C` in BLD-GOG-EN. It has no neutral names in other builds. It encodes a container entry as kind 2 and returns the stored length, or 0 when that fails [FND-RES-005].
