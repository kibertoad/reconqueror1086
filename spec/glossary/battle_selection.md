# battle_selection

The indices in `battle_units` of the units the player has selected, a list of `UINT8` in the order they were added, kept in the block the pointer at `0x000A9C84` points to, with the count in its first byte [FND-BATTLE-015, FND-BATTLE-018].
