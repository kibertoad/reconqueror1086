# vesa_800_available

A value from outside the game: 1 when the VESA BIOS sets mode `0x103`, 800 by 600 in 256 colours, `INT32`. The original asks through `0x000834F0` [FND-CONFIG-004]. It can change only between runs.
