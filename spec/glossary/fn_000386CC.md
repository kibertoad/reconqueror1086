# fn_000386CC

A neutral name: the function at `0x000386CC` in BLD-GOG-EN. It has no neutral names in other builds. The War Planning screen calls it when it opens, and calls `fn_000386A0` when it returns 0 [FND-ESTATE-002].
