# screen_buffer

The drawing buffer of the current display mode, one palette index per pixel, `screen_width` bytes per row; the pointer to it is kept in the global at `0x000B0840` [FND-MEDIA-003, FND-MEDIA-005].
