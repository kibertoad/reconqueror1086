# battle_control_rects

The three rectangles of the control strip of the field battle, each a list of four `INT16`, `[left, top, width, height]`, kept in the same block as `battle_rects` right after the unit rectangles [FND-BATTLE-015, FND-BATTLE-018].
