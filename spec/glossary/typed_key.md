# typed_key

A value from outside the game: the code of the waiting key, below 256 for a character. The original reads it from `0x00065388`, which removes the key [FND-SAVE-001]. It is read once after `key_waiting` is true.
