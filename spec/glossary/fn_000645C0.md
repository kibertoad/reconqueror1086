# fn_000645C0

A neutral name: the function at `0x000645C0` in BLD-GOG-EN. It has no neutral names in other builds. It takes the sound driver handle and a sample request and starts it, returning the voice handle [FND-SOUND-003].
