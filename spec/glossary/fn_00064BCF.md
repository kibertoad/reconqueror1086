# fn_00064BCF

A neutral name: the function at `0x00064BCF` in BLD-GOG-EN. It has no neutral names in other builds. It takes the sound driver handle and a voice handle, stops the voice and returns an error code [FND-SOUND-003].
