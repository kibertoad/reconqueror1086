# war_mode_width

A value from outside the game: the width of the display mode the field battle manages to switch to, 1024 or 800, or 0 when it keeps 640. The original derives it from the `WAR_MODE` setting and the display driver [FND-BATTLE-018]. It is read once for each interactive battle.
