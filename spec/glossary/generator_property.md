# generator_property

The property the hostile generator works with, `INT32`, a local of the generator that only the finder and the pursuit loop write. Its value at the start of a pass is (unknown): whatever the stack held [FND-STRATEGY-004].
