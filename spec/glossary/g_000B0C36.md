# g_000B0C36

A neutral name: the global `UINT8` at `0x000B0C36` in BLD-GOG-EN. It has no neutral names in other builds. One of three bytes that must all be nonzero for the hidden switches of the field battle [FND-BATTLE-016].
