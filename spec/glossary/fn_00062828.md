# fn_00062828

A neutral name: the function at `0x00062828` in BLD-GOG-EN. It has no neutral names in other builds. It reads conversation variable `v` of the table it receives into its third argument and gives 1, or gives 0 when `v` is out of range, as `get_variable` and `variable_in_range` of RULE-TALK-003 do [FND-STRATEGY-036, FND-TALK-006].
