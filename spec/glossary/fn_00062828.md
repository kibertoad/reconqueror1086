# fn_00062828

A neutral name: the function at `0x00062828` in BLD-GOG-EN. It has no neutral names in other builds. It reads conversation variable `v` of the conversation state it receives [FND-STRATEGY-036].
