# fn_000258FC

A neutral name: the function at `0x000258FC` in BLD-GOG-EN. It has no neutral names in other builds. It runs a field battle between the six troop counts it receives by reference, with a morale value and a second value, and returns 1 when the player wins [FND-STRATEGY-022, FND-STRATEGY-033].
