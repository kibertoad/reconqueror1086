# fn_00039900

A neutral name: the function at `0x00039900` in BLD-GOG-EN. It has no neutral names in other builds. The hostile pass calls it with the record and the person when a force arrives next to a person with no assignment whose flag bit 0 is set [FND-STRATEGY-003].
