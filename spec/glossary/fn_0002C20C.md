# fn_0002C20C

A neutral name: the function at `0x0002C20C` in BLD-GOG-EN. It has no neutral names in other builds. It returns the dword at offset 4 of the record the pointer at `0x0009ABEC` points to [FND-TOURNEY-005].
