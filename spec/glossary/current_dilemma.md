# current_dilemma

The dilemma the generation screen shows, a structure of FMT-PERSON-002, kept in the `0x84`-byte record the pointer at `0x000A95F0` points to: the number at `+0x00`, the scoring fields at `+0x5C`, the breakpoints at `+0x60` with their count at `+0x7C`, the modifiers at `+0x64` with their counts at `+0x68` [FND-PERSON-005].
