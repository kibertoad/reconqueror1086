# fn_0006E0F3

A neutral name: the function at `0x0006E0F3` in BLD-GOG-EN. It has no neutral names in other builds. It takes the sound driver handle and a voice handle and returns non-zero when that voice has finished [FND-SOUND-003].
