# fn_0006B3B4

A neutral name: the function at `0x0006B3B4` in BLD-GOG-EN. It has no neutral names in other builds. The game calls it with 0 once, at the start of a session, and seeds the generator with half its result, and again with the whole result before each prompt variant is drawn [FND-RNG-003].
