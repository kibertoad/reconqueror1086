# fn_0001B584

A neutral name: the function at `0x0001B584` in BLD-GOG-EN. It has no neutral names in other builds. It runs the dragon run of RULE-JOUST-003 and returns 0 when the run succeeds and 2 otherwise [FND-DRAGON-002].
