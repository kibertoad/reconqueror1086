# g_0009DED8

A neutral name: the global `INT32` at `0x0009DED8` in BLD-GOG-EN. It has no neutral names in other builds. The village exterior stores 1 in it on entry when it is 0, and 0 when the player leaves for the tournament, the map, the inn or the church [FND-UI-005].
