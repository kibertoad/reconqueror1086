# fn_00072119

A neutral name: the function at `0x00072119` in BLD-GOG-EN. It has no neutral names in other builds. It is called with 1, and then 3 when `DELAYVGA` is `ON`, before the display returns to 640 by 480 after a full-screen movie [FND-MEDIA-009].
