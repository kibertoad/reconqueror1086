# fn_00038678

A neutral name: the function at `0x00038678` in BLD-GOG-EN. It has no neutral names in other builds. It returns the dword at `+0x1C` of the block the pointer at `0x0009AE00` points to, a calendar field [FND-UI-004].
