# cyberman_present

A value from outside the game: 1 when a CyberMan controller answers the query the original makes through `0x00018080` and `0x000181E8`, `INT32` [FND-CONFIG-003]. It can change only between runs.
