# blit_tile

A function: draws tile `tile` of the season's atlas for the screen position `(x, y)`, clipped to the view from `(20, 7)` to `(403, 441)` [FND-STRATEGY-028]. Where the tile's image sits relative to the position is (unknown).
