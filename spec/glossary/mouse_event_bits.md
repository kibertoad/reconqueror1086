# mouse_event_bits

A value from outside the game: the bits the mouse driver passes to its callback, bit 1 for a primary press, 2 a primary release, 3 a secondary press and 4 a secondary release [FND-BATTLE-020]. It changes with each call.
