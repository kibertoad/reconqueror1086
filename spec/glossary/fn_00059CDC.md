# fn_00059CDC

A neutral name: the function at `0x00059CDC` in BLD-GOG-EN. It has no neutral names in other builds. The July collection calls it before the fight with Drogo and passes its result to `fn_0001070C` after [FND-ESTATE-003].
